# 카이로스 HTS 화면캡처 수집 파이프라인 — 설계 계획서

작성 2026-07-26. 계기: `data.krx.co.kr` 403 차단(당일 실측)으로 투자자별 수급·공매도 잔고가 끊김.
목표: 미래에셋 카이로스(HTS)가 떠 있는 원격 노트북의 화면을 캡처해 이 PC로 가져오고,
Cowork 분석가가 이미지를 읽어 수급·선물옵션 정보를 판단 근거로 쓴다.

---

## 0. 먼저: 이 계획이 지켜야 할 경계

- **매매 실행과 물리적으로 분리한다.** 카이로스 노트북에는 Pico HID 매매 매크로가 이미 있다.
  이 파이프라인은 **읽기(캡처) 전용**이며, 캡처 에이전트는 매매 모듈을 import 조차 하지 않는
  별도 프로세스로 둔다. 캡처 요청으로 주문이 나갈 수 있는 경로를 만들지 않는다.
- **이 시스템은 실제 9명에게 메일을 보낸다.** 캡처에서 읽은 숫자가 추천 근거가 되므로,
  '읽었다'와 '맞다'를 구분하는 검증이 데이터 자체보다 중요하다.
- 캡처 이미지는 계좌 잔고·보유종목이 함께 찍힐 수 있다. **잔고 화면은 캡처 대상에서 제외**하고,
  대상 화면도 계좌 영역이 보이면 마스킹 후 저장한다.

---

## 1. ★설계 정정 — Funnel 대신 tailnet 직결

사용자 구상은 "이 PC에서 Tailscale **Funnel**로 서버를 열고 노트북이 이미지를 보낸다"였다.
**Funnel은 공개 인터넷 노출이다.** 실측: 현재 이 PC(`pc21`)는 이미 Funnel 2개가 켜져 있다.

```
https://pc21.tail375e52.ts.net       -> 127.0.0.1:9000
https://pc21.tail375e52.ts.net:8443  -> 127.0.0.1:8001
```

여기에 이미지 업로드 엔드포인트를 Funnel로 추가하면 **인터넷의 누구나 가짜 스크린샷을 POST**할 수 있고,
그 이미지는 곧바로 "9명에게 발송되는 추천"의 입력이 된다. 이 시스템의 사명이
'가짜 정보로 잘못 매수해 피해보는 사람을 없애는 것'인데, 정반대의 구멍을 스스로 뚫는 셈이다.

**대안(권고): Funnel 불필요.** 이미 tailnet 계정이 있고 Windows 기기들이 등록돼 있으므로,
카이로스 노트북을 같은 tailnet에 넣으면 `100.x.x.x` 사설 주소로 직접 통신된다.

| 방식 | 노출 | 권고 |
|---|---|---|
| Funnel | 공개 인터넷 | ✗ 쓰지 마라 |
| `tailscale serve` | tailnet 내부만 | 가능 |
| tailnet IP 직결(`100.x:PORT`) | tailnet 내부만 | **○ 이게 가장 단순** |

추가 방어(모두 적용): 서버는 `127.0.0.1`/tailnet IP에만 bind(`0.0.0.0` 금지) ·
공유 시크릿 헤더(`X-Capture-Token`, 파일은 `*_api.txt` 와 동급 취급 → gitignore) ·
요청 기기 식별 로그.

---

## 2. ★방향 정정 — 노트북이 보내지 말고, 이 PC가 가져온다(pull)

| | push (노트북 → PC) | **pull (PC → 노트북)** |
|---|---|---|
| 시점 통제 | 노트북 스케줄에 종속 | **PC가 원할 때** = 06:30 룩어헤드 규율에 맞춤 |
| 재시도 | 어려움 | 실패 시 즉시 재요청 |
| 서버 위치 | PC에 업로드 수신 서버 | 노트북에 캡처 에이전트 |
| 이 PC의 Funnel | 건드려야 함 | **안 건드림** |

즉 **노트북에 작은 캡처 에이전트(HTTP)를 띄우고, 이 PC가 호출**한다.

```
[이 PC] hts_capture_collect.py
   GET http://<노트북 tailnet IP>:8710/capture?screen=investor_flow
        헤더 X-Capture-Token: <시크릿>
                    |
                    v
[노트북] capture_agent.py  (읽기 전용 프로세스)
   1) 화면 전환: Pico HID 로 카이로스 화면번호 입력  ※매매 매크로 미사용
   2) 안정화 대기(고정 지연 + 화면 변화 정지 감지)
   3) 지정 창 영역 캡처 → PNG
   4) 응답: PNG + 메타(JSON 헤더): screen_id, captured_at_kst, 해상도, sha256
```

---

## 3. 캡처 대상 — KRX가 막혀 잃은 것과 1:1 매핑

우선순위는 "대체 소스가 없는 것"부터다(2026-07-26 실측 기준).

| # | 카이로스 화면 | 메우는 공백 | 대체 소스 유무 | 우선 |
|---|---|---|---|---|
| 1 | 매매동향 > **종목별 외국인/기관 매매현황** | `force_analysis` 종목 수급, 신규 '전일 투자자별 수급' 메일 표 | 네이버 근사 있음(수량 단위) | **P0** |
| 2 | **공매도 잔고/추이** (있다면) | `short_collect` — pykrx `get_shorting_balance_by_date` rows=0 | **없음** | **P0** |
| 3 | 매매동향 > **투자자 매매현황**(시장 전체) | `flow_collect` 시장 수급 | 네이버 근사 | P1 |
| 4 | 선물옵션 > **선물옵션 시세** | `night_futures_collect` 보완(베이시스·미결제) | investing 있음 | P2 |

2번(공매도)이 이 계획의 최대 가치다. 나머지는 네이버 폴백으로도 어느 정도 메워지지만
공매도 잔고는 KRX 독점이라 현재 완전 공백이다.

---

## 4. ★신뢰성 설계 — '엉뚱한 화면을 찍는 것'이 최대 위험

HTS 자동화의 지배적 실패는 접속 실패가 아니라 **잘못된 화면이 조용히 찍히는 것**이다:
로그인 세션 만료 → 로그인 창, 공지 팝업, 창 위치·DPI 변경, 화면 전환 지연.
이때 캡처는 "성공"하고 파일도 정상이며, 분석가만 엉뚱한 숫자를 읽는다.

**대책: 화면 검증 마커(자기검증).** `night_futures_collect.py` 의 `session_guess`
(체결시각을 KST로 환산해 그 값이 간밤인지 주간인지 스스로 판정)와 같은 설계를 쓴다.

- 모든 캡처에 **화면 제목/화면번호 영역을 반드시 포함**해서 찍는다.
- 수신 측이 그 영역을 읽어 기대 화면과 대조 → 불일치면 **그 캡처를 폐기**하고
  `capture_status="marker_mismatch"` 로 남긴다(조용히 통과 금지).
- 화면에 표시된 **조회 기준일자**도 함께 읽어, 기대 영업일과 다르면 `stale` 로 표시.

**실패는 소리나게.** 오늘 KRX가 403 차단인데 pykrx가 예외 없이 `rows=0` 을 돌려주는 바람에
'차단'이 '데이터 없음'으로 조용히 둔갑하는 것을 확인했다. 같은 실수를 반복하지 않는다.

```
capture_status ∈ { ok | marker_mismatch | stale | unreachable | agent_error | disabled }
```
`ok` 가 아니면 그 화면의 값은 **아예 산출하지 않는다**(0 으로 채우지 않는다 —
retro_label 의 `dist_disc_count` 계약과 동일: 0 = 확인된 값, 부재 = 확인 불가).

---

## 5. 이미지 → 숫자: Cowork vision + 교차검증

두 안이 있다.

- (a) **Cowork(Claude)가 이미지를 직접 읽는다** — 표 구조 이해에 강하다. 숫자 오독 위험.
- (b) 로컬 OCR(tesseract 등) → JSON — 결정적이지만 HTS 폰트·색상·음수 표기에서 깨지기 쉽고
  유지보수 대상이 하나 더 는다.

**권고: (a) + 교차검증 게이트.** 이유는 이 시스템이 오늘 겪은 사고 때문이다 —
수급 필드를 억원으로 착각해 `+3,803,330조` 가 찍혔다(발송 직전 발견). 이미지에서 읽은 숫자는
그보다 검증이 약하므로, 반드시 다음을 통과해야 한다.

1. **겹치는 값 대조**: 캡처 화면의 종가·등락률을 FDR/네이버 값과 비교. 불일치하면
   **그 캡처 전체를 불신**(한 숫자가 틀리면 같은 표의 다른 숫자도 못 믿는다).
2. **단위 명시 의무**: 화면의 단위 표기(주/천주/백만원/억원)를 값과 함께 기록.
   단위를 못 읽었으면 값을 버린다.
3. **크기 sanity**: `retro_label.flow_unit_check` 와 같은 상한 검사.
4. **출처 태깅**: 모든 파생값에 `_src="hts_capture"` + `confidence`.

---

## 6. 파이프라인 배선

신규 파일 2개(+에이전트 1개는 노트북 측):

```
capture_agent.py          # 노트북. 읽기 전용. 매매 모듈 미참조.
hts_capture_collect.py    # 이 PC. 다른 수집기와 동일 규약:
                          #   독립 실행 · 부분실패 exit 0 · UTF-8 · ensure_ascii=False
                          #   common.save_json_atomic · 이모지 금지(BMP만)
```

산출물(세션 폴더):
```
output/<세션>/hts_captures/<screen_id>_<YYYYMMDD_HHMM>.png
output/<세션>/hts_capture.json     # 메타+검증결과. 이미지 경로·상태·마커판정·기준일자
```

`supervisor.py` 삽입 위치: **step9(mirae_collect) 직후 = step9.5**.
`short_collect`(step10)·`flow_collect`(step12)보다 앞에 둬서, 그 수집기들이 KRX 실패 시
캡처 결과를 폴백으로 참조할 수 있게 한다.

세션 폴더에 직접 저장하므로 루트 신호 6종과 달리 `snapshot_signals` 동결은 불필요하다.

> 현재 운영은 **코워크 예정작업 온디맨드**이고 supervisor 데몬은 상시 오프다.
> 따라서 실제 실행 경로는 예정작업이 `hts_capture_collect.py` 를 직접 호출하는 것이며,
> supervisor 배선은 데몬 복귀 시를 대비한 것이다.

분석 지시문: `cowork_instructions.md` 에 **[5.15] HTS 캡처 이미지 읽기** 신설.
- `capture_status != ok` 인 화면은 없는 것으로 취급하라
- 이미지에서 읽은 값은 `[캡처]` 로 표기하고 **API 값과 섞어 쓰지 마라**
- 교차검증 실패 시 그 화면 전체 폐기
- **처음에는 규칙화 금지 · 참고 힌트로만** (야간선물 [5.14]와 같은 규율)

## 7. 회고 연결 — 처음에는 관찰 전용

캡처 값을 곧바로 `pre_*`(진입 피처)로 올리지 않는다. 이유:
캡처는 실행 시각에 따라 값이 달라지고(장중 실시간), 마커·OCR 실패율이 아직 미측정이다.
룩어헤드 규율(06:30 이전 데이터만)을 만족하려면 **캡처 시각이 데이터에 박혀 있어야** 한다.

단계: `hts_capture.json` 에 캡처 시각·기준일자를 남겨 **관찰만** →
2주 이상 `ok` 비율과 교차검증 통과율을 측정 → 그 뒤에야 회고 컬럼 승격을 논의.
(원장 승격 요건: 서로 다른 2개 이상 국면 재현 + prediction 서브샘플 방향 유지 + 신규 만기 3회+)

---

## 8. 단계별 실행 계획

| Phase | 내용 | 완료 기준 |
|---|---|---|
| **0. 사전 확인** | 카이로스 노트북을 tailnet 가입·상시 온라인 확인. 카이로스에 공매도 화면이 있는지 육안 확인. 캡처 대상 4종의 화면번호·창 좌표 기록 | 노트북이 `tailscale status` 에 online, 화면번호 목록 확정 |
| **1. 수동 1장** | 에이전트 없이, 노트북에서 손으로 1번 화면을 캡처해 이 PC로 옮겨 Cowork가 읽어본다 | **Claude가 그 표의 숫자를 정확히 읽는지** 실측(여기서 실패하면 계획 전면 재검토) |
| **2. 캡처 에이전트** | `capture_agent.py` + 토큰 + 마커 검증. 화면 1종만 | PC에서 GET 1회로 PNG+메타 수신, 마커 불일치 시 거부되는 것 확인 |
| **3. 수집기 배선** | `hts_capture_collect.py`, 세션 저장, `capture_status` 5종, 교차검증(종가 대조) | 라이브 1회 실행 → `hts_capture.json` 생성, 검증 게이트 통과 |
| **4. 화면 확대** | 나머지 3종 추가 | 4종 모두 `ok` |
| **5. 분석 배선** | `cowork_instructions.md` [5.15] + 하네스 테스트 | 아침 리포트에 `[캡처]` 근거가 실림 |
| **6. 관찰·평가** | 2주 `ok`율·교차검증 통과율 측정 → 원장 등재 | 회고 승격 여부 판단 |

Phase 1에서 멈출 수 있다는 점이 중요하다. **Claude가 카이로스 표를 정확히 못 읽으면
나머지 인프라는 전부 무의미**하므로, 가장 싼 실험을 가장 먼저 한다.

---

## 9. 리스크 대장

| 리스크 | 영향 | 완화 |
|---|---|---|
| 노트북 오프라인 | 그날 수급 공백 | `unreachable` 명시 + 네이버 폴백 유지. 캡처를 유일 경로로 삼지 않는다 |
| HTS 세션 만료 → 로그인 화면 캡처 | **오독 위험 최대** | 화면 검증 마커(필수) |
| 창 위치·DPI·해상도 변경 | 좌표 깨짐 | 창 핸들 기준 캡처 + 마커. 좌표 하드코딩 최소화 |
| 실시간 화면이라 캡처 순간 값 | 재현 불가 | 캡처 시각 KST 기록. 장 시작 전(06:30)엔 전일 확정치만 대상 |
| Claude 숫자 오독 | 잘못된 추천 | 교차검증 게이트 + 단위 명시 의무 + `[캡처]` 표기 분리 |
| 캡처에 계좌 정보 노출 | 개인정보 | 잔고 화면 제외, 계좌영역 마스킹 |
| Funnel 오용 | **외부 주입** | Funnel 미사용, tailnet 전용 + 토큰 |
| Pico 매매 경로 오작동 | 실주문 | 캡처 에이전트가 매매 모듈 미참조. 별도 프로세스 |

---

## 10. 열린 질문(사용자 확인 필요)

1. 카이로스 노트북이 지금 tailnet에 들어 있나? (`tailscale status` 상 Windows 2대가
   각각 3일·48일 전 오프라인 — 둘 중 하나가 그 노트북인지)
2. 카이로스에 **공매도 잔고 화면**이 있나? (이 계획의 최대 가치가 여기에 달려 있다)
3. 노트북은 24시간 켜 두나, 아침에만 켜나? (캡처 시각 설계가 갈린다)
4. Pico HID 화면 전환을 이 파이프라인이 호출해도 되나, 아니면 화면 4개를 **미리 띄워두고
   캡처만** 하나? (후자가 훨씬 안전하다 — 권고)
