# 시스템 전면 감사 보고서 (Fable 5 — 2026-07-27)

6개 독립 차원(과적합·사고방법론·통계·무결성·정합성·실성과) 병렬 감사, 41건 발견.
반박 검증 에이전트 9개가 세션 한도로 실패해 **P0 전건과 상위 P1은 호스트가 코드·데이터로
인라인 재검증**했다(검증 방법을 각 항목에 명시). 이 문서는 원장(RETRO_ISSUES)의 상위 요약이다.

---

## 한 줄 결론

**시스템의 학습 인프라(폐루프·원장·게이트)는 잘 지어졌지만, 그 위에서 계산되던 핵심 판정축
(alpha)과 진입피처 6종이 오염돼 있었고, 실측 성과는 아직 배포 최소선에 못 미친다.**
P0 4건은 당일 수리 완료(A31~A33). 정확도의 다음 병목은 코드가 아니라
**규칙 과잉(과적합)과 표본 부족**이다.

---

## P0 — 당일 수리 완료 (검증: 코드 직접 확인 + 합성 데이터 실증 + 하네스 고정)

### 1. 알파 지수 앵커 비대칭 (A31) — 두 차원이 독립적으로 발견
종목 다리는 `entry_ref`=**D-1 종가**, 지수 다리는 **D 종가**(holding_review 는 D+1) 앵커.
→ 추천일 하루치 지수 변동이 통째로 '종목선택(alpha)'으로 오귀속.
주말 추천 뒤 월요일 -8.95% 폭락(07-13)이 전부 "종목 탓"으로 계산된 실측 —
**만기 9건 중 6건의 알파 부호가 반전**될 크기다. 하락기에는 픽 alpha 과소·숏 alpha 과대의
한 방향 편향. 세 계산기(retro_label·accuracy_tracker·holding_review) 전부 D-1 앵커로 통일.
**재판정 대상**: 19회차 '테마집중 경고', A24 숏 alpha 우위 수치, 분배합류 경고 수치.

### 2. pre_tech 6종 당일 종가 포함 (A32)
`pre_rsi14`·`pre_up_streak`·`pre_overheat` 등이 추천일 **당일 종가**로 계산됨(FDR end 포함 경계).
06:30 시점에 당일 종가는 미래다. A22(pre_kospi_ret5d)와 동일 클래스인데 그 수리에서 빠졌던 것.
'개별 과열피처 비지지 9회'·즉시고점 판별 분석이 전부 이 오염 피처 위였다. → base 미만 봉만.

### 3. flow/fsc 잔여 당일 혼입 (A33)
`get_flow_asof` 가 asof 당일 포함(백필 시 D일 수급 유입), fsc FDR 전면 폴백이 end 무시(오늘까지
조회 — 장중 실행 시 미확정 부분봉 유입). 둘 다 차단 + 하네스 고정.

### 4. 실성과 — 배포 최소선 미달 (측정 재현 확인, 판정은 유지하되 alpha 는 재측정 대상)
표본 수치는 재계산으로 정확히 재현됐다(만기 330행·픽 257·실발송 102):
- 픽 적중 38.5%, **실발송 픽 적중 26.5%·만기 평균 -7.4%**
- 시장콜 T+1 28%(무정보 벤치 59%)·T+5 21%(벤치 96%)·Brier 0.617(무정보 0.667보다 겨우 나음)
- 유일한 안정 축은 메가캡 픽과 숏 — 단 그 근거인 alpha 가 위 A31 로 오염돼 있었으므로
  **교정 후 재측정이 선행**이다(이 문서 갱신란에 결과 기록).
**판정: 현재 상태로 외부 배포는 사명('잘못된 매수로 인한 피해 축소')에 반한다.**
배포 최소선(프리레지스터): ① prediction 픽 종목클러스터 alpha 평균>0 이 t>2 ②
시장콜 Brier<0.667 지속 ③ 상승·하락 2개 국면 모두에서 유지 — 셋 다 미충족.

---

## P1 — 구조 결함 (당일 일부 수리, 나머지는 아래 로드맵)

| # | 발견 | 상태 |
|---|---|---|
| 5 | k5 게이트 '금지 vs 유지' 정면충돌(F6③ vs [3-차익실현](5)·[6.3]) — 강세추격일마다 복불복 | **수리 완료**(경고 수준으로 통일) |
| 6 | [장기]/40 enum 이 6곳에서 구 3값으로 잔존 | **수리 완료**(전파) |
| 7 | 확신도 규율 상호모순 — calibration 기반 하향 명령([0.5]/[6.3]/[7.5]) vs 금지(F7/[4.8](7)) | 로드맵 |
| 8 | vol_ratio>2.5 '최고 등급' 지위가 아카이브 3일·8행 표본 위 + 1.8~2.5 '무방' 구간이 현 데이터와 불일치 | 로드맵(재검) |
| 9 | 동일 종목 연속 재추천이 상관 손실 증폭(기아 8연속) + 열린 픽 26%가 2종목 | 1.95 섹션이 노출 시작 — 관찰 |
| 10 | 같은 종목에 픽과 숏 동시 발행(수신자에게 모순 시그널) | 로드맵(발송 게이트 후보) |
| 11 | calibration 표가 픽·숏·시장콜을 단일 표로 합산(구성 아티팩트) | 로드맵 |
| 12 | '확실' 판정이 매체 개수(2+)만 세고 출처 독립성·공시 원문 대조 미요구 — **사명 직결 격차** | 로드맵(우선) |
| 13 | B6~B14 백테스트 게이트군이 단일 19일 백테스트 근거인데 원장 생애주기 밖 | 로드맵(원장 편입) |
| 14 | 1877줄 지시문 인지 부하 + 전역 우선순위 규칙 부재(모순이 편집을 살아남는 구조적 이유) | 로드맵 |
| 15 | 폐루프 학습 효과 미검출 — 19회 회고에도 후반부 실발송 성과가 더 나쁨 | alpha 교정 후 재측정 |

## P2/P3 요지 (22건 — 원장·차기 배치)

- **통계 규율**: §3 16개 축 매회 전수 검정 = 회차당 우연 발견율 ~56%인데 단일 회차 p<0.05 로
  '관찰' 티어 통과 / 소표본(N>=3) % 자동 판정 / 코스닥 픽 30.4%에 KOSPI 벤치마크 /
  scorecard 채널에 클러스터 보정 없음(회고 원장 규율과 불일치)
- **낡은 수치·죽은 절차**: A24 '해결' 표기인데 예측채점_리포트는 여전히 기저율 없는 '숏 90%' /
  데몬 전제 핸드셰이크 절차 잔존 / F2 '즉시고점 94건' 산술 불성립(17+86=103≠94, 실측 131) /
  retro_feedback 에 틀린 결측 주장 잔존 / 루트 신호 '5종 vs 6종' 표기 불일치
- **과적합 신호**: 테마캡 '3개 이상'이 관측 손실 클러스터(정확히 3건)에 사후 맞춤 + 승격 선후
  역전 / VKOSPI 25/14/+30%·credit +15bp 등 실측 0회 임계값이 처방 규칙으로 상설
- **시간축 3중 불일치**: [6.5+] '4필드 모두' vs [7.5] '2필수+2권장' vs 검증기 '2필수'

---

## 정확도 로드맵 (권고 우선순위)

1. **[완료] 측정을 먼저 고쳐라** — A31~A33. 오염된 자로 잰 성과 위에서 규칙을 다듬는 것은
   과적합을 가속할 뿐이다.
2. **규칙 다이어트** — 다음 회고에서 B표 전 규칙에 '도출 표본·국면 수' 명시 → 승격 요건
   미달 규칙을 '관찰'로 강등(특히 vol_ratio 등급, B6~B14 원장 편입, 실측 0회 임계값 처방 해제).
   규칙을 더하는 것보다 **빼는 것이 지금 정확도에 더 기여한다**(41건 중 과적합·모순 계열이 15건).
3. **통계 최소선** — scorecard 에 클러스터 병기(행/종목/일 3종), 소표본 자동판정 N>=10 승격,
   §3 다중검정에 '2회차 연속 재현 전 관찰 금지 승격' 명문화, 코스닥 픽 KQ11 차감 병기.
4. **사명 격차** — '확실' 판정에 출처 독립성(같은 통신사 재전송 = 1개) + DART 원문 대조 의무를
   지시문 본문으로 승격(현재 비Fable 폴백 파일에만 있음).
5. **배포 판단** — 위 최소선 3조건이 2개 국면에서 충족될 때까지 외부 배포 보류.
   그 전까지의 가치는 '추천'이 아니라 **검증 이력이 쌓인 시스템** 그 자체다.

---

## 갱신란 (교정 후 재측정)

- [x] **alpha 교정 후 재측정 완료(2026-07-27, 만기행·리베이스라인 검증 통과)** —
  불변 라벨 9종 0행 변경, alpha/kospi 338행·pre_tech 230~295행 재계산(예상과 정확히 일치).

  | 표본 | 교정 전 alpha | 교정 후 alpha | 해석 |
  |---|---|---|---|
  | 픽 전체 (n=262) | -0.61% (중앙 -0.96, t=-0.76) | **+0.27% (중앙 +1.10, t=0.32)** | 음→양 전환 |
  | 실발송 픽 (n=115) | +0.63% (t=0.58) | **+1.81% (중앙 +1.53, t=1.80, >0 비율 55.7%)** | 배포조건① t>2 에 근접(미달) |
  | 숏 전체 (n=76) | -2.77% (t=-2.00) | **-2.15% (t=-1.38)** | 우위 축소·유의성 상실 |

  **판정 갱신**: 감사가 예측한 편향 방향 그대로였다 — 구 버그는 픽의 종목선택을 체계적으로
  **과소평가**하고 숏 우위를 **과대평가**했다. 시스템은 알려진 것보다 종목을 잘 고르고 있었고
  (실발송 픽 alpha +1.81%), A24 '숏 alpha 우위 -2.3%'는 과대였다(-2.15%·비유의).
  ★단 **절대수익 -7.4%는 불변** — alpha 양수는 '지수보다 덜 잃었다'이지 번 게 아니다.
  배포 최소선 3조건은 여전히 미충족(①t 1.80<2 ②Brier ③2국면) — **배포 보류 판정 유지**.
- [x] **반복 추천 클러스터 재판정(2026-07-27, 교정 앵커 holding_review 재실행)** —
  | 종목 | 구앵커 | 교정 후 | 판정 |
  |---|---|---|---|
  | 기아(8회) | -12.02 (음 8/8) | **-8.12 (음 8/8)** | **논지 실패 유지**(07-12 진입분만 -9.8→-1.0 완화 — 07-13 폭락 오귀속분. 07-17 이후는 여전히 -9~-11%p) |
  | 알테오젠(3회) | -1.13 (음 1/3) | **+3.37 (음 0/3)** | **★판정 반전** — 절대 -18.8% 는 전부 시장 베타. 종목선택은 오히려 우위였다 |
  | 삼성전자(6회) | -5.04 (음 5/6) | -3.49 (음 6/6) | 실패 유지(더 일관) |
  | 셀트리온(16회) | +10.97 (음 4/16) | **+12.76 (음 0/16)** | 성공 더 견고(음수 사례 소멸) |
  | 삼성바이오(18회) | +18.26 | **+20.04** | 최강 유지 |
- [ ] 19회차 '테마집중 경고' 재판정 (다음 회고 — 교정 데이터로)
- [ ] A24 숏 alpha 우위 수치 재판정 (다음 회고)
