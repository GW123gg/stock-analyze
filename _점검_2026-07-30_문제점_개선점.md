# 2026-07-30 온디맨드 실행 점검 — 문제점과 개선점

작성: 아침 리서치 세션 `output\2026-07-30_063659` 실행 중 관측 + 코드 확인.
전부 **실측/코드 근거**를 붙였다. 라이브 코드는 하나도 수정하지 않았다(지시 대기).

---

## A. 심각 — 채점·판정의 신뢰도를 직접 훼손

### A-1. 시장콜 채점 앵커가 한 세션 어긋난다

`accuracy_tracker.py::_index_return(..., anchor_before=False)` — 시장콜은 `c0 = base_date(=발행일 D) 종가`,
`ch = D+h 거래일 종가`로 채점된다.

그런데 리포트는 **D-1 종가를 기준으로** 레인지·지지·저항·무효화를 적는다. 오늘 예: 리포트의
"현재 5,663.24 → T+5 5,050~6,300"의 5,663.24는 **07-29(D-1) 종가**다. 리포트는 06:30에 발행되고
독자가 실제로 거래하는 첫 세션은 D(07-30)인데, 채점기는 그 D 세션을 시작점으로 삼아 제외한다.

실측 확인 (`accuracy_log.json`):

| pred_date | horizon | settle_date | index_return_pct | 실제로 잰 구간 |
|---|---|---|---|---|
| 2026-07-28 | 1 | 2026-07-29 | -5.98 | 07-28 종가 → 07-29 종가 |
| 2026-07-25(휴장) | 1 | 2026-07-28 | -10.84 | 07-27 종가 → 07-28 종가 |

즉 07-28 아침에 발행한 콜은 **07-28 세션(-10.84%)을 건너뛰고** 07-29 세션(-5.98%)으로 채점됐다.
오늘 콜도 07-30 세션이 아니라 07-31 세션으로 채점된다.

픽 쪽은 v10.5(2026-07-27)에서 이미 D-1 앵커로 교정됐다(`anchor_before=True`). 같은 파일 주석이
"시장콜 정의는 불변(이력 채점 일관성)"이라고 명시하는데, **불변으로 둔 쪽이 리포트가 실제로
예측하는 대상과 다르다.** 하루 -6~-11% 움직이는 국면에서 이 한 칸은 방향 판정 자체를 뒤집는다.
시장 방향 T+5 적중률 27%의 일부가 여기서 나올 수 있다.

**제안(비파괴)**: 기존 계열을 지우지 말고 `kind="market_v2"` 항목을 병렬로 추가해
`anchor_before=True`(D-1 → D+h)로 같이 채점하라. 두 계열이 몇 주 쌓이면 회고가 어느 쪽이 실제
예측 대상인지 실측으로 판정할 수 있다. 지금 정의를 갈아치우면 300여 건 이력과 비교가 끊긴다.

### A-2. run_signals의 신선도 판정이 절반만 구현돼 있다

`CLAUDE.md`는 "(가) 이번 실행으로 mtime이 갱신됐는가 (나) `asof_date`/`generated_at`이 전 거래일인가
**두 가지로** 판정하라"고 한다. 그런데 `run_signals.py:172-184`는 **(가) mtime만 판정**하고,
asof는 요약표에 **찍기만** 한다. 판정 로직에 asof가 전혀 들어가지 않는다.

더 나쁜 것은 `ASOF_KEYS = ("asof_date", "asof", "latest_trade_date", "generated_at")`에
`generated_at`이 포함된 점이다. 수집 시각을 asof로 노출하는 수집기는 **항상 최신으로 보인다.**

오늘 실측 오탐:

| 단계 | 요약표 표시 | 페이로드의 실제 데이터 시점 | 실제 지연 |
|---|---|---|---|
| short | `OK 2026-07-30T06:44:16` | `tickers[].asof = 2026-07-27` | **2거래일** |
| nightfut | `OK 2026-07-30T06:44:30` | `last_trade_kst = 2026-07-28 14:00`, `session_guess=day` | **사용 불가** |
| vkospi | `OK 2026-07-28` | `asof_date = 2026-07-28` | 1거래일 (표시는 맞으나 OK 통과) |
| credit | `OK 2026-07-28` | `asof_date = 2026-07-28` | 1거래일 (동일) |

**이것이 2026-07-29 사고(short.json이 5일 전 값인 채로 근거에 쓰인 일)를 러너가 여전히 못 잡는다는
뜻이다.** 오늘 잡힌 이유는 러너가 아니라 내가 각 json을 직접 열어 per-ticker asof를 확인했기 때문이다.

**제안**:

1. 페이로드별 '데이터 시점 추출기'를 만들어라 — `short` → `min(tickers[].asof)`,
   `night_futures` → `last_trade_kst` + `session_guess`, `flow_data` → `min(tickers[].asof)`.
   `generated_at`은 **마지막 폴백**으로만.
2. `LAG(n거래일)` 판정을 신설해 asof가 전 거래일이 아니면 요약표에 지연 거래일 수를 찍어라.
   지금은 분석가가 그 숫자를 리포트에 쓰라고 요구받지만, 러너가 그 숫자를 주지 않는다.

### A-3. mirae 단계의 출력 경로 불일치 — 내 오판을 유발했다

`logs\sig_mirae.log` 마지막 줄: `[mirae] 저장 완료: ...\output\2026-07-30_063659\mirae_data.json`
→ 수집기는 **세션**에 저장한다. 그런데 `run_signals.py:63-64`의 스텝 spec은 `root:mirae_data.json`이다.

결과: 루트에 남은 **07-12 잔존 파일**이 STALE로 잡히고, 세션의 당일 파일(06:44:05 생성, 24KB)은
아예 평가되지 않았다.

**이 때문에 발송된 메일에 부정확한 문장이 들어갔다.** 나는 러너 표만 믿고 데이터 신뢰도 칸에
"미래에셋 수급 파일은 07-12 생성분이라 13거래일 지연되어 사용하지 않았다"라고 썼다. 정확한 사실은
"오늘 06:44 생성됐으나 미래에셋 API 미발급으로 yfinance 폴백이 돌아 `investor` 필드가 58종 전부
비어 있어 사용하지 않았다"이다(실측: `investor` 채워진 종목 0/58). 결론(수급은 force_scores로 판단)은
같지만 **사유 서술이 틀렸다.**

**제안**: spec을 `session:mirae_data.json`으로 고치고, 루트의 07-12 잔존 파일은 삭제하거나
`_retired`로 옮겨라. 두면 다음 회차에도 같은 오판을 유발한다.

---

## B. 중간 — 운영 안정성

### B-1. run_signals.py 요약표 print가 cp949에서 죽는다

1차 실행이 mirae 단계에서 죽었다:

```
File "run_signals.py", line 240, in main
    print("   %-6s rc=%s %.1fs %s" % (res["verdict"], res["rc"], res["took"], ...
UnicodeEncodeError: 'cp949' codec can't encode character '\u2014' in position 36
```

STALE note의 em-dash(`—`)가 원인이다. 19단계 중 10단계까지 돌고 죽어서 short 이후 9단계
(short·hts·taildrop·vkospi·nightfut·credit·earnings·caution·snapshot·holdrev)가 통째로 누락됐다.
`PYTHONIOENCODING=utf-8`을 주고 재실행해 완주했다.

하위 수집기는 이미 `subprocess.run([PY, "-X", "utf8"] ...)`로 UTF-8을 강제하는데(`:161`)
**러너 자기 자신만 빠져 있다.**

**제안**: `run_signals.py` 진입부에 한 줄.

```python
try: sys.stdout.reconfigure(encoding="utf-8", errors="replace")
except Exception: pass
```

### B-2. taildrop marker 검증 regex가 새 파일명 규칙과 안 맞아 27장 전량 폐기

`taildrop_receive.py:160`:

```python
m_no = re.match(r"^(\d{4})_", base)     # 0231_short_lend.png 를 가정
if m_no and marker and ("[%s]" % m_no.group(1)) not in marker:
    rejected.append(... "marker 불일치 — 기대 [%s]" ...)
```

노트북은 배치 전송 시 `HHMM_화면번호_라벨.png` 형식으로 보낸다(실측 파일명:
`1937_0254_investor_daily_call.png`, `0120_0313_basis.png`). regex가 첫 4자리 = **전송 시각**을
화면번호로 오인해 marker에서 `[1937]`을 찾다가 100% 실패한다.

실측: zip 4개, `n_ok=0 / n_rejected=27`, 폐기 사유 전부
`marker 불일치 — 기대 [1937], 실제 '[0254] 투자자 일별 매매현황'`.
**캡처는 정상이었고 검증기만 틀렸다.** 공매도·대차잔고 보강 경로가 이 한 줄 때문에 통째로 끊긴다.

**제안**:

```python
m_no = re.match(r"^\d{4}_(\d{4})_", base) or re.match(r"^(\d{4})_", base)
```

2중 접두사 형식을 먼저 시도하면 기존 단일 접두사도 그대로 동작한다.

### B-3. hts_capture_collect의 별개 실패 — Kairos HTTP 409

`logs\sig_hts.log` 전문: `[htscap] 예기치 못한 오류(무시): KairosError: HTTP 409 (/watchlist)`

관심종목 등록(0231/0261용)에서 409 Conflict가 나서 `hts_capture.json`이 아예 생성되지 않았다(SKIP).
B-2와 **독립된 문제**다. 즉 캡처 경로는 두 곳에서 끊겨 있다.

**제안**: 409는 '이미 등록됨'일 가능성이 높다. 등록 전 조회 후 스킵하거나, 409를 성공으로 간주하고
진행하도록 멱등 처리하라.

### B-4. MCP 온디맨드 실행 규약이 실제 환경과 안 맞는다

마스터 지시서 A-0은 `wait_ms=30000`으로 job_id를 받고 `wait_job` 폴링을 지시한다. 그런데 이 환경의
MCP 요청 타임아웃이 60초 미만이라 `wait_job`이 `-32001`로 끊기고 **job 추적이 사라진다.**
run_signals 1차 시도는 그 과정에서 자식 프로세스까지 죽었다(`list_jobs` → "No active or recent jobs").

실제로 완주한 패턴은 이것뿐이었다:

```powershell
$env:PYTHONIOENCODING='utf-8'; $env:PYTHONUTF8='1'
Start-Process -FilePath 'python' -ArgumentList 'run_signals.py' `
  -WorkingDirectory 'C:\Users\USER\Desktop\stock_research' `
  -RedirectStandardOutput 'logs\cw_x.log' -RedirectStandardError 'logs\cw_x.err' `
  -WindowStyle Hidden -PassThru
```

그 뒤 로그 파일과 산출물 mtime을 폴링한다. collect·deep·run_signals 셋 다 이 패턴으로 완주했다.
참고로 `start "" /b cmd /c "... > log 2>&1"`은 **동작하지 않았다**(로그 파일이 아예 생기지 않음).

**제안**: A-0의 '긴 명령' 항목을 위 패턴으로 갱신. `wait_job` 폴링은 30초 미만 명령에만.

### B-5. collect 소요시간이 문서와 3~8배 차이

문서 "collect 1~2분", 실측 **8분 33초**(06:28:02 → 06:36:59). `KR (한경 컨센서스) requests 0건 →
셀레늄 피드 폴백 시도` 로그가 있어 셀레늄 폴백이 원인 후보다. 이 차이 때문에 1차 시도에서
"1~2분 지났는데 왜 안 끝나나"로 프로세스가 죽은 줄 오판했다.

**제안**: 문서를 "collect 5~10분(셀레늄 폴백 시)"으로 갱신하고, 폴백 진입 시 예상 지연을 로그에 찍기.

---

## C. 계약·설계 — 리포트 품질

### C-1. holding_review가 엔트리 단위라 리포트 계약과 충돌

실측: `n_open 138`, `n_priority1 40`. `[7.0]`은 "priority 1~2는 전부 개별 행"을 요구하지만
40행은 메일에서 무의미하다. 원인은 같은 종목 반복 추천이 엔트리로 계속 쌓이는 구조다
(`duplicate_clusters`: 셀트리온 21건, 삼성바이오로직스 20건, 한국전력 10건).

나는 **종목·종류 단위 최신 엔트리로 접어** 22행으로 만들었는데, 이건 지시문에 없는 내 임의 규칙이다.
다음 회차에 다른 판단이 내려지면 회고가 비교할 수 없다.

**제안**: `cowork_instructions.md` 1.95에 "표는 **(종목, 종류) 단위로 최신 엔트리 1건**으로 접고
클러스터 건수를 괄호로 병기한다"를 명문화하라.

### C-2. 숏은 손절 판정이 영구 불가 — 스키마에 target/stop이 없다

실측: `contract_missing 69 / 138`. 최근 롱 픽은 계약이 채워져 개선 중인데, **숏은 만기 도달 8건이
전부 `미비: target_pct, stop_pct`**다. 원인은 `[7.5]` 스키마가 숏에 target/stop을 요구하지 않는 것이다.
그러면서 `F7`은 "숏 손절폭을 넓게(예 +8~10%) 표기"하라고 숫자를 요구한다 — 계약이 충돌한다.

나는 오늘 임의로 `target_pct`/`stop_pct`를 숏에 채워 넣었다(셀바스AI -9/+10, SK텔레콤 -8/+9).

**제안**: `[7.5]` shorts 스키마에 `target_pct`·`stop_pct`를 필수로 추가하고 발송 게이트에 넣어라.

### C-3. analyst_reco.json이 19단계에 없어 신선도 게이트가 없다

`1.9 애널리스트 액션`의 '컨센서스 대비' 칸이 이 파일에 의존하는데, 실측은
`generated_at 2026-07-25`(3거래일 지연), **4종목만**. 러너 단계에 없으니 STALE 판정도 안 된다.
오늘은 웹검색으로 대체했다(셀트리온 평균 272,857원, SK이노베이션 136,000원, 삼성바이오 180만원).

**제안**: `analyst_reco.py`를 선택 단계로 19단계에 추가하고, 대상 티커를 직전 세션 predictions의
픽·숏으로 자동 세팅하라.

### C-4. VKOSPI 절대 임계가 현 국면에서 무력하다

`[5.11]`은 "레벨 ≥25 부근 + 5일 +30%↑ 겹치면 공포"라고 한다. 실측 VKOSPI는 **83.43**이고
`pct_rank_60d`는 **27.3**이다. 즉 두 달 내내 25를 크게 초과해 임계가 상시 발동 상태이고,
정작 60일 분포에서는 하위 27%다. 이 국면에서 판별력이 있는 건 절대 임계가 아니라 백분위·추이다.

**제안**: 1차 판정을 `pct_rank_60d`(예 ≥80)로, 절대 레벨은 2차 참고로 재배치하라.

---

## D. 조치가 필요한 것 (사용자 판단)

1. **A-3의 메일 정정** — 발송본에 mirae 지연 사유가 잘못 적혀 있다. 선택지: (a) 다음 회차
   데이터 신뢰도 칸에 정정 한 줄, (b) 정정 메일 별도 발송, (c) 세션 파일만 수정하고 메일은 그대로.
   중복 발송을 피하려 임의로 재발송하지 않았다. (a)를 권한다.
2. **루트 `mirae_data.json`(07-12) 처리** — 삭제 또는 `_retired` 이동. 두면 오판이 재발한다.
3. **B-1·B-2는 한 줄짜리 수정**이고 검증 게이트도 가볍다. 지시하면 백업 후 수정하고
   `tests/run_tests.py` → `py_compile` → 라이브 1회 실행까지 돌리겠다.
