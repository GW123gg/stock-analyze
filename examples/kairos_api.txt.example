# kairos_api.txt
# 카이로스 노트북 캡처 에이전트 토큰
#
# 이 파일은 **양식**입니다. 값이 비어 있습니다.
#   1) 이 파일을 같은 폴더에 kairos_api.txt 라는 이름으로 복사하세요.
#   2) = 오른쪽에 본인 값을 채우세요.
#   3) kairos_api.txt 는 .gitignore 로 막혀 있어 저장소에 올라가지 않습니다.
#
# ── 원본에 있던 설명 ──
# 카이로스 캡처 에이전트 토큰 (노드 agent_config.json 과 동일해야 함)
# 값 출력/커밋 금지. 노드가 정본 - 어긋나면 unauthorized.
#
token= 
