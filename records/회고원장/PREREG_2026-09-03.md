# 사전등록 — 35회차 (2026-09-03 회고)

작성 시각: **2026-09-03 17:50 KST** — `retro_label.py` 실행 중(PID 10412, 17:38:13 기동).
**이 문면은 이번 회차 데이터(신규 드롭)를 한 줄도 보기 전에 고정한다**(지시사항 §0 절대규칙 6번,
C절 09-01 "가지치기 판정은 데이터를 보기 전에 기각 요건까지 수치로 고정하라 — 어긋나면 규약을
바꾸지 말고 결과를 받아들여라").

전거: `RETRO_ISSUES.md` 34회차(2026-09-02) 블록 · B표 · C절.
직전 회차 상태: matured **540**, Δ +4, 규칙 총량 **18/30**(10회차 연속 불변), 신규 발행 0·기각 1(T3).

---

## 0. 계상 전제 (Δ 정의)

- Δ = 이번 드롭 `label_status='matured'` 키 집합 − 34회차(2026-09-02 드롭) matured 키 집합.
  키 = `(rec_id, horizon)`.
- **Δ = 0 이면 어떤 규칙도 재현·반증·승격·기각을 계상하지 않는다**(§3 재현 계상 규칙,
  27회차 판례). 그 경우 확증 목록 대신 **V1~V9 무결성·재현성 검증만** 수행한다.
- Δ 가 **단일 `pred_date` × 단일 `horizon`** 이면 `kospi_ret_h_pct` 가 단일값이므로
  **유효 독립관측 1**로 센다(C절 08-31). 행 수로 세지 않는다.
- A47 라벨창 중복: Δ 안에 `(ticker, kind, horizon, entry_ref, settle_close)` 동일 행이 있으면
  접어서 유효 독립관측을 센다.

---

## 1. 확증검정 가설 (H) — 이 목록에 있는 것만 승격·기각·재현+1 을 할 수 있다

### H1. T1 픽 종목 선별력 — ★이번 회차 최대 판정(가지치기 정면 판정)
34회차가 **§4(0) 가지치기 (a) 후보로 정식 지목**했고 반증이 **누적 5회**다.
33회차 T3 판례(강화 규약이 첫 적용에서 작동)를 그대로 T1 에 적용한다.

판정 규약 — **데이터 확인 전 고정**:
- **(a)** Δ pred 픽 alpha(`alpha_vs_own_idx_pct`) 양수 비율 **>= 50%** AND 누적 방향 유지
  → 반증 불계상 · **관찰 유지**(가지치기 보류).
- **(b)** Δ pred 픽 alpha 양수 비율 **< 50%**(0/N 포함) → **반증 +1(누적 6회)** 이고
  **가지치기 (a) 정식 발동** → B표에서 T1 을 **'선별력 무신호 확정(강등)'** 으로 기록.
  ※T1 은 집행 중 게이트가 아니므로 **규칙 총량은 불변**이다(자체검증 ③ 허수아비 기각 금지).
- **(c)** Δ pred 픽 **0행** → 계상 불가 · 카운터 동결(강등 판정도 하지 않는다).
- 강건성 병기 의무: 행 / 종목 클러스터(C절 08-24 정의 — 종목별 alpha 평균>0) / 고유일 3종,
  + **종목내 대조**(Δ 종목의 기존 행 평균과 비교 — 34회차가 결정적이라고 지목한 검정).

### H2. T1b 바이오 제외 판 — 확증 실패 7회차 연속 여부
- Δ 에 **비바이오 픽이 1행 이상** 유입되면 계상 가능. 0행이면 **표본 불변 → 불계상**.
- 판정: 비바이오 pred 픽 alpha>0 비율의 양측 p **>= 0.05** 유지 → 확증 실패 +1(7회차).
- 2축 교차(종목군 × h) 셀별 N·고유종목 병기 의무(C절 08-28).

### H3. T1c h·소속지수 교락 병기 규약 — 확증 6회 → 7회?
- Δ 행에서 `ret_h_pct` 부호와 `alpha_vs_own_idx_pct` 부호가 **1행 이상 갈리면** 확증 +1(7회).
- 갈리지 않으면 불계상(반증 아님 — 측정 규약이지 예측 규칙이 아니다).
- 전체 matured 부호 괴리 비율을 34회차 **138/540 = 25.6%** 와 나란히 적는다.

### H4. T2 k5 과열 게이트 — 카운터 동결 해제 여부
- Δ 중 `pre_kospi_ret5d > +2%` 행이 **1행 이상**이면 계상 가능, 0행이면 **동결 유지**.
- Δ 내 대조군이 0행이면 'Δ 처리군 평균 vs 누적 대조군 평균'만 쓰고 그 사실을 문장에 명시(C절 08-27).
- 관측 단위는 **일(day)** — 행 단위 N 사용 금지(C절 07-17).

### H5. T4 시간축 전망 — 재현 여부
- 정본: **Spearman(동순위 평균 랭크)**, `expected_peak_days` vs `days_to_peak`, matured 전체,
  순열 **seed=7 · 20,000회**(C절 08-26/08-27). **Pearson 은 재현 근거로 쓰지 않는다.**
- N 이 배증 기준 **102** 미만이면 '확증 보류' 유지. rho 부호가 양수 유지 + Δ 유효쌍 1+ 이면 재현 +1.

### H6. B204 profit_take_flag 단독 손절 금지 — 검정 성립 여부
- Δ 안에 TRUE·FALSE 가 **둘 다** 있어야 Δ-단독 검정이 성립한다. 한쪽이 0행이면
  **'검정 불성립'으로 기록**(기각도 재현도 아님 — C절 08-04, 34회차 판례).
- 누적 방향(TRUE 가 오히려 우수)이 유지되는지 병기.

### H7. 숏 종목 선별력 — Δ 숏 유입 여부
- Δ 숏 0행이면 **8회차 연속 불계상**. 1행+ 이면 `alpha_vs_own_idx_pct` 기반 **초과하락 비율**로만
  판정하고 **방향적중률 단독 인용 금지**(C절 08-04 동어반복).
- 무정보 기저는 always-short 이 아니라 **초과하락 코인플립 50%**.

### H8. §3.9 서브샘플 분열(전체 vs prediction vs archive)
- 34회차: 전체 52.0%(p=0.4420) / pred 58.0%(p=0.0077) / archive 40.1%(p=0.0206),
  단 **종목 클러스터로 올리면 pred 58.5%(p=0.2717)로 유의성 소멸**.
- 이번 회차도 **행·종목 클러스터 두 판을 병기**하고, 클러스터에서 소멸하면
  '유사반복 경보'로만 기록한다(규칙화 금지).

### H9. A64 검증 — market_calls 축의 §3.9 구조적 불성립
- 34회차 신규 등재. 이번 드롭 `market_calls_*.json` 에 **archive 시대(`_src_kind != 'prediction'`)
  콜이 1건이라도 있는지** 실측한다.
- 0건이면 **A64 미해결 유지** + 리포트에 "market_call 축 §3.9 는 대조군 부재로 불성립"을
  명시(두 수치가 같다는 이유로 '준수'라고 적지 않는다 = 거짓 준수 방지).

---

## 2. 무결성·재현성 검증 (V) — Δ 와 무관하게 전부 수행

- **V1 무음 재라벨**: 34회차 드롭과 공통 matured 행을 **17열 확장 대조**
  (`ret_h_pct`·`alpha_h_pct`·`alpha_vs_own_idx_pct`·`kospi_ret_h_pct`·`days_to_peak`·
  `days_to_trough`·`post_peak_drawdown_pct`·`max_drawdown_pct`·`profit_take_flag`·
  `ret_if_stop8_pct`·`ret_if_stop8_tp12_pct`·`ret_if_stop8_tp12_cap_pct`·
  `short_cover_d4_ret_pct`·`short_stop9_day`·`short_stop9_ret_pct`·`dist_disc_count`·
  `peak_day_vol_ratio`). **결측채움(nan→값)과 값변경을 갈라 센다**(C절 09-01).
- **V2 사후 라벨 커버리지 단조성**: `flow_foreign_eok`·`dist_disc_count` 비결측 수가
  34회차(540 기준) 대비 감소하지 않는가(A46 회귀 감시).
- **V3 원장 수치 재현**: 34회차 헤더·B표 기재 수치 **10종 이상**을 이번 드롭으로 재계산해
  일치 여부를 건별로 적는다(A52 — 재현 불가 수치 위에 서사를 쌓지 않기 위함).
- **V4 A41 3조건**: `pre_regime_kind` 소수 클래스 15행+ / `pre_allow_market_up=false` 5일+ /
  `pre_caution_score>=60` 5일+ — 충족 여부. 미충족이면 F1/F8 '검증 대기' 유지.
- **V5 라벨 지연**: `partial_asof_date` 최대값 vs 직전 영업일(2026-09-02). 차이를 거래일 수로
  리포트 첫머리에 적는다. 17:30 슬롯 연속 실증 회차 수 갱신.
- **V6 무결성 대조**: `latest.json` date=2026-09-03 · roles 7파일 **sha256·bytes·rowcount 3종
  재계산** 전건 대조.
- **V7 스냅샷 편향**: `partial_asof_index` 로 asof 일의 코스피 1d·5d 를 읽어 미만기 수치와
  **같은 문장에 병기**(A35). 34회차 asof 09-01 = +0.23%/+1.38%(경미·낙관)와 비교.
- **V8 A47 라벨창 중복**: `(ticker, kind, horizon, entry_ref, settle_close)` 중복 그룹 수·
  **초과행 수**(그룹 첫 행 제외 기준, C절 08-24)를 34회차 대비 갱신.
- **V9 A62 superseded 비율**: `night_scorecard` 원장 대비 superseded 비율. 34회차 대비 증감.
  **market_calls 와 합산 금지 · 비교 전용 · 표본 30건 전 규칙화 금지.**

---

## 3. 이번 회차 운영 관측(사전 기록 — 판정과 무관)

- **A50/A60 재발 실측 1건**: 첫 `run_command("python retro_label.py")` 가 전송 타임아웃
  → `list_jobs` 가 **"No active or recent jobs"**(MCP 서버 재기동, C절 08-28/08-31 패턴 3회차째)
  → 재기동으로 **2 인스턴스**(PID 13336 @17:36:57 · PID 10412 @17:38:13).
  `Get-CimInstance Win32_Process -Filter "Name LIKE 'python%'"`(A58 처방)로 17:47 에 검출,
  **PID 13336 을 taskkill /F 로 정리**하고 로그가 붙은 10412 만 남겼다. 오염 여부는 V1 로 판정.
- 실기동 시각 **17:36 KST** = SKILL 설계 시각 17:30 과 일치(아침 슬롯 침범 없음).
- 0번 가드 3종 전부 통과: (가) latest.json date=2026-09-02 ≠ 오늘 (나) 17:36 ∉ 06:00~08:00
  (다) 선행 회고 프로세스 0 · 윈도 예정작업 `*retro*` 0건.

---

## 4. 다중비교 사전 선언

- 수행 검정 총수 **N** 을 리포트에 의무 기재하고, 인용하는 모든 p 에 **Bonferroni×N** 과
  **BH-FDR(q=0.05)** 생존 여부를 병기한다.
- p 는 **양측을 정본**으로, 단측은 괄호 병기(C절 08-24).
- 순열검정은 **seed=7 · 20,000회** 고정(C절 08-26).
