# 회고리포트 2026-08-21 (26회차)

## 0. 원장 대조 (이전 지적 처리 현황 한 줄)

25회차 등재/이월 4건 중 **A46 재차단 확인(악화)** — 이번 `retro_label` 실행 로그를 확보했더니 pykrx 3종
(`get_market_trading_value_and_volume_on_ticker_by_date` · `..._by_investor` · `get_shorting_balance_by_date`)이
**60회 연속 비-JSON 응답**(KDM 차단 안내 HTML)을 반환했다 = A46 재차단 실측. **A45 해결 확인**(08-20 세션 생성됨 —
단 06:20 정규가 아니라 **09:17 장중 대체 발행**이고 예측 0건, 사유는 세션 `_note`에 명시: *"2026-08-20 정규 06:20
미발행(회고 작업이 슬롯 점유)"* → **회고 시간 초과가 아침 발행을 삼킨 사고가 실제로 재발했다**). **A44 3회차 연속
독립 검증 불가**(scorecard 생성시각이 24·25·26회차 모두 **2026-08-19 21:37**로 동일 — 아침 실행이 없으면 갱신되지
않는 구조). 무결성: latest.json 대조 **7파일 전건 일치**(retro_dataset_2026-08-21.json sha256 `8468894713e3ee2e` ·
2,604,720B · 544행, CSV 492,134B 일치). **무결성=일치.**

## 1. 요약

- **표본**: matured **519행**(전체 544) — **신규 만기 Δ +5**(514→519, **전량 prediction 픽**, 숏 0, **소실 0건**).
  maturing 25 · no_label 0. pred_date 2026-05-30~**08-19**(08-20은 예측 0건이라 신규 행 없음).
- **라벨 지연 0 거래일**: `partial_asof` 최대 **2026-08-20** = 직전 영업일(오늘 08-21 금).
  ★**A35 스냅샷 편향 — 이번엔 방향이 반대다**: meta `partial_asof_index`에 08-20 KOSPI **1d +5.89% / 5d +4.16%**.
  25회차의 08-19(-5.80%)가 비관 편향이었다면 이번 08-20은 **급반등일 종가 스냅샷 = 낙관 편향**이다.
  미만기 25행 평균 `ret_partial` **+3.15%**를 성과로 읽지 마라(이틀 만에 편향이 뒤집힌 실례).
- **국면 pre_* 커버리지(A41 축)**: matured **116/519 = 22.4%**. 분산: 공포 59 · 눌림목 38 · **과열 16** · 중립 3
  → 소수 클래스 15행+ 는 2개 클래스 충족. 그러나 `pre_allow_market_up=false` **6행/2일** · `pre_caution_score>=60`
  **6행/2일**(기준 각 5일+) → **A41 3중 조건 1/3 충족 — F1/F8 '검증 대기' 유지**(3회차 연속).
  `pre_vkospi_label` 71행 **전부 '공포'(분산 0)**.
- **사전등록 가설(분석 전 고정) 8건**: T1 픽 종목 선별력 재검(원장 B표 지정) · T2 k5 게이트 · T3 분배합류 부호판
  (25회차가 '6회차 무재현 시 기각 절차' 예고) · T4 시간축 전망(표본 배증) · T5 A44 `scored_from` 독립 검증
  · T6 A41 재개조건 · T7 개별과열 Δ 재확증(25회차 지정) · P1 가지치기: 재추천 방향.
  **수행 검정 총수 N=25**(p 산출 25건 — 픽 선별력 이항 18 · 숏 초과하락 이항 4 · 블록순열 3).
  **Bonferroni×25(임계 0.00200) 생존 3** — pred 픽 행 0.0007 · 최대일 제거 0.0012 · 라벨창 dedup 행 0.0014.
  **BH-FDR(q=0.05) 생존 5** — 위 3 + 고유일 0.0023 + dedup 고유일 0.0057.
  **전부 T1 계열이며 서로 중첩된 부분표본이라 독립 검정이 아니다**(그만큼 Bonferroni 는 과보정, 실효 검정력은
  이 사이 어딘가다). 종목 클러스터(0.1058)·거래일만 판(0.0263)은 **어느 보정에서도 생존하지 못한다**.
  **신규 판단 규칙 발행 0건.**
- **조기경보(§3.12)**: `k5>+2%` 진입인 채로 살아있는 픽 **7건**(07-28 2 · 08-05 1 · **08-19 4**). 08-19 4건은
  k5 **+9.05%**(관측 최대) 진입분으로 27회차 이후 k5 재검의 첫 극단 표본이다. 08-20 급반등(+5.89%) 덕에
  현재 partial 은 -5.07~+3.36%로 흩어져 있다(스냅샷 편향 주의).
- **회차 성격**: **Δ 극소(+5) 2회차 연속** — 규칙 재현 계상은 거의 불가. 이번 회차의 가치는
  ① **주말 발행 예측의 라벨창 중복(유사반복) 최초 정량화 — 신규 A47** ② T3 분배합류 6회차 무재현 확정
  ③ 08-20 전야 콜 vs 전날 아침 next_day 콜의 직접 대조(§15-b) ④ A46 재차단 실측 확인에 있다.

### 핵심 수치 병기 (§3.9 전체/prediction · §3.10 알파, matured 기준)

| 구분 | N(행/종목/일) | ret_h | alpha_own | hit |
|---|---|---|---|---|
| 전체 픽 | 412/80/58 | -2.33% | +2.75% | 44.2% |
| pred 픽 | 265/52/46 | -2.82% | +4.47% | 44.5% |
| 전체 숏 | 107/41/49 | -3.80% | -1.61% | 62.6% |
| pred 숏 | 69/29/39 | -4.08% | +0.02% | 66.7% |

(`alpha_own` = `alpha_vs_own_idx_pct` = 소속시장 지수 차감. `hit` 은 수익률 부호의 재라벨이므로 같은 표본
기저율과 비교하지 않는다 — C표 08-04 규약.)

### 핵심 발견 1~3

**(1) 신규 A47 — 주말 발행 예측이 다음 거래일 발행분과 라벨창을 통째로 공유한다(유사반복)**

matured 519행에서 `(ticker, kind, horizon, entry_ref, settle_close)` 가 **완전히 같은데 pred_date 만 다른**
중복 그룹이 **29개**, 초과 계상 행이 **31행(전체의 6.0%)** 이다. 이 중 **28행이 토·일 발행분**이다.
원인은 단순하다 — 토요일 발행분의 `entry_ref` 는 금요일 종가이고, 월요일 발행분도 금요일 종가다.
horizon 이 같으면 정산일까지 같으므로 **두(때로 세) 행이 하나의 미래 창을 나눠 갖는다**.

| 종목 | 구분 | h | 같은 창을 공유한 pred_date | ret_h |
|---|---|---|---|---|
| 기아(000270) | pick | 5 | 07-17(금)·07-18(토)·07-19(일) | -12.89% |
| HMM(011200) | pick | 5 | 07-17(금)·07-18(토)·07-20(월) | +2.99% |
| SK(034730) | short | 5 | 06-28(일)·06-29(월) | -22.26% |
| 삼성바이오로직스(207940) | pick | 5 | 07-17(금)·07-19(일) | +10.67% |
| 카카오(035720) | pick | 5 | 08-01(토)·08-03(월) | +5.75% |
| 클래시스(214150) | pick | 5 | 08-01(토)·08-03(월) | +12.52% |

주말 발행일은 **10일**(06-27·06-28·07-11·07-12·07-18·07-19·07-25·08-01·08-02·08-08)이고
prediction 픽 58건·숏 17건이 여기서 나왔다. **08-08(토) 4건을 끝으로 이후 주말 발행은 0건** — 즉
발행 쪽 결함은 이미 닫혔고(주말 가드 작동), 남은 것은 **회고 표본에 박제된 과거분**이다.
그런데 **회고 쪽에는 접는 규약이 없다**: `scorecard` 는 이미 접는다(*"주말·휴장 중복 픽은 같은 정산 창
1건으로 접음 — 2026-08-01 집계분부터, #A14 확장"*)는데 `retro_dataset` 은 접지 않는다. C표에도
시장콜(07-19 규약)만 있고 픽/숏 판은 없다 — **같은 병에 한쪽 약만 발라 둔 상태**다.

**영향 측정(T1 픽 선별력에 그대로 적용)**:

| 판 | 행 | 종목 클러스터 | 고유일 | alpha_own |
|---|---|---|---|---|
| 원본 | 159/265 = **60.0%** (p=0.0007) | 31/52 = 59.6% (p=0.1058) | 33/46 = 71.7% (p=0.0023) | +4.47% |
| 라벨창 dedup | 149/250 = **59.6%** (p=0.0014) | 31/52 = 59.6% (p=0.1058) | 32/46 = 69.6% (p=0.0057) | +4.43% |
| 거래일 발행만 | 119/209 = **56.9%** (p=0.0263) | 29/50 = 58.0% (p=0.1611) | 24/36 = 66.7% (p=0.0326) | +4.08% |

→ **결론의 방향은 세 판 모두 살아남지만 크기는 깎인다**. 특히 '거래일 발행만' 판에서는 행 단위 p 가
0.0007 → **0.0263** 으로 올라 **Bonferroni×25·BH-FDR 어느 쪽도 통과하지 못한다**(24회차의 "Bonferroni×20 생존"
서술은 원본 판 기준이었음을 명시해 둔다). 숏 축은 어느 판에서도 결론 불변(초과하락 42.0% → 40.9% → 40.4%,
전부 코인플립 50% 미만).

반례/한계: 주말 발행 픽 자체의 성과가 나쁜 것은 **아니다**(주말 pred 픽 56행 alpha +5.92% vs
거래일 209행 +4.08%) — 문제는 성과가 아니라 **독립 관측 수를 부풀린다**는 것뿐이다.
또한 중복 31행 중 3행은 금·월 조합이 아니라 금요일+토요일처럼 **거래일 발행분끼리도 겹친다**
(기아 07-17·HMM 07-17) — '주말만 접으면 끝'이 아니므로 규약은 **요일이 아니라 `(entry_ref, settle_close)`
동일 여부**로 써야 한다.

**(2) T3 진입 전 분배합류 게이트 — Δ TRUE 0건 6회차 연속(21~26회차) → 가지치기 기준 충족**

Δ 픽 5건 전부 조건 FALSE 다. 더 정확히는 **5건 전부 `pre_indiv_5d_eok` 가 음수(개인 순매도)** 라
'개인만 매수' 다리가 아예 성립하지 않는다(-443 · -388 · -258 · -264 · -12억). 25회차의 관찰
("8월 반등장에서 발동 모집단이 소멸 중")이 그대로 이어졌다. 누적 수치는 방향 유지(TRUE 40행/18종목/23일
alpha_own **-2.55%** vs FALSE 372행/72종목/58일 **+3.32%**)이나 **일내 대조 25블록 평균차 -2.26%p ·
음수 14/25 · 블록순열 p=0.4111** 로 유의성은 없다. §4(0) 가지치기 규칙 (a) **'6회차+ 신규 만기 재현 없음'**
을 충족하므로 **기각 절차에 들어간다** — 다만 이 항목은 23회차에 이미 '게이트 아님·경고도 보류'로
비집행 상태였으므로 **규칙 총량 18/30 은 불변**이다(25회차 T1 기각과 같은 처리).
라이브 리허설: 최근 10추천일(08-03~08-19) 발동 **5행/46행 = 10.9%**(23회차 0% 대비 회복 — 발동 자체는
가능하나 만기 표본이 안 쌓인다). 반례: TRUE 40행 중 alpha 양수가 14행(최대 SK하이닉스 06-13 +20.93% —
15회차부터 반복 인용되는 같은 반례).

**(3) §15-b 전야 콜 vs 아침 next_day 콜 — 08-20 급반등에서 정면으로 갈렸다(N=1, 관찰)**

08-20 코스피 **+5.89%**(meta 실측)에 대해 두 콜이 존재한다.
- **08-19 아침(06:20) 콜의 `next_day`**: `dir=down`, `prob_down=0.70`, `expected_pct=-3.5%`,
  근거 = *야간선물 9월물 -4.29% 실측 + SOX -4.98% + SK하이닉스 ADR -9.2% + 미 30년물 5.33%* → **틀림**.
- **08-19 전야(23:45) 콜(for_date=08-20)**: `kospi.dir=up` → **맞음**(21:05 초판도 up, 23:45 최종판도 up).

즉 **간밤 미국장을 다 보고 낸 23시 콜이, 같은 날 아침에 낸 익일 전망을 이겼다**. §15-b 규약대로
**market_calls 와 합산하지 않고 비교로만** 적는다. **N=1 — 규칙화 금지**(표본 30건 전 금지 규약).
다만 이 사례는 night_track 의 존재 이유(아침 정보 vs 밤 정보)를 정확히 겨냥하므로 추적 축으로 기록한다.
★단 `night_scorecard` 는 **갱신 2026-08-19 06:31 · 채점 N=3 로 25회차와 동일**이라 이 건은 아직 채점 전이다
(원장 jsonl 에는 for_date 08-20 2건·08-21 2건이 들어와 있는데 scorecard 가 8건 중 4건만 안다 = 2회차 스테일).

## 2. 차익실현 패턴 (§3.1·§3.6 — Δ 한정 + 누적)

Δ 5건 중 `profit_take_flag=True` **2건**: 셀트리온 h20(07-22 진입, peak **+22.67%** D+14 → settle +13.11%,
alpha_own +11.56) · 삼성SDI h5(08-12 진입, peak +12.30% D+2 → settle +8.71%, alpha_own +0.72).
둘 다 **고점을 만들고 일부 반납한 승자**다 — 23회차 이후 반복 확인된 "profit_take=True 군이 오히려 우수"가
Δ에서도 유지된다. 누적: TRUE 71행/31종목/31일 alpha **+5.95%** · `dist_disc_count` 6.61 · `peak_day_vol_ratio`
**1.73** vs FALSE 341행/76종목/58일 alpha +2.08% · 5.64 · **1.19**. 분배 공시·거래량 클라이맥스 동반은
재확인되나 **손절·진입회피 트리거 금지 유지**(보유관리 신호 전용).

즉시고점(days_to_peak<=2)은 Δ 2건(신성이엔지 D+1 peak +0.60% → settle -2.08 / 한화에어로 D+1 peak +7.92%
→ settle +6.28). 신성이엔지는 '못 오르고 꺾인' 전형, 한화에어로는 '올랐다 조금 반납' — 같은 라벨 아래
경로가 다르다는 23회차 부수 발견(두 경로를 묶어 서술하지 말 것)과 정합.

## 3. 태그·타이밍·확신도 (§3.2·§3.3)

- **확신도 동결 축**: `conviction>0.65` 만기 누적 **0건**(재개 기준 10건) → **동결 유지**. F5 조건부 상한
  정책의 산물이며 데이터의 산물이 아니다.
- (관찰 메모 — 사전등록하지 않았으므로 확증 판정 아님) `>=0.50` 이분: pred 픽 conv>=0.50 **138행 alpha +6.01%**
  vs <0.50 **127행 +2.79%**(격차 +3.22%p, 25회차 +3.23%p와 사실상 동일 = 같은 표본 재계산).
  **Δ 에서는 부호가 가설 방향으로 돌아왔다**(>=0.50 3건 -0.07 vs <0.50 2건 -4.67 — 25회차는 반대였다).
  Δ=5 소표본이라 강건성 4종+순열 배터리는 돌리지 않았다. **③종목내 대조 미통과 상태이므로 승격 논의 금지** 유지.
- 태그: scorecard 최근창 [단기스윙] 54건 alpha +2.1% · [장투가능] 8건 +4.0% — **태그 단독 비중 조정 금지**
  (8회 기각 유지). 회고 완전표본과 모집단이 다르므로 처방 근거로 쓰지 않는다.

## 4. 시장국면 의존 (§3.5·§3.14)

- **T2 k5 강세추격 게이트**: Δ 픽 5건의 k5 는 -0.87(07-22)·-0.21×4(08-12) → **Δ 신규 과열일 0건, 재현 계상
  불가(2회차 연속)**. 누적 방향 유지: 전체 과열 101행/51종목/**12일** ret -4.95%/alpha +2.18% vs 그 외 311행
  -1.48%/+2.93%; pred 과열 40행/28종목/**7일** ret -6.31%/alpha +3.27% vs 그 외 225행 -2.20%/+4.68%.
  **일 단위**(시장수준 피처 규약)로는 과열 7일 alpha +1.75% vs 그 외 39일 +3.34% — 격차 작고 검정력 없음.
  상태 불변: **'경고·순노출 축소', 기계적 차단 아님**. 리허설 발동 19행/46행 = 41.3%(08-05·06·07·19).
- **T3 분배합류**: 핵심 발견 (2) 참조 — 6회차 무재현, 기각 절차.
- **T6/A41**: 1/3 충족(요약 참조) → F1/F8 **검증 대기 유지**. `pre_caution_score` 고유값이 이번에 처음
  60 을 넘는 값(62.5)을 포함했으나 **해당 행이 6행·2일**뿐이라 5일 기준 미달이다.

## 5. 숏 익절 경로 (§3.11)

**Δ 숏 0건 — 3회차 연속**(24회차 +13 이후 신규 만기 숏 없음). 따라서 숏 전 축의 판정은 불변이며
재현·반증 어느 쪽도 계상하지 않는다(§3 계상 규칙 준수). 참고 재계산(신규 정보 아님):
pred 숏 초과하락 **29/69 = 42.0%**(코인플립 50% 미만, p=0.9260) · `days_to_trough` 중앙 **3.0** ·
저점깊이 -9.29% vs 만기 -4.08% = **5.21%p 누수** 재확인. A42 대체 라벨 두 종도 불변
(`short_cover_d4_ret_pct` pred 67건 평균 **-2.43%** vs 만기 -4.08% → **D+4 커버는 1.65%p 악화**,
`short_stop9_ret_pct` 32건 -12.96%). **B(11) 기각 상태 유지.**

## 6. 시간축 전망(T4) · 시장콜 · A44 · 전야 콜

- **T4**: 채점 가능 N **57행/34종목/16일**(24회차 51 → 25회차 53 → 57. 배증 기준 102 미달) →
  **확증 보류 유지**. 누적 Spearman(path_view 순서 ↔ days_to_peak) **+0.494**(같은 표본 재계산 — 재현 아님).
  즉시상승 콜 `days_to_peak<=2` **7/8** vs 무정보 기저 **26/57 = 45.6%**. **일내 혼합블록은 2개뿐 ·
  평균 -0.75일 · p=1.0000 → 여전히 미통과**(원시 상관이 일 효과와 분리되지 않는다).
  **Δ 4건은 전부 예상보다 이른 고점**(한화에어로 실제 1 vs 예상 4 / HMM 3 vs 4 / 삼성SDI 2 vs 5 /
  신성이엔지 1 vs 5). 25회차 Δ 3건도 같은 방향이었으므로 **7건 연속 조기 고점** — 관찰로만 기록한다.
  방어실패(|실낙폭|>|예상되돌림|) **26/57 = 45.6%**(25회차 45.3%와 정합) → 되돌림 예측 보수화 지시 유지.
  ★**정의 고정 필요(§9 정정 참조)**: 고점일 예측오차를 `실제-예상` 으로 계산하면 전체 평균 **-0.77일**
  (실제>예상 14 · 동일 17 · 실제<예상 26)인데, 24회차는 같은 축을 "평균 +0.59일·낙관 41%"로 적었다.
  Δ 제외 53행(=25회차 표본)으로 재계산해도 **-0.62일 · 실제>예상 14/53(26.4%)** 라 24회차 수치는 이 정의로
  재현되지 않는다 → **정의 불일치**로 판단하고, 앞으로 **오차=실제-예상 · 낙관=실제>예상** 으로 고정한다.
- **시장콜**: `market_calls` 47콜, **08-12 이후 신규는 08-19 1건뿐**(08-20 세션은 예측·시장콜 모두 0건 발행)
  → 신규 채점 0. 08-19 콜의 발송 게이트 3종(합=1.00 · argmax=dir · 최대확률<=0.75) **위반 0건 — A43 정착 유지**.
- **T5 A44 `scored_from` 독립 검증 — 3회차 연속 불가**: 이번 드롭 scorecard 도 **생성시각 2026-08-19 21:37 ·
  "이번 실행 추가 0건"** 으로 24·25회차와 같은 파일이다(next_day 11/14=79% vs main_call 2/4=50%도 동일 수치).
  구조적 원인이 이번에 드러났다 — **scorecard 는 아침 실행에서 갱신되는데 회고 push 는 그보다 먼저 돌고,
  아침이 결번이면 아예 갱신되지 않는다**. 08-20 아침이 09:17 대체 발행이었으므로 이번에도 갱신이 없었다.
  → A44 를 '해결됨'으로 닫아 두면 안 되는 상태이며, **A48 로 별도 등재**(아래 원장 참조).
- **전야 콜(15-b)**: 핵심 발견 (3) 참조. night_calls 원장은 8건(for_date 08-20 2 · 08-21 2 신규 유입)인데
  `night_scorecard` 는 4건까지만 알고 채점 N=3(KOSPI 방향 1/3, Brier 0.5928 / KOSDAQ 1/3, 0.8402) —
  **market_calls 와 합산하지 않았다**(비교 전용). 표본 30건 전 규칙화 금지 유지.

## 7. T1 픽 종목 선별력 — 원장 지정 26회차 재검 (사전등록 확증 판정)

- **누적(원본 판)**: pred 픽 alpha_own>0 **159/265 = 60.0%**(p=0.0007) · 종목 클러스터 **31/52 = 59.6%
  (p=0.1058)** · 고유일 33/46 = 71.7%(p=0.0023) · 최대일(06-25, 12행) 제거 151/253 = 59.7%(p=0.0012).
  전체 표본(archive 포함)은 218/412 = 52.9%(p=0.1286)로 여전히 미달 — 부진 귀속처가 archive 라는 판정과 정합.
- **★종목 클러스터 축이 이번에 유의성을 잃었다**: 24회차 63.3%(p=0.0427) → 26회차 **59.6%(p=0.1058)**.
  종목 수는 49→52로 늘었는데 양수 종목은 31로 정체했다. 승격 요건의 강건성 축 하나가 내려앉은 것이다.
- **★Δ 부호가 가설과 반대**: Δ pred 픽 5건 중 alpha_own>0 은 **2/5 = 40%**(평균 alpha **-1.91%**).
  개별: 셀트리온 +11.56 · 삼성SDI +0.72 · 한화에어로 -1.71 · HMM **-10.07** · 신성이엔지 **-10.07**.
  §3 **Δ-단독 검정 규칙**에 따라 **이 회차는 재현 불계상 + 반증 1회 계상**한다.
- **판정**: 재현 **1회 유지(24회차분) + 반증 1회**. 승격 요건(재현 3회+) 미달이 확정적이므로 **승격 금지 ·
  관찰(강→중) 하향**. 처방은 F8-b 그대로 불변(픽 억제 금지 · 순노출 축소) — 방향 자체는 세 판 모두 살아 있다.
- 반례(누적 최악 5): 클래시스 08-07 **-25.53** · 한화오션 06-18 -22.59 · 삼성전기 07-07 -21.81 ·
  이오테크닉스 07-01 -19.96 · ISC 06-28 -19.63.

## 8. T7 개별과열 · P1 재추천 방향

- **T7 개별종목 과열 단독 차단 금지(비지지)** — 25회차가 지정한 26회차 재확증:
  누적 IMM(days_to_peak<=2) 180행/62종목/49일 vs REST 232행/54종목/53일 —
  `pre_rsi14` **47.91 vs 47.80**(동일) · `pre_dist_52w_high_pct` -28.05 vs -28.19(동일) ·
  `pre_up_streak` 1.15 vs 0.99 · `pre_overheat` **9.23 vs 7.84**(격차 1.39 — 23회차 8.64/8.18 대비 확대) ·
  `peak_day_vol_ratio` **1.14 < 1.40**(즉시고점에 클라이맥스 없음, 재확인).
  결과 격차는 여전히 극단(IMM alpha -3.54%·hit 20.0% vs REST +7.63%·62.9%).
  ★유일하게 벌어진 `pre_overheat` 를 **일내 대조**로 검정: **44블록 평균 +2.59 · 양수 23/44 · 순열 p=0.1297
  → 미통과**. Δ(IMM 3 vs REST 2)에서도 IMM 이 더 과열(overheat 9.60 vs 6.75, rsi 59.73 vs 54.50)로
  24회차 반증 방향과 정황이 2회차 연속 일치하나 **N=3/2 로 검정 불성립**.
  → **카운터 불변(비지지 11회 + 반증 1회)**, 규칙 부활 **금지 유지**. 단일 피처의 2.6점 격차가 일 효과와
  분리되지 않는 한 진입 차단 근거가 될 수 없다.
- **P1 재추천 방향(가지치기 후보)**: 재추천 **331건** corr(직전 entry_ref 대비 변화율, alpha_own) = **-0.081**
  (23회차 -0.067 → 24 -0.079 → 25 -0.080 → 26 -0.081, 사실상 불변). 추격 133행 alpha +3.21% vs 눌림 198행
  +4.02%(격차 -0.81%p). **★그러나 Δ 4건의 부호는 가설 방향이다**: 추격 3건(한화에어로 +9.0% → -1.71 /
  HMM +0.9% → -10.07 / 삼성SDI +5.9% → +0.72, 평균 **-3.69**) vs 눌림 1건(셀트리온 -0.6% → **+11.56**).
  3 대 1 · 2일뿐이라 검정 불성립이므로 **재현 계상은 하지 않는다**. 다만 **Δ 부호가 가설 방향인 회차에
  기각을 발의하는 것은 부당**하므로, 25회차가 예고한 **'26회차 무재현 시 기각 발의'는 보류**하고
  관찰을 1회차 더 유지한다(27회차 Δ 에서도 신호가 없으면 그때 기각 발의).

## 9. 다음에 고칠 점

1. **(호스트·최우선) 회고 실행이 아침 발행 슬롯을 다시 삼켰다** — 08-20 세션 `_note` 가 사유를 명시하고 있다.
   회고 쪽 시간 예산 규율만으로는 재발을 못 막았으므로 **슬롯 분리(회고 종료 데드라인 강제 또는 아침 작업
   우선순위 선점)** 가 필요하다. 이번 회차는 03:15 시작 → 04:03 데이터셋 완료로 여유가 있었다.
2. **(호스트) A47 — `retro_dataset` 에 `label_window_id`(= `ticker|kind|horizon|entry_ref|settle_close` 해시)와
   `label_window_dup_n` 를 실어 달라.** scorecard 는 이미 접고 있으니 정의는 그대로 재사용하면 된다.
   회고가 매 회차 손으로 접으면 회차 간 수치가 또 갈린다.
3. **(호스트) A48 — scorecard 갱신 시점과 회고 push 순서.** 아침이 결번이면 scorecard 가 갱신되지 않아
   A44 축이 3회차째 같은 표본을 본다. 회고 push 직전에 `accuracy_tracker` 를 1회 돌리거나,
   scorecard 헤더에 '마지막 갱신 이후 경과 거래일'을 찍어 스테일을 가시화하라.
4. **(호스트) A46 재차단 확정** — 이번엔 로그를 확보해 60회 연속 비-JSON 응답을 실측했다.
   `openapi.krx.co.kr` 등록(사용자 액션)이 근본 해법이라는 판단은 그대로다.
5. **(회고) 27회차 예정**: A47 규약 적용 후 T1 재판정 · T3 기각 확정 · 08-19 과열 4픽 만기 유입분으로 k5 재검 ·
   P1 최종 판정 · T4 표본 배증 여부.
6. **(회고) 08-20 partial 스냅샷은 +5.89% 급반등일**이다(A35). 27회차에서 미만기 행을 읽을 때 전제로 삼아라.

## 10. 자체검증 (§4-2.5 6점검)

① **열 이름 = 실제 계산값**: 모든 알파는 `alpha_vs_own_idx_pct`(소속시장 차감)이며 `ret_h_pct` 와 구분해
표기했다. 클러스터 수치는 종목별 평균의 평균으로 계산했고 단순 행 평균과 섞지 않았다. — 확인.
② **재현 카운터가 §3 계상 규칙을 지켰는가**: 이번 회차 재현 계상 **0건**. T1 은 Δ 부호 반대로 **반증 1회**,
T2·T3 은 해당 부분군 신규 만기 0 → 계상 불가/보류, T4 는 배증 미달로 보류, T7 은 N=3/2 검정 불성립으로
카운터 불변, P1 은 3대1 검정 불성립으로 불계상. 숏 축은 Δ 0 → 전건 불변. — 확인.
③ **기각 대상이 실제로 살아 있는가**: T3 분배합류는 원장 B표에 '계상 보류 5회차 연속 · 26회차 무재현 시
기각 절차'로 살아 있었고(25회차 헤더 원문 인용 확인), 25회차 PART A 별첨 B 에도 예고돼 있었다.
허수아비 아님. — 확인. 반대로 **P1 은 기각 예고가 있었으나 Δ 부호가 가설 방향이라 기각하지 않았다**.
④ **부등호·대소 방향이 표의 숫자와 일치**: 숏 행에서 양수 `ret_h` = 숏 손실(C표 07-25 규약) 준수 —
pred 숏 ret_h -4.08% 는 수익이다. A47 표의 p 값은 판이 강해질수록(dedup→거래일만) **커지는** 방향으로
적혀 있고 실제 수치와 일치한다. — 확인.
⑤ **N·'전부 X' 류 단정을 재집계**: 행/고유종목/고유일 3종을 본문 전반에 병기했다. Δ 5건 전량 prediction·
소실 0건은 (rec_id, horizon) 키 차집합으로 재확인했다. "Δ 5건 전부 개인 순매도"는 5건의
`pre_indiv_5d_eok` 값을 개별 출력해 확인했다. "주말 발행 08-08 이후 0건"은 prediction 행의 pred_date
요일을 전수 판정해 확인했다. — 확인.
⑥ **적중률 옆에 무정보 기저율**: 픽 선별력은 코인플립 50%, 숏 초과하락도 코인플립 50%(자기표본 기저율
비교는 C표 08-04 규약대로 금지), 즉시상승 콜은 무정보 기저 45.6% 를 병기했다. — 확인.

**정정 3건**
1. **초안의 '데이터 결함 의심' 철회** — Δ 의 HMM 과 신성이엔지가 `ret_h -2.08` · `alpha_own -10.07` 로
   소수점까지 같아 산출 버그를 의심했으나, 원본 대조 결과 **우연의 일치**였다(HMM 21,650→21,200 = -2.078%,
   신성이엔지 16,800→16,450 = -2.083%, 둘 다 KOSPI·같은 창 kospi_ret_h +7.99%). 결함 보고하지 않는다.
   추가로 `fwd_days_avail >= horizon` 인데 `ret_h` 가 비어 있는 행이 **0건**임을 전수 확인했다.
2. **수행 검정 총수 과소 보고 정정 — N=11 → N=25.** 초안은 본문에 인용한 p 값만 셌으나, §0 규약은
   **수행한 검정 전부**를 세도록 한다. 실제로는 픽 선별력 이항 18(pred·전체·Δ 각 4 + dedup 3 + 거래일만 3)
   · 숏 초과하락 이항 4 · 블록순열 3 = **25건**을 돌렸다. 재계산 결과 **Bonferroni 생존 4→3**
   (고유일 p=0.0023 이 임계 0.00200 을 근소하게 초과해 탈락), **BH-FDR 생존 4→5**(dedup 고유일 0.0057 추가)
   로 바뀌어 본문을 교체했다. **판정에는 영향 없다**(T1 은 어차피 Δ 반증으로 승격 금지).
3. **시간축 고점일 예측오차의 부호 정의를 24회차와 통일할 수 없음을 확인하고 본 회차 정의로 고정**
   (§6 참조). 24회차의 "평균 +0.59일·낙관 21/51=41%"를 그대로 이어 쓰지 않고,
   `실제-예상` 기준 **-0.77일 · 실제>예상 14/57** 로 다시 적었다.

### 전파 확인 (§4-(2) 직접 관측 2종 — 추론 금지 규약)

(a) `stock_research\retro_feedback.md` 헤더 = *"호스트가 **2026-08-21 04:18** 에 회수"* —
(b) `outbox\RETRO_DONE.flag` **소멸 확인**(scan-back 이 회수 시 삭제). → **26회차 PART A 지시는 라이브다.**
실행 타임라인: 03:15 시작 → 04:03 데이터셋 완료 → 04:18 회수 완료(아침 06:20 슬롯 미침범).

### 참고 출처(웹 검증)

이번 회차는 **웹검색을 사용하지 않았다.** 시간 예산 규율(아침 06:20 이전 종료)에 따라, 데이터셋·메타
(`partial_asof_index`)·원장·세션 원본(`predictions.json`·`night_calls.jsonl`·`scorecard.md`)만으로 답할 수
있는 질문에 한정했다. 08-20 코스피 +5.89% 는 dataset meta 실측값이며 외부 기사로 교차 확인하지 않았다 —
등락률 자체는 FSC 종가 기반이라 신뢰 가능하나, **'왜 반등했는가'의 해석은 이번 회차에 수행하지 않았다**
(27회차 이월).

---

## 11. 부록 — 26회차 2차 실행 추가 실측 (2026-08-21 04:15~04:30, 터미널 MCP 경로)

> 이 회차는 회고 에이전트가 **두 경로로 동시에** 돌았다(아래 (F)). 본문 §0~§10 은 1차 경로(`retro_cli`,
> 03:30 기동)가 1차 드롭 `retro_dataset_2026-08-21_041314` 로 작성했고, 이 부록은 2차 경로(Cowork
> 예정작업, 터미널 MCP)가 **scorecard 갱신 후 재드롭(`_041552`)** 으로 추가 확보한 실측이다.
> **본문 수치를 덮어쓰지 않는다** — 본문 판정을 바꾸는 항목은 (C) 한 건뿐이며 그것도 결론이 아니라 근거 교체다.
> dataset 바이트는 두 드롭이 동일하므로(sha `8468894713e3ee2e`·2,604,720B·544행) 표본 판정은 전부 그대로다.

### (A) T5/A44 독립 검증 — 3회차 연속 불가 상태를 해소했다 (A48 부분 해결)

1차 드롭의 `scorecard.md` 는 생성시각 **2026-08-19 21:37 · "이번 실행 추가 0건"** 으로 24·25회차와 같은 파일이었다
(본문 §6·A48 지적). 2차 실행에서 `python accuracy_tracker.py` 를 1회 돌리자 갱신됐다:

- 누적 채점 엔트리 **524 → 544건(이번 실행 추가 20건)**, 집계창 **07-22~08-12 → 07-23~08-19**
- 갱신본을 `retro_forward.py --push` 로 재드롭 → `latest.date=2026-08-21`, 무결성 **7파일 전건 일치**

**`채점출처별(#A44)` 분해 (T+1)**

| 출처 | 26회차(신규) | 24회차(최초 실측) | 변화 |
|---|---|---|---|
| `next_day` (v11.1 익일 전용) | **11/16 = 68.8%** | 11/14 = 78.6% | 분모 +2 · 분자 +0 (신규 2건 전부 오답) |
| `main_call` (본 콜 이중사용) | 2/4 = 50.0% | 2/4 = 50.0% | 불변 |
| 미기록(구건) | 8/12 = 66.7% | 9/14 = 64.3% | 창 롤링 |

- 시장콜 전체: **T+1 21/32 = 65.6%**(무정보 always-down **37.5%** · Brier 0.635 / 무정보 0.667) ·
  **T+5 14/30 = 46.7%**(always-down **40.0%** · Brier 0.620)
- **판정: 재현 계상 없음.** 신규 표본이 2건뿐이고 T+1 창은 서로 겹치지 않으나 유효 독립관측이 극소다.
  방향(`next_day` > `main_call`)은 3회차째 유지되나 **크기는 78.6% → 68.8% 로 후퇴**했다 —
  24회차의 "v11.1 T+1 분리가 방향상 유효"는 **아직 승격 근거가 아니다**. 27회차 재검.
- 반례: 신규 2건이 모두 오답이며 그중 하나가 **08-19 `next_day` down(prob_down 0.70 · 기대 -3.5%)** 인데
  실제 08-20 은 **+5.89%** 였다(아래 (D)·(E)).

### (B) A46 KDM 차단의 정량적 파급 — `flow_*` 사후 라벨 커버리지 99% → 69% 회귀

본문은 차단 사실(60회 연속 비-JSON)을 확정했으나 **파급의 크기**는 측정하지 않았다. 드롭 간 대조 결과:

| 컬럼 | 23회차(08-04) | 24회차(08-19) | 25회차(08-20) | **26회차(08-21)** |
|---|---|---|---|---|
| `flow_foreign_eok` | 0/505 (0%) | 0/540 (0%) | **540/544 (99%)** | **374/544 (69%)** |
| `dist_disc_count` | 366/505 (72%) | 455/540 (84%) | 466/544 (86%) | 472/544 (87%) |
| `pre_short_change_10d` | 467/505 (92%) | 522/540 (97%) | 530/544 (97%) | 530/544 (97%) |

- 25회차 대비 **동일 행 대조(공통 544행)**: `flow_foreign_eok` **소실(값→None) 170행 · 신규(None→값) 4행 · 값변경 25행**.
- 즉 25회차에 처음 채워졌던 축이 한 회차 만에 **3분의 1 가까이 빠졌다 — 비단조 회귀**이며,
  `pre_*` 가 A1 에서 겪은 병(회차마다 라이브 조회 성패에 좌우)과 **정확히 같은 형태**다.
- **다른 축은 안정** (무음 재라벨 없음 실측, 공통 544행 기준):
  `pre_kospi_ret5d` 변경 **0/544** · `pre_foreign_5d_eok` 0/544 · `pre_indiv_5d_eok` 0/544 ·
  `pre_overheat` 0/544 · `pre_rsi14` 0/544 · `sector`·`cap_bucket`·`exchange` 각 0/544 ·
  `pre_short_balance_ratio` 0/544 · `pre_short_change_10d` 1/544 · `avg_volume_20d` 4/544 ·
  `ret_h_pct` **5/544** · `alpha_vs_own_idx_pct` **5/544** ← 정확히 신규 만기 5행이다(과거 라벨 무변경 확인).
- **영향 범위는 한정적이다**: `flow_*` 는 사후 라벨이라 진입규칙·T1/T2/T3 판정에 쓰이지 않는다(룩어헤드 금지 규약).
  손상되는 것은 §3.1 "큰손이 개인에게 떠넘겼나" 의 **교차검증 축**과 그 서사다.
- **요청(A46 에 추가)**: A1 이 `pre_*` 에 넣은 **캐리포워드를 `flow_*`·`dist_disc_count` 등 사후 라벨로 확장**하라.
  한 번 계산된 점시점 사실은 불변이므로 재사용에 룩어헤드가 없고, 커버리지가 **단조 증가**로 고정된다.
- ★★**결정적 실측(04:27 재실행)**: A50 의 이중 실행 탓에 같은 회차에 `retro_label` 이 **두 번 완주**했고,
  두 산출물의 `flow_*` 커버리지가 갈렸다 — **04:02:44 판 374/544(69%)** vs **04:27:43 판 0/544(0%)**.
  다른 필드는 전부 동일하다(544행 · matured 519 · partial_asof 08-20 · `ret_h_pct`/`alpha_vs_own_idx_pct`/
  `pre_kospi_ret5d` 차이 **0**). 즉 `flow_*` 는 **실행 시각이 KDM 차단 창의 어디에 걸리느냐로 0%↔99% 를 오간다**
  (23·24회차 0% → 25회차 99% → 26회차 04:02판 69% → 04:27판 0%). 이것이 (B)의 캐리포워드 요청을 결정적으로 만든다.
  ★현재 `WORK\retro_dataset.json` 은 **0% 판(04:27)** 이라 27회차 push 는 그 상태에서 시작한다.
  26회차 드롭(69% 판)은 무결성 대조를 통과한 별개 스냅샷이므로 **본 회차 결론은 불변**이다.
- 부수 실측: 같은 실행에서 FDR 도 실패해 `sector`(2,762종목)·`cap/market`(2,872종목)이 **디스크 캐시 폴백**으로 채워졌다
  (`[retro] sector: FDR 실패 -> 디스크 캐시 사용`). 값 자체는 회차 간 변경 0/544 라 이번엔 무해했다.

### (C) 정정 — "분배합류 발동 모집단 소멸" 은 과잉 일반화다 (결론 유지, 근거 교체)

본문 핵심발견 (2)와 원장 헤더 ③이 "Δ 5건 전부 개인 순매도라 조건 자체가 불성립 — **발동 모집단 소멸**"이라 적었다.
**v11.6 라이브 리허설**(지시사항 §4(2) 의무)로 조건 문구를 최근 10추천일(2026-08-03~08-19, 픽 46행)에 그대로 적용하면:

- **발동 5/46 = 10.9%** — 08-06 에이피알(f5 -891 / i5 +291) · 08-06 파인엠텍(-8 / +7) ·
  08-07 포스코퓨처엠(-58 / +10) · 08-07 LG유플러스(-59 / +48) · 08-19 현대건설(-127 / +180, 미만기)
- 23회차 리허설 **0.0%** → 26회차 **10.9%** 로 오히려 **회복**했다. 소멸한 것은 모집단이 아니라
  **Δ(신규 만기 5건)에 우연히 안 들어온 것**이다(Δ 5건은 07-22 1건 + 08-12 4건뿐).
- **기각 결론은 그대로 유지한다.** 다만 근거를 아래 셋으로 교체하라:
  (i) 6회차(21~26) 연속 Δ TRUE 0 = §4(0) 가지치기 (a) 충족
  (ii) **최근 만기 4건의 alpha_own 이 +8.49 · +12.01 · +12.15 · -10.42 = 평균 +5.56% 로 가설과 부호 반대**
  (iii) 5회 검정(19·21·22·23·26회차) 전부 유의성 미달(이번 회차 일내 25블록 -2.26%p · p=0.4111 — 본문 §4 실측 인용,
       2차 경로에서 재계산하지 않음)
- 규칙 총량 **18/30 불변**(비집행 항목).

### (D) 전야 콜 채점 갱신 — §15-b 수치를 확정하고, 본문 (3)의 범위를 좁힌다

1차 실행 시점의 `night_scorecard.md` 는 **2026-08-19 06:31 생성 · 채점 N=3** 으로 스테일이었다.
2차에서 `python night_track.py` 실행 → 원장 **8건(운용 5 · superseded 3) · 채점 4 · 대기 1 · void 0 · invalid 0**.

| 지수 | 방향 적중 | argmax | Brier(3분류) | 레인지 | 평균 \|실현-예상\| |
|---|---|---|---|---|---|
| KOSPI | **2/4 = 50%** (Wilson CI 15.0~85.0%) | 2/4 | **0.5732** (무정보 0.6667) | 3/4 | 2.14%p |
| KOSDAQ | **1/4 = 25%** (CI 4.6~69.9%) | 1/4 | **0.7953** (무정보보다 나쁨) | 3/4 | 2.55%p |

- `for_date=2026-08-20`: **KOSPI up = 적중(+5.89%) · KOSDAQ down = 오답(+1.99%)**
- → **본문 핵심발견 (3) '전야 콜 승리'는 KOSPI 축에서만 성립한다.** 같은 콜의 KOSDAQ 다리는 틀렸다.
- ★**정보집합 비대칭을 반드시 병기하라**: 08-20 을 겨냥한 아침 `next_day` 콜은 **08-19 06:20**(당일 -5.80% 급락 **전**)
  발행이고, 전야 콜은 **08-19 23:45**(급락·미국장 확인 **후**) 발행이다. 08-20 아침 정규 콜은 세션 결번(A49)으로 부재다.
  따라서 이 1건은 §15-b 가 묻는 **"아침 정보(갭·야간선물 확정치)가 정확도를 더했나"의 답이 아니다** —
  '늦게 낸 쪽이 유리했다'는 자명한 방향이며, 사과-사과 비교가 되려면 **같은 날 아침 정규 콜이 존재해야 한다**.
- **market_calls 와 합산 금지 · N=4 로 규칙화 금지**(표본 30건 규약) 유지.

### (E) 08-20 코스피 +5.89% 원인 — 웹 확인(본문이 27회차로 이월한 항목 해소)

- 종가 **6,852.58** (+381.41p, **+5.89%**), **매수 사이드카 발동**, 원·달러 환율 5.1원 하락 마감.
- 촉매: **SK하이닉스 40조원 규모 자사주 매입·전량 소각 발표** + **미국 장기국채 금리 진정** + 외국인 매수 유입.
  삼성전자 **+4.5~4.8%**.
- 회고적 함의: 08-19 급락(-5.80%, 미 30년물 19년 최고 금리쇼크·반도체 차익실현)과 08-20 급반등은
  **같은 축(미 장기금리 × 반도체)의 이틀 왕복**이다. 국면 피처가 아니라 **개별 기업의 자본정책 발표(자사주 소각)**
  가 반등의 절반을 설명하므로, `pre_*` 국면 게이트로 예측 가능한 사건이 아니었다(F8-b '순노출 축소' 처방과 정합).
- **08-19 진입 4픽의 부분수익 평균 +0.095%**(셀트리온 -0.72 · 현대건설 +3.36 · 삼성바이오로직스 +2.81 · 신한지주 -5.07,
  asof 08-20) vs **같은 2세션 코스피 누적 -0.25%** → **+0.34%p 앞섬**.
  ※ A35 규약대로 **단일일 스냅샷이므로 k5 게이트 판정을 바꾸지 않는다**. 4픽 전부 미만기(h=5 3건·h=20 1건)이며
  만기 유입은 27회차다 — 25회차가 예고한 "08-19 과열 4픽으로 k5 재검"은 **27회차로 다시 이월**된다.

### (F) 운영 지적 — 회고 실행 경로가 이중화돼 있다 (신규 A50)

- 이번 회차에 회고 에이전트가 **두 개** 돌았다:
  (i) **Cowork 예정작업**(03:13 기동, 터미널 MCP 경로) · (ii) **Windows 예정작업 `retro_cli`**(03:30:01 기동,
  `prompts\retro_review.md`, `logs\retro_cli_20260821.log`).
- 실측 피해:
  1. `retro_label` 이 **03:14·03:29 두 번 기동됐다가 각각 1800초 잡 캡에 걸려 취소**됐다(약 32분 손실).
     KDM 차단 상태에서 pykrx 가 매 행 재시도·스택 출력을 하느라 정상 대비 실행이 길어진 것이 원인이다.
  2. 두 인스턴스가 동시에 KRX 를 두드리면 **차단(자동화 대량 조회 탐지) 자체를 악화**시킬 수 있다.
  3. 최종적으로 **03:59:57 에 분리 기동(`wmic process call create`)** 한 인스턴스가 04:02:44 에 완주했다.
  4. **산출물 순서가 엇갈렸다**: (ii)가 04:18~04:19 에 원장·리포트·PART A 를 썼는데, (i)이 04:15 에 갱신·재푸시한
     scorecard 가 (ii)의 **A48('scorecard 3회차 연속 동일 파일')을 발행 직후 무효화**했다(위 (A)).
     다음 회차엔 리포트 본문 자체가 덮어써질 수 있다.
- **요청(호스트)**: 회고 실행에 **단일 실행 락**(예: `outbox\RETRO_RUNNING.lock` — 존재하면 즉시 종료)을 걸거나
  두 경로 중 하나를 비활성화하라. A20(double-session)·A3(드롭 경합)와 같은 계보다.
- **절차 권고(회고 지시서)**: 터미널 MCP 의 `run_command` 잡은 **1800초에 자동 취소**된다.
  KDM 차단 상태의 `retro_label` 은 이 캡을 넘길 수 있으므로 **분리 기동 + stdout 리다이렉트**를 표준 절차로 넣어라
  (A46 의 25회차 권고 '로그 리다이렉트 복원'의 구체안 — 이번에 이 방식으로 차단 HTML 원문을 확보했다).

### (G) 2차 자체검증 — 정정 1건 · 독립 재계산 일치

- **정정 1건**: 위 (C) — "발동 모집단 소멸" 서술을 근거 3종으로 교체(결론 불변).
- **독립 재계산 전건 일치**: 본문 §1·§2·§7·§8 의 수치를 원본 JSON 에서 다시 계산했다 —
  matured **519** · Δ **+5**(소실 0) · A41 **116/519 = 22.4%**(공포 59 · 눌림목 38 · 과열 16 · 중립 3) ·
  T1 pred 픽 **159/265 = 60.0%(p=0.0007)** · 종목 **31/52 = 59.6%(p=0.1058)** · 일 **33/46 = 71.7%(p=0.0023)** ·
  최대일 제거 **151/253 = 59.7%(p=0.0012)** · Δ **2/5 = 40%(평균 alpha -1.91)** ·
  profit_take True **71행 alpha +5.95 · dist_disc 6.61 · peak_vol 1.73** vs False **341행 +2.08 · 5.64 · 1.19** ·
  개별과열 누적 IMM 180 vs REST 232 에서 `pre_overheat` 9.23/7.84 · `pre_rsi14` **47.91/47.80** ·
  `pre_dist_52w_high_pct` -28.05/-28.19(무판별 재확인) — **불일치 0건**.
- **경미한 차이 2건(결론 무영향)**:
  · P1 재추천 본문 N=331·corr -0.081 vs 재계산 N=332·corr **-0.082** → 페어링 필터(직전 행 `entry_ref` 결측 처리)
    1행 차이. 추격 132행 alpha +3.18 vs 눌림 200행 +4.08(격차 -0.90%p)로 방향 동일.
  · T4 본문 N=57 vs 재계산 N=58 → `path_view='횡보후상승'` 1행이 순서 사전에 없어 Spearman 에서만 빠진다
    (Spearman N=57, ρ **+0.491** — 24회차 +0.58 → 25회차 +0.545 → 26회차 +0.491 로 3회 연속 감소).
    즉시상승 콜 `days_to_peak<=2` **7/8** vs 전체 기저 **26/58 = 44.8%**, 방어실패 **26/58 = 44.8%**.
- **확인(본문 철회와 동일 결론)**: Δ 의 HMM·신성이엔지 `ret_h_pct` 동일값(**-2.08**)은 원가격 재계산으로
  **우연의 일치 확정** — HMM 21,650→21,200 = **-2.078%**, 신성이엔지 16,800→16,450 = **-2.083%**.
  같은 `(pred_date, horizon)` 안에서 `ret_h` 가 겹치는 그룹은 이 1건뿐이다.
- **표기 불일치 1건(기록용)**: `retro_cli` 종료 요약은 "자체검증 정정 3건", 원장 헤더는 "정정 2건"이다.
  리포트 §10 기준(**2건**)을 정본으로 읽고, 본 부록의 1건을 더해 **26회차 총 정정 3건**으로 계상하라.

---

## 12. 부록 — 26회차 3차 실행 기록 (2026-08-21 06:29~06:50, Cowork 예정작업 경로 — 분석 없음)

본 회차 날짜(08-21)에 회고 예정작업이 **세 번째로 기동**됐다(03:13 Cowork · 03:30 retro_cli · 06:29 본 실행).
본 실행은 **분석을 수행하지 않았다** — 기록만 남긴다.

- **중단 판정 근거(순서대로)**: ① inbox `latest.json` date=2026-08-21(04:15 재드롭)로 지시서 §1 의
  '이미 처리한 날짜'에 해당. ② 04:27 이후 신규 종가가 없고(08-21 장 시작 전) 08-21 아침 세션도 없어
  **신규 라벨·신규 행이 0 임이 자료 없이 확정**된다 — 완주해도 같은 표본 재계산(§0 재현 계상 규칙이
  금지하는 형태)일 뿐이다. ③ 기동 시각이 아침 06:20 리서치 슬롯과 겹쳐(A49) 지속 점유 자체가 해악이다.
  → `retro_label` 중단, `retro_forward --push`·`gen_scorecard` 미실행, 리포트 본문·PART A 무변경.
- **실측 1 — A50 축소 재현**: 첫 `run_command`(retro_label)가 전송 타임아웃으로 잡 추적에서 유실된 채
  프로세스는 생존했고, 재시도가 두 번째 인스턴스를 만들어 **retro_label 2개가 동시 실행**됐다.
  taskkill 로 둘 다 종료. `WORK\retro_dataset.json` 은 04:27:43 판(2,605,100B) 그대로 — 오염 0.
- **실측 2 — KDM 차단 지속 정황**: retro_label 이 A38 병합 로그 한 줄 이후 4분+ 무출력(26회차 2차와
  동일한 pykrx 재시도 패턴). 원문 재확보는 하지 않았다(중단이 우선).
- **실측 3 — 폐루프 상태**: `RETRO_DONE.flag` 부재 + `WORK\retro_feedback.md` 04:32:26 갱신 =
  26회차 PART A 회수 완료 상태 유지(§4-(2) 직접 관측 2종 규약).
- **자체검증**: 본 부록은 신규 수치 주장이 없어 6점검 대상 항목 0 — **정정 0건**(26회차 누계 3건 불변).
- **산출물 변경 목록**: 원장 헤더 노트 1건 + A49·A50 행 관측 추가 + 본 §12. RETRO_DONE.flag 미생성.
