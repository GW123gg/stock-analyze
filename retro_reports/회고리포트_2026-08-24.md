# 회고리포트 2026-08-24 (27회차)

생성: 2026-08-24 04:15 KST · 회고 Cowork(터미널 MCP 경로, 03:16 기동)
입력: `latest.json` date=**2026-08-24**, pushed_at 03:56:04 · dataset `retro_dataset_2026-08-24.json`

---

## 0. 원장 대조 한 줄

26회차(08-21) 산출은 **회수 완료**다 — 직접 관측 2종 모두 확인: `stock_research\retro_feedback.md`
헤더에 "호스트가 2026-08-21 04:32 에 회수"(파일 mtime 04:32:26), `outbox\RETRO_DONE.flag` **부재**.
미해결 이월 4건 중 **A46 해결**(아래 §1-2), **A48 4회차 연속 동일 본문 · A44 는 분모 불변으로 갱신 불가**(둘 다 원인은 A45/A49 계열의 신규 A53),
**A47 규약 적용**(§3.1), **A41 재개 조건 미충족 유지**. 신규 A 항목 3건 등재(**A52·A53·A54**).
★번호 규칙 처리: 호스트가 `코워크_통합지시_최종.md` v11.29 §A-0.5 에서 이미 **#A51**(예정작업 배정표)을
배포했는데 원장 A표에는 행이 없었다 — 재사용 금지 규칙(2026-07-26)에 따라 **A51 을 예약행으로 등재**하고
채번을 A52 부터 시작했다. **다음 신규 항목은 A55 부터.** A표 A1~A54 중복 **0건**(전수 스캔).

---

## 1. 요약

### 1-0. 표본 — 이번 회차는 **신규 만기 0**이다

| 항목 | 27회차(08-24) | 26회차(08-21) | 판정 |
|---|---|---|---|
| 전체 행 | 544 | 544 | 불변 |
| `label_status=matured` | **519** | 519 | **Δ +0** |
| `maturing` | 25 | 25 | 불변 |
| 소실(직전 matured 키 이탈) | **0** | 0 | 정상 |
| 고유 종목(`n_unique_tickers`) | 86 | 86 | 불변 |
| `pred_date` 최댓값 | **2026-08-19** | 2026-08-19 | **정체(A53)** |

**따라서 §3 재현 계상 규칙에 의해 이번 회차는 어떤 규칙의 재현도, 반증도 계상하지 않는다.**
Δ-단독 검정(v11.6)의 분모가 0이므로 승격·기각 발의도 전부 금지다. 26회차가 27회차로 이월한
**T2 k5 재검(08-19 k5=+9.05 진입 4픽)과 P1 재판정은 두 건 다 표본이 안 들어와 28회차로 재이월**한다.

### 1-1. 무결성 · 라벨 지연 · 스냅샷 편향

- **무결성 대조 통과(7파일 전건)**: `retro_dataset_2026-08-24.json` sha256 `73a560a58e807ec1` ·
  2,609,294B · rowcount 544 — 매니페스트와 sha·바이트·행수 3종 모두 일치. CSV(496,160B·544행)
  및 scorecard·market_calls·night_calls·night_scorecard·intraday_review 도 전건 일치.
- ★**라벨 지연 0 거래일**: `partial_asof_date` 최댓값 **2026-08-21**(미만기 25행 전부 동일값),
  직전 영업일 2026-08-21(금)과 일치. 03:16 야간 실행인데도 FSC 공식시세가 직전 거래일 종가를
  싣고 있었다 — 26회차에 이어 2회 연속 지연 0.
- **스냅샷 편향은 이번엔 경미**: `partial_asof_index`가 08-21에 대해 `kospi_ret_1d_pct` **+0.88%**,
  `kospi_ret_5d_pct` **+1.46%**. 26회차 스냅샷일(08-20, 코스피 **+5.89%**)의 극단 낙관 편향과 달리
  이번 미만기 부분수익은 약한 낙관 쪽으로만 기운다(A35 규약대로 스냅샷일과 함께만 인용).
- **국면 스냅샷 결측**: `dates_missing_after_start` = **['2026-07-19'] 1일 불변**. 신규 결함 없음.

### 1-2. 이번에 본 핵심 3가지

**(1) matured 라벨의 무음 재라벨 0건 — 점시점 불변성 재확인**

공통 544행 전수 대조에서 **`label_status=matured` 519행의 라벨 5종(`ret_h_pct`·`alpha_h_pct`·
`alpha_vs_own_idx_pct`·`days_to_peak`·`profit_take_flag`)이 전부 0/519 변경**이었다.
드리프트가 잡힌 필드는 전부 **미만기 25행에만** 국한된다:

| 필드 | 값변경 | 결측→값 | 해석 |
|---|---|---|---|
| `ret_partial_pct` | 25 | 0 | 미만기 25행 = 08-21 종가 반영(정상) |
| `post_peak_drawdown_pct` | 15 | 0 | 미만기 경로 라벨 갱신(정상) |
| `max_drawdown_pct`·`days_to_trough` | 5·5 | 0 | 동상 |
| `days_to_peak`·`peak_gain_pct` | 3·3 | 0 | 동상 |
| `flow_foreign/inst/indiv_eok` | 25 | **170** | ★A46 복원(아래) |
| `dist_disc_count` | 2 | **17** | 커버리지 개선 |

→ A18/A22 계열의 '점시점 불변성 위반'은 이번 회차에도 재발 없음. **이건 무결성 점검이지 규칙
재현이 아니므로 어떤 카운터에도 +1 하지 않는다.**

**(2) A46 해결 — `flow_*` 사후 라벨 커버리지가 100%로 복원됐고, 값이 25회차판과 일치한다**

| 필드 | 27회차 | 26회차 | 25회차(08-20판) |
|---|---|---|---|
| `flow_foreign_eok` | **544/544 = 100%** | 374/544 = 68.8% | 540/544 = 99% |
| `flow_inst_eok` | **544/544 = 100%** | 374/544 = 68.8% | 99% |
| `flow_indiv_eok` | **544/544 = 100%** | 374/544 = 68.8% | 99% |
| `dist_disc_count` | 489/544 = 89.9% | 472/544 = 86.8% | — |

- 결측→값으로 채워진 **170행이 26회차가 실측한 '소실 170행'과 정확히 일치**한다.
- 복원된 값을 25회차(08-20)판과 직접 대조: **동일 515 · 상이 25 · 08-20엔 없고 새 판에만 4**.
  상이 25건은 **미만기 25행**(보유창이 하루 늘어 순매수 누계가 갱신된 정상 변화)이다.
- → 26회차가 요청한 "A1 캐리포워드를 `flow_*`·`dist_disc_count` 등 사후 라벨로 확장"이
  **이행됐고, 커버리지 단조 증가가 실증됐다. A46 [해결됨 2026-08-24]로 종결.**
- ※ 다만 `retro_label` 실행 자체는 여전히 **37분**(03:18:43~03:55) 걸렸다 — KDM 차단 정황은
  해소되지 않았고, 캐리포워드가 그 파급만 막고 있다(A54와 함께 볼 것).

**(3) ★원장 B표 수치의 계산 정의가 문서에 없어 같은 데이터로도 재현이 갈린다 (신규 A52)**

Δ=0 이라 새 판정이 불가능한 회차이므로, 이번 회차의 주 과제를 **"원장 수치를 같은 데이터로
다시 만들어 보는 재현성 감사"**로 잡았다. 결과는 **4건 중 3건 역추적 재현 성공 · 1건 실패**다.

| 원장 26회차 수치 | 내 1차 정의로 계산한 값 | 역추적으로 찾은 원장 정의 | 재현? |
|---|---|---|---|
| T1 종목 클러스터 **31/52 = 59.6%** | 행 과반 양수 → 27/52 = 51.9% | **종목별 `alpha_vs_own_idx_pct` 평균 > 0** | **성공** |
| P1 corr **-0.081 / -0.082**(N 331/332) | y=`alpha_h_pct` → -0.141 | **y=`alpha_vs_own_idx_pct`**, distinct_day 처리 유무가 N 331/332를 가름 | **성공** |
| T3 일내 **25블록 -2.26%p** | y=`alpha_h_pct`·픽 → -1.27%p | **y=`alpha_vs_own_idx_pct`·픽 한정** | **성공** |
| T7 **IMM 180 / REST 232** (overheat 9.23/7.84) | `dtp<=2 & 되돌림>=5%` → 135/277 | **`days_to_peak<=2` 단독**, 픽 한정 | **성공** |
| T4 Spearman **rho +0.491 (N=57)** | Spearman +0.214 (N=58) | — 12개 변형 전부 실패 | **실패** |

- T4 실패의 의미가 크다. matured 라벨이 **0/519 변경**이므로 데이터 변화로는 설명할 수 없다.
  변형 스윕 결과(전부 N=53~58): `all` +0.214 · `pick only` +0.215 · `pred only` +0.214 ·
  `횡보후상승 제외(N=57)` +0.213 · `이벤트대기 제외` +0.159 · `peak_gain>0` +0.032 ·
  **Pearson +0.375**(최댓값). **어떤 조합도 +0.491 에 닿지 않는다.**
- 이건 "규칙이 약해졌다"가 아니라 **"원장에 실린 숫자를 이 데이터로 다시 만들 수 없다"**는
  문제다. 회고의 모든 판정은 회차를 넘어 누적되는데, 그 누적의 단위인 숫자가 재현되지 않으면
  '3회 연속 감소' 같은 서사 자체가 검증 불가가 된다.
- → **정정**: T4 상관 수치를 이번 회차 명시 정의(**Spearman +0.214 / Pearson +0.375, N=58,
  expected_peak_days vs days_to_peak, matured 전체**)로 대체하고, 26회차 부록의 +0.491 은
  **원장에서 '재현 불가'로 표기**한다. 정의 5종을 C절에 고정한다(§6).
- 부수 확인: 26회차의 p 값들은 **단측(one-sided)**이었다(예: 159/265 → 단측 0.0007 · 양측 0.0014,
  정확히 2배). 이후로는 **양측을 정본으로 하고 단측을 괄호 병기**한다.

### 1-3. 사전등록 가설 목록 · 수행 검정 총수 (§0 규약)

Δ 신규 만기가 0으로 확정된 시점에서 **확증검정(승격·기각·재현+1)은 대상이 없다.** 그래서
이번 회차는 확증 목록을 비우고, **무결성·재현성 검증(V) 5건**을 사전등록했다.

- **V1** matured 라벨 무음 재라벨 검사(519행 × 라벨 5종) → 통과(0건)
- **V2** A46 `flow_*` 커버리지 복원 및 값 일치 확인 → 통과(100%·515/540 동일)
- **V3** 원장 B표 5개 수치 재현 → 3 성공 · 1 성공(T7) · **1 실패(T4)**
- **V4** A41 국면 게이트 재개 3조건 충족 여부 → **미충족**
- **V5** 라벨 지연·스냅샷 편향 정량화 → 지연 0거래일 · 편향 +0.88%(1d)
- **탐색 E1**(후보 등재만) 픽 선별력의 테마·시총 의존도 → §2-2

**수행 검정 총수 N = 42** (이항 20 · 순열 2 · 상관 20).
Bonferroni×42 생존 **3건**(T1 바이오만 p1<0.0001 · 메가캡만 p1<0.0001 · KOSPI만 p1<0.0001),
BH-FDR(q=0.05) 생존 **4건**(위 3건 + 전체 행 159/265 p1=0.0007). **★생존한 것들은 전부 같은
265행에서 잘라낸 중첩 표본이므로 독립 검정이 아니다** — 다중비교 보정의 전제가 깨져 있다.
그리고 **이 전부가 탐색 스캔이므로 §0-2 에 따라 후보 등재까지만이다.**

### 1-4. 국면 `pre_*` 커버리지와 분산 (A41 재개 조건)

- 커버리지: `pre_regime_kind`·`pre_allow_market_up`·`pre_caution_score`·`pre_pcr_oi`
  각 **116/519 = 22.4%**(불변), `pre_vkospi` **71/519 = 13.7%**, `pre_kospi_ret5d` 100%.
- **분산(A41 문구 그대로 — 행 수가 아니라 소수 클래스)**:
  · `pre_regime_kind` 클래스 분포 = 공포 59 · 눌림목 38 · 과열 16 · **중립 3** → **소수 클래스 3행 < 15 기준 미달**
  · `pre_allow_market_up=false` **고유일 2 < 5 기준 미달** (행 6)
  · `pre_caution_score>=60` **고유일 2 < 5 기준 미달** (행 6)
- → **3조건 전부 미충족. F1/F8 은 '검증 대기' 유지**, 행 수 기준의 예고는 하지 않는다.

### 1-5. 진행 중 조기경보 (§3.12)

미만기 **25행**(고유 종목 9 · h=20 22행 · h=5 3행), `ret_partial` 평균 **+2.25%** 중앙 **+3.50%**,
양수 18/25 — **단 08-21 단일일 스냅샷이므로 성과로 읽지 마라(A35)**.
확정 게이트 위반이 살아있는 픽은 **`pre_kospi_ret5d`>+2% 진입 7행**이며, 그중 **08-19 진입
4픽(k5 = +9.05)**이 26회차가 27회차 재검 표본으로 지목한 그 픽들이다.

| 종목 | h | conv | tag | ret_partial(asof 08-21) |
|---|---|---|---|---|
| 셀트리온(068270) | 5 | 0.45 | 단기스윙 | -1.64% |
| 현대건설(000720) | 5 | 0.40 | 단기스윙 | -4.09% |
| 신한지주(055550) | 5 | 0.38 | 단기스윙 | -2.25% |
| 삼성바이오로직스(207940) | 20 | 0.38 | 장투가능 | +1.31% |

평균 **-1.67%**. 26회차 시점(asof 08-20)의 **+0.095%**에서 내려왔고, 같은 08-21 코스피는
**+0.88%**였다. 방향은 k5 게이트 가설과 부합하지만 **전부 미만기이고 단일일 스냅샷**이므로
**판정하지 않는다.** h=5 3건은 08-26 만기 예정이니 그 다음 드롭이 T2 의 진짜 재검 표본이다.

---

## 2. 차익실현 패턴 (§3.1·3.6·3.9·3.10)

### 2-1. 표본 불변 — 수치만 재확인

전 수치가 26회차와 동일(Δ=0)하다. §3.9/§3.10 병기 의무만 이행한다.

| 구분 | N | `ret_h_pct` | `alpha_h_pct` | `dist_disc_count` |
|---|---|---|---|---|
| 픽 `profit_take_flag=True` | 71 | **+1.17%** | **+5.37%** | 6.59 |
| 픽 `profit_take_flag=False` | 341 | -3.06% | +2.19% | 5.53 |
| 픽 즉시고점 IMM(`days_to_peak<=2`) | 180 | — | — | — |
| 픽 REST | 232 | — | — | — |

- 분배 공시 가설(§3.6)은 **방향 유지·크기 미미**: True 6.59 vs False 5.53건(+1.06건).
  단독으로는 판별력이라 부르기 어렵다. **반례**: 알테오젠(08-02, h=20)은 `dist_disc_count`가
  낮은 쪽인데도 미만기 부분수익이 26회차 +10.05% → 27회차 +3.73%로 되돌렸다.
- ★**`profit_take_flag`는 사후 관측 라벨이다** — 진입 회피·손절 트리거로 쓰지 마라(07-03 지침).
  True 군의 `ret_h`가 오히려 양수(+1.17%)라는 사실이 그 이유를 그대로 보여준다.
- 손절 반사실(§3.13, 픽 412행): 실제 `ret_h` **-2.33%** / `-8%` 손절 **-2.46%** /
  `-8%·+12%캡` **-2.28%**. **세 값의 차이가 0.2%p 미만**이고, 손절이 오히려 손해였던
  행이 **81/412 = 19.7%**다. → **손절 기본값 변경 권고는 이번에도 발행하지 않는다**(15회차 철회 유지).

### 2-2. ★탐색 발견(후보 등재만) — 픽 선별력 60.0%가 대형 바이오 3종목에 얹혀 있다

B표 최상단 항목 '픽의 종목 선별력'(`alpha_vs_own_idx_pct>0` 비율 vs 코인플립 50%)을
**섹터·시총·거래소로 분해**했다. prediction 픽 265행 기준(행/고유종목/고유일 = **265 / 52 / 46**):

| 부분군 | 행 적중 | 단측 p | 종목 클러스터 | 평균 alpha |
|---|---|---|---|---|
| **전체(prediction 픽)** | 159/265 = **60.0%** | 0.0007 | 31/52 = 59.6% | +4.47% |
| 바이오(기초 의약물질 제조업)만 | 45/49 = **91.8%** | <0.0001 | 3/3 = 100% | **+17.49%** |
| **바이오 제외** | 114/216 = **52.8%** | **0.2271** | 28/49 = 57.1% | +1.51% |
| 메가캡(10조+)만 | 141/217 = 65.0% | <0.0001 | 25/38 = 65.8% | +6.12% |
| **메가캡 제외** | 18/48 = **37.5%** | 0.9703 | 6/14 = 42.9% | **-3.02%** |
| KOSPI만 | 138/210 = 65.7% | <0.0001 | 25/39 = 64.1% | +6.27% |
| KOSDAQ만 | 21/55 = **38.2%** | 0.9710 | 6/13 = 46.2% | -2.42% |

- 종목별 기여 1·2위가 **셀트리온(N=21, 평균 +18.62%)·삼성바이오로직스(N=27, +16.94%)** 로,
  둘이 바이오 49행 중 **48행**이다. 즉 **'선별력'의 통계적 유의성은 사실상 두 종목이 만든다.**
- 최대 기여일(2026-06-25) 1일 제거로는 안 무너진다(151/253 = 59.7%, 단측 0.0012) — **강건성이
  깨지는 축은 날짜가 아니라 섹터·시총**이다.
- **웹 확인(§0.5)**: 2026년 국내 제약·바이오 강세의 서술된 동인은 GLP-1 비만치료제 시장 확대,
  글로벌 빅파마 특허만료(LOE), CDMO 생산능력 확장, M&A·라이선스 활성화 등 **섹터 구조 요인**이고,
  삼성바이오로직스는 5공장·미국공장·신규 CDMO 수주가 주가 핵심 변수로 지목된다. 우리 픽이
  종목을 잘 골라서라기보다 **강한 테마에 반복 노출된 결과**로 읽히는 쪽에 무게가 실린다.
- **반례(최소 1개 의무 — 4개 제시)**: '메가캡이면 다 된다'도 아니다. 비바이오 대형·메가에서
  **이오테크닉스(N=6, 평균 -12.67%) · HD현대중공업(N=1, -19.24%) · 에코프로(-15.67%) ·
  LG에너지솔루션(N=2, -12.26%)** 이 크게 음수다. 또 바이오 대형인 알테오젠은 아직 미만기다.
- **판정: 확증 금지.** §0-2 대로 탐색 스캔 발견이므로 **B표에 후보 메모로만 등재**하고,
  28회차에 **"바이오 섹터 제외 판 선별력"을 사전등록해** 신규 만기에서 확증한다.
  다만 이 결과는 26회차의 '관찰(강)→관찰(중) 하향, 승격 요건 미달' 판정과 **같은 방향**이며,
  승격 논의를 다시 여는 것을 더 어렵게 만든다.

### 2-3. §3.9 서브샘플 분열 — archive 픽이 방향을 희석한다

| 표본 | 행 적중 | 비율 | 양측 p (단측) |
|---|---|---|---|
| 전체 matured 픽 | 218/412 | **52.9%** | 0.2571 (0.1286) |
| `_src_kind='prediction'` | 159/265 | **60.0%** | 0.0014 (0.0007) |
| **차분 = archive 픽** | 59/147 | **40.1%** | — |

archive 147행이 40.1%로 코인플립 아래다. A47(라벨창 중복)을 적용해도 방향은 살아남되 크기가 준다:
**라벨창 dedup 149/250 = 59.6%**(양측 0.0029) → **거래일 발행만 119/209 = 56.9%**(양측 0.0525,
Bonferroni×42·BH 모두 미통과). 26회차 결론과 동일하다.

---

## 3. 태그 / 타이밍 / 확신도 점검 (§3.2·3.3·3.4)

### 3-1. 확신도 — 동결 유지

`conviction > 0.65` 만기 표본 **누적 0건**(F5 조건부 상한 정책의 산물) → **동결 유지**,
이번 회차 사전등록 안 함. `>=0.50` 이분 축은 **Δ=0 이므로 재검하지 않는다**(§3.3 재검 시점 규약:
신규 만기 +1건 이상인 회차에만). scorecard calibration 도 표본 불변: [0.00-0.50] 94건 52% /
(0.50-0.65] 31건 61% / (0.65-0.80] 2건 0% / (0.80-1.00] 0건.

### 3-2. 태그·타이밍·선반영 (탐색 관찰 — 처방 금지)

픽 matured 412행, `alpha_vs_own_idx_pct` 평균:

| 축 | 값 |
|---|---|
| tag | 장투가능 N=140 **+8.31%** / 단기스윙 N=222 +0.20% / 무태그 N=35 -1.78% / 장전선취매 N=15 -1.01% |
| timing | 중기 N=60 **+16.11%** / 임박 N=15 +1.00% / 단기 N=177 +0.94% / 미기입 N=160 -0.10% |
| preprice | 부분 N=162 +4.88% / 미반영 N=95 +4.37% / 강함 N=8 -2.85% / 미기입 N=147 -0.35% |
| cap_bucket | 메가 N=314 +3.98% / 대형 N=81 -0.34% / 중형 N=11 -8.11% / 소형 N=6 -0.23% |
| exchange | KOSPI N=306 +3.67% / KOSDAQ N=106 +0.07% |

- ★**태그 수치로 비중 조정을 제안하지 마라**(scorecard 07-17 규약 + '단기스윙 축소' 8회 기각).
  더구나 이 표의 장투가능·중기 우위는 **§2-2 의 바이오 클러스터와 거의 같은 행들**이다
  (셀트리온·삼바가 장투가능·중기에 몰려 있다). 태그의 효과가 아니라 **테마의 그림자**로 봐야 한다.
- 선반영 게이트(§3.4)는 방향 유지: 강함 8건 -2.85% < 부분 +4.88% · 미반영 +4.37%. **N=8 이라
  규칙화 금지**, 기존 [3] 선반영 게이트 유지 근거로만.
- 시총 단조(B표 '시총·시장 분해' 가설)는 이번에도 방향 유지 — **가설 유지·규칙화 금지**.

---

## 4. 시장국면 의존성 (§3.5·3.14·3.15)

### 4-1. k5 강세추격 게이트 — Δ 과열일 0, **계상 불가 3회차 연속**

| 표본 | 과열(k5>+2%) | 눌림 |
|---|---|---|
| 전체 matured 픽 | N=101 · `ret_h` **-4.95%** · `alpha_h` +2.38% · 적중 31/101 = 30.7% | N=311 · -1.48% · +2.86% · 151/311 = 48.6% |
| prediction 픽 | N=40 · **-6.31%** · +2.99% · 16/40 = 40.0% | N=225 · -2.20% · +5.77% · 102/225 = 45.3% |

- 과열 진입 **고유일 12일**(05-30·05-31·06-03·06-04·06-05·06-18·06-19·07-01·07-28·08-05·08-06·08-07).
  **Δ 신규 과열일 0 · Δ 과열행 0** → 재현 계상 불가(24회차 +1 이후 3회차 연속).
- ★**알파 분해(§3.10)가 처방을 가른다**: 과열군은 `ret_h` 가 크게 음수인데 `alpha_h`는 **양수**다
  (전체 +2.38%, prediction +2.99%). **주범은 시장 베타이지 종목 선택이 아니다** →
  처방은 '픽 억제'가 아니라 **그날 순노출 축소**. 현행 '경고·순노출 축소' 등급이 정확하다.
- 라이브 리허설(최근 10 추천일 픽 46행): k5>+2% 발동 **19/46 = 41.3%**(불변).
- 판정: **'경고·순노출 축소' 유지. 하드 게이트 승격 금지.**

### 4-2. 진입 전 분배합류 게이트 — Δ 0, 7회차 연속 무재현(21~27)

| 표본 | TRUE(`pre_foreign_5d<0` & `pre_indiv_5d>0`) | FALSE |
|---|---|---|
| 전체 matured 픽 | N=40 · `alpha_h` **-2.49%** · `ret_h` -2.49% | N=372 · +3.30% · -2.32% |
| prediction 픽 | N=17 · -0.35% | N=248 · +5.74% |

- 일내 블록 대조(**원장 정의 = 픽 한정 · y=`alpha_vs_own_idx_pct`**): **혼합 25블록 · 평균차
  -2.26%p · 양수 11/25** — 26회차 값 그대로 재현(§1-2(3)). 유의성 없음.
- 라이브 리허설: 최근 10 추천일 픽 46행 중 **발동 5행 = 10.9%** — 26회차와 동일.
  **발동 모집단은 살아 있다**(26회차 정정 #3 유지).
- 26회차가 **기각 절차를 개시**했고 이번 회차는 Δ=0 이라 **새 근거를 더할 수 없다**.
  → **기각 결론 유지 · 절차는 28회차 신규 만기에서 마무리**한다.
  **비집행 관찰 항목이므로 규칙 총량과 무관.**

### 4-3. 신호 13종 (§3.14) · 지수 방향 콜 (§3.15) · 전야 콜 (§3.15-b)

- **신호 13종**: A41 3조건 미충족(§1-4)이라 **국면 프록시 비교 자체를 수행하지 않는다.**
  `pre_kospi_ret5d`(100% 커버)보다 나은지의 판정은 **검증 대기**다.
- **market_call**: scorecard 가 **4회차 연속 동일 본문**(누적 채점 544건 · **이번 실행 추가 0건**,
  생성 시각만 08-21 04:15 → 08-21 09:15 로 바뀜). ★**정정(자체검증 ③)**: A44/T5 는 '연속 불가'가
  아니다 — **26회차 2차 실행이 이미 수행했다.** 이번 회차의 정확한 상태는 **"분모가 한 건도 늘지
  않아 그때 수치를 그대로 재확인할 뿐, 갱신이 불가"**다. 기록만 남긴다 —
  T+1 **21/32 = 66%**(무정보 always-down 38% · Brier 0.635),
  채점출처별 `next_day` **11/16 = 69%** / `main_call` 2/4 / 미기록 8/12 ·
  T+5 **14/30 = 47%**(always-down 40% · Brier 0.620). **분모가 안 늘었으므로 재현 계상 없음.**
- **전야(23시) 콜**: `night_scorecard` 갱신 2026-08-21 09:15, 채점 **4** / 대기 1.
  KOSPI 방향 2/4 = 50%(Wilson 15.0~85.0) · Brier **0.5732**(무정보 0.6667) /
  KOSDAQ 1/4 = 25% · Brier **0.7953**(무정보보다 나쁨). **market_calls 와 합산하지 않았다.**
  ★관측: **for_date 2026-08-21 콜(KOSPI·KOSDAQ 모두 `down`)이 아직 `pending`인데, 우리 dataset 의
  `partial_asof_index` 는 08-21 코스피 **+0.88%** 를 싣고 있다** — 밴드 ±0.5% 밖이므로 채점되면
  오답이 될 값이다. 즉 **성적표가 라벨보다 늦다**(신규 A54). 규칙화는 하지 않는다(표본 30 미달).

---

## 5. 숏 익절 경로 (§3.8·3.11)

**Δ 숏 신규 만기 0 — 4회차 연속.** 모든 판정 불변.

| 표본 | N | 초과하락(`alpha_h`<0) | `ret_h` | `alpha_h` |
|---|---|---|---|---|
| 전체 matured 숏 | 107 | 56/107 = 52.3% | -3.80% | -1.21% |
| prediction 숏 | 69 | **29/69 = 42.0%** | -4.08% | +0.79% |

- **무정보 기저율 병기(A24)**: scorecard 상 숏 방향적중 8/17 = 47% vs **always-short 53%** —
  기저율 아래다. `hit`은 수익률 부호의 재라벨이라 같은 표본 기저율과 비교 금지(08-04 규약).
  **판단 축은 alpha 이고, prediction 42.0%는 코인플립 아래**다 → 숏 방향 우위 **무신호 유지**.
- A42 대체 라벨: `short_cover_d4_ret_pct` 평균 **-3.47%**(N=105) ·
  `short_stop9_ret_pct` 평균 **-13.81%**(N=48). **Δ=0 이므로 in-sample 재확인일 뿐 판정 불가.**
- B(11) '저점 대비 +8% 반등 시 커버'는 **정식 기각 유지**(실시간 관측 불가 트리거).

---

## 6. 다음에 고칠 점 (원장에도 반영)

1. ★**원장 지표 정의를 C절에 수식 수준으로 고정한다(신규 A52).** 이번에 5개 중 4개는 역추적으로
   재현했지만, 그 역추적에만 12개 변형 스윕이 들었다. 고정할 5종:
   · **픽 선별력(행)** = matured·`kind=pick`·`_src_kind='prediction'` 중 `alpha_vs_own_idx_pct>0` 비율
   · **종목 클러스터** = 종목별 `alpha_vs_own_idx_pct` **평균 > 0** 인 종목 수 / 고유 종목 수
   · **일내 블록 대조** = 같은 `(pred_date,horizon)` 블록·**픽 한정**·y=`alpha_vs_own_idx_pct`
   · **즉시고점 IMM** = **`days_to_peak <= 2` 단독**(되돌림 조건 없음), 픽 한정
   · **재추천 방향 corr** = y=`alpha_vs_own_idx_pct`, gap=(이번 `entry_ref`/직전 `entry_ref`-1)
   그리고 **p 값은 양측을 정본으로, 단측을 괄호 병기**한다.
2. ★**T4 상관 수치 정정.** 26회차 부록의 Spearman +0.491(N=57)은 **재현 불가**다.
   정본을 **Spearman +0.214 / Pearson +0.375 (N=58)**로 교체하고, '3회 연속 감소' 서사는
   재현 가능한 값으로 다시 세워야 한다(현 시점 계상 없음).
3. ★**아침 예측이 2거래일 연속 0건이다(신규 A53).** dataset `pred_date` 최댓값이 08-19 에서
   멈춰 있고, `output\` 에는 **2026-08-20_091700 · 2026-08-21_092110** 세션이 있으나 **둘 다
   06:20 정규가 아닌 09:17~09:21 장중 대체이고 예측 0건**이다. A45(미발행)는 세션 생성으로
   해결됐지만 **예측 산출은 회복되지 않았다**. 회고 표본이 Δ +5 → +5 → **0** 으로 말라간다.
   회고가 아침 슬롯을 삼키는 A49 가 근본 원인일 개연성이 크다(오늘 회고는 03:16 기동 →
   03:55 라벨 완료 → 04:20 산출·회수로 **06:20 슬롯을 침범하지 않았다**).
4. ★**`night_track` 이 회고 실행 경로에 없다(신규 A54).** 08-21 전야 콜이 `pending`인 채로
   성적표가 굳었는데, 같은 dataset 은 08-21 지수 등락(+0.88%)을 이미 싣고 있다. 회고 드롭 직전에
   night_track 재채점을 한 번 돌리면 이 시차가 사라진다.
5. **A48 은 A53 의 종속 문제다.** scorecard 본문이 4회차 연속 동일한 이유는 채점할 신규 예측이
   없어서다. A44 는 **26회차 2차 실행에서 이미 수행됐고**(next_day 11/16 = 68.8%), 남은 과제는
   '수행'이 아니라 **분모 증가**다. → **28회차부터는 'A44 검증 시도'를 사전등록하지 말고
   A53 해결(신규 예측 유입) 확인만 하라**(헛된 사전등록 방지, A41 교훈의 확장).
6. **가지치기(§4(0))**: 규칙 총량 **18/30 불변**, 신규 판단 규칙 발행 **0건**. 규칙이 늘기만 한
   회차는 아니므로 §6 경고 조건 미해당. '진입 전 분배합류'는 (a) 조건 충족 상태 유지(7회차 무재현).
7. **A47 카운트 정의도 갈렸다.** 라벨창 중복은 **29그룹·초과 31행(6.0%)**로 26회차와 동일하나,
   '주말 발행분'을 26회차는 28행, 나는 **초과행(그룹 첫 행 제외) 기준 8행**으로 셌다.
   **초과행 기준으로 고정**하고 원장 C절에 적는다.

### 별첨 A — 아침 지시서(`cowork_instructions.md`) 청소 후보 3건

1. **806행 부근 '분배합류 추격 금지' 문구** — 26회차가 이미 '경고'로 하향 권고했고, 이번 회차에
   기각 절차가 유지된다. **다음 물리 삭제 회차에 '경고'로 문구 교체**(삭제가 아니라 강등).
2. **F1/F8 국면 게이트 조항의 '행 수' 기준 잔존 표현** — A41 이 분산 기준으로 교체했는데
   행 수 문구가 남아 있으면 헛된 사전등록을 유발한다. 분산 문구로 통일.
3. **'단기스윙 태그 축소' 계열 잔재** — 8회 연속 기각된 처방이다. scorecard 는 이미 처방을 뺐으니
   지시서 쪽 잔존 문장이 있으면 제거 대상.

---

## 7. 자체검증 (§4(2.5) 6점검) — **정정 7건**

| # | 점검 | 결과 |
|---|---|---|
| ① | 열 이름 = 실제 계산값인가 | **정정 4건**(아래 1~4) |
| ② | 재현 카운터가 §3 계상 규칙을 지켰는가 | 통과 — Δ=0 이라 **전 규칙 카운터 동결**, 승격·기각 발의 0건 |
| ③ | 기각/철회 대상이 실제로 살아 있는가 | **정정 1건**(아래 7) — 분배합류 기각은 26회차 개시분의 '유지'이지 신규 기각이 아님은 통과 |
| ④ | 부등호·대소 방향이 표의 숫자와 일치하는가 | 통과 — 과열군 `ret_h` 음수 / `alpha_h` 양수의 부호 분열을 '베타 주범'으로 정확히 서술 |
| ⑤ | N·'전부 X' 단정을 재집계했는가 | 통과 — 행 265 / 고유종목 52 / 고유일 46 3종 병기, `_src_kind` 별 분해(pred 265 · archive 147) |
| ⑥ | 적중률 옆에 무정보 기저율을 적었는가 | 통과 — 숏 always-short 53%, market_call always-down 38%/40%, 코인플립 50% 병기 |

**정정 내역 6건**

1. T1 종목 클러스터를 '행 과반 양수'(27/52 = 51.9%)로 계산했다가 → 원장 정의(종목별 alpha 평균>0)
   **31/52 = 59.6%** 로 교체.
2. T3 일내 블록 대조를 `alpha_h_pct`(-1.27%p)로 계산했다가 → 원장 정의(`alpha_vs_own_idx_pct`·픽 한정)
   **-2.26%p** 로 교체.
3. T7 IMM 정의를 `days_to_peak<=2 AND |되돌림|>=5%`(135/277)로 잡았다가 → 원장 정의
   **`days_to_peak<=2` 단독, 픽 한정 180/232**(overheat 9.23/7.84 · rsi14 47.91/47.80 ·
   dist52w -28.05/-28.19)로 교체.
4. P1 corr 을 y=`alpha_h_pct`(-0.141)로 계산했다가 → 원장 정의 y=`alpha_vs_own_idx_pct`
   **-0.082(N=332) / -0.081(N=331)** 로 교체.
5. 26회차 T4 Spearman **+0.491(N=57)** 을 그대로 인용하려다 → **12개 변형 전부 재현 실패**를
   확인하고 **+0.214(Spearman) / +0.375(Pearson), N=58** 로 정정, 원장에 '재현 불가' 표기.
6. p 값을 양측만 적었다가 → 26회차 관행이 **단측**임을 확인(159/265 단측 0.0007 = 양측 0.0014의 절반)
   하고 **양측 정본 · 단측 병기**로 교체.
7. ★초안에 "A44/T5 독립 검증 4회차 연속 불가"라고 썼다가 → **원문 확인 결과 26회차 2차 실행이
   이미 수행**(next_day 11/16 = 68.8% · main_call 2/4 · 미기록 8/12)했음을 확인.
   **"수행 불가"가 아니라 "분모 불변으로 갱신 불가"** 로 교체(§4-3·§6-5·원장 A48/A53 동시 정정).
   자체검증 ③ '기각/철회 대상이 실제로 살아 있는가'가 잡아낸 건이다.

**독립 재계산 불일치 0건**(matured 519 · Δ 0 · 소실 0 · 무결성 7파일 · flow 100% ·
T1 159/265 · 종목 31/52 · 숏 pred 42.0% · A47 29그룹/31행).

---

## 8. 이번 회차 운영 실측 (다음 회고가 참고할 것)

- `retro_label.py` 소요 **37분**(03:18:43~03:55). A46 가 해결됐어도 실행 시간은 안 줄었다.
- ★**터미널 MCP 의 전송 타임아웃은 `wait_ms` 와 무관하게 약 60초에 걸린다** — `wait_ms=90000`
  이나 `wait_job(wait_ms=120000)` 을 줘도 "Request timed out"이 나고 **잡 추적을 잃는다**
  (A50 이 기술한 그 사고의 원인이다). 이번 회차 대응: `Start-Process -PassThru` 로 **PID 를 쥔 채
  분리 실행**하고 로그 파일을 50초 단위로 폴링했다. 프로세스는 PID 로 추적되므로 **재시도로
  두 번째 인스턴스가 생기는 일이 원천 차단**된다. 이 방식을 다음 회차 표준으로 권한다.
- 실행 순서 준수: retro_label(03:55) → `retro_forward --push`(03:56) → `gen_scorecard`(03:59,
  49건 채점) → 분석 → 산출·회수(04:20). **06:20 아침 슬롯 침범 없음.**

---

## 9. 출처

- 08-21 장중 시황(외국인·기관 순매도, 개인 방어, 코스피 6,900선 부근):
  Businesskorea "[개장 시황] 코스피, 장 초반 6,900선 안팎에서 등락"
- 2026년 제약·바이오 강세 동인(GLP-1, 특허만료, CDMO 증설, M&A) 및 삼성바이오로직스
  5공장·미국공장·신규 수주 변수: 돈뿌/thesmileinfo "2026 제약·바이오 주식 전망 대장주 Top5"
- 수치 전거는 전부 `retro_dataset_2026-08-24.json`(sha256 `73a560a58e807ec1`) ·
  `scorecard_2026-08-24.md` · `night_scorecard_2026-08-24.md`.
