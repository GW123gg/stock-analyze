# 회고리포트 2026-09-02 (34회차)

기동 17:35:57 · `retro_label` 17:36:30~18:03:31(약 27분) · push 18:05:20 · 데이터셋 `retro_dataset_2026-09-02.json`
사전등록 문면: `outbox\PREREG_2026-09-02.md` (작성 17:52 — retro_label 실행 중, **데이터 확인 전** 고정)
계산 스크립트: `outbox\scripts\retro34_*.py` 5종 (A52 계산 정의 첨부)

---

## ⓪ 원장 대조

**해결/진전**: A63 미이행이나 **회고가 우회에 성공**(지수 종가로 독립 재채점 + `scored_from` 재구성) · A50 미재발(단일 인스턴스) · A60 처방 4회차 연속 작동 · A49 미발동(17:30 슬롯 4회차 연속 정상) · A62 superseded 33%→**31%** 완만한 개선.
**미해결 유지**: A34(sector 소속부 라벨) · A41(3조건 **4회차 연속 진전 0**) · A47(**신규 오염 1건 재발**) · A54(night_track 미배선 3회차) · A55 · A57(재현 3회) · A59(**90/90** 분산 0) · A60(재현 2회) · A61(**이번 회차 최대 발견 — 아래 ①**) · A62 · A63.
**신규**: **A64** — `market_calls` 원장이 prediction 시대만 담아 §3.9 병기가 구조적으로 불성립(거짓 준수 위험).
**규칙 상태 변경**: **T3 정식 기각**(가지치기 의무 집행) · T1 반증 5회 → 가지치기 (a) 후보 정식 지목 · T1c 확증 6회 · T4 재현 +1 · B204 검정 불성립.

---

## ① 요약

### 표본
- 전체 **567행** / **matured 540** / maturing 24 / no_label 3. 고유종목 89 · 예측일 60 · prediction 382 / archive 185.
- ★**신규 만기 Δ = +4행(536→540) = 유효 독립관측 3.** 셀트리온 `08-01 h20` 과 `08-03 h20` 이 `entry_ref`·`settle_close` 동일이라 같은 정산창으로 접힌다(A47 신규 오염 1건). Δ 4행 전부 **[장투가능]·h=20·prediction·pick**: 셀트리온 ×2 · 알테오젠(KOSDAQ) · 삼성바이오로직스.
- 무결성: `latest.json` date=2026-09-02, roles 7파일 **전건 일치**(sha256·bytes·rowcount 3종 재계산, 불일치 0·결측 0).
- **라벨 지연 0 거래일** — `partial_asof_date` 최대 **2026-09-01** = 직전 영업일(17:30 슬롯 **4회차 연속** 실증).
- 스냅샷 편향(A35): asof 09-01 코스피 1d **+0.23%** · 5d **+1.38%** = 경미·낙관. ※미만기 수치는 이 문장과 함께만 인용하고, 33회차 `ret_partial` 과 직접 비교하지 마라.

### 사전등록 가설 목록 (§0 — 이 목록에 있는 것만 확증 판정)
H1 T1 픽 선별력 / H2 T1c h·지수 교락 / H3 T3 분배합류(**34회차 강화 규약**) / H4 T4 시간축 / H5 B204 profit_take / H6 A61 3방향 벤치마크 / H7 A59 vkospi 분산 · V1~V9 무결성·재현성.
**수행 검정 총수 N = 15** · Bonferroni(α=0.00333) **생존 2** · BH-FDR(q=0.05) **생존 3**.
※ p 값은 **양측 정본**, 순열검정은 **seed=7 · 20,000회**.

### 국면 pre_* 피처 — 커버리지와 분산 (A41 정본: 커버리지가 아니라 **분산**으로 판단)

| 피처 | 커버리지 | 분산 |
|---|---|---|
| `pre_regime_kind` | 137/540 = 25.4% | 공포 75 · 눌림목 43 · 과열 16 · **중립 3** |
| `pre_allow_market_up` | 137/540 = 25.4% | True 123 · **False 14(고유 3일)** |
| `pre_caution_score` | 137/540 = 25.4% | 고유값 13 · 15.0~90.0 · **>=60 고유 3일** |
| `pre_vkospi` | 90/540 = 16.7% | 고유값 14 · 55.31~87.79 (원값은 살아 있다) |
| `pre_vkospi_label` | 90/540 = 16.7% | ★**90/90 전부 '공포' — 분산 0** |

**A41 재개 3조건 전부 미충족 — 4회차 연속 진전 0.** 커버리지는 24.5→24.8→25.4% 로 늘지만 소수 클래스(중립 3행)와 고유일(3일)은 31회차부터 **완전히 고정**이다. 표본이 쌓여도 저절로 충족되지 않는다 — 새 국면이 와야 한다. **F1/F8 검증 대기 유지**(행 수 기준 예고 금지).

### 핵심 발견

**① ★ A61 — scorecard 가 보고하는 T+1 우위 +24%p 는 전부 벤치마크 선택의 산물이다.**
A63 미이행으로 드롭에 채점값이 없어, 회고가 KS11/KQ11 종가를 직접 받아 콜 52건을 밴드 규약(T+1 ±0.5% / T+5 ±1.2%)으로 **전건 재채점**했다. 재채점 정확성은 호스트의 **독립 산출물** `night_scorecard` 실현값과 대조해 검증했다 — for_date 08-20 **+5.89%**, 08-28 **-1.79%**, 08-31 **+0.46%**, 08-11 **+0.73%** **전건 일치**.

| 창 / 판 | 실측 적중 | **3방향 상한** | 격차 |
|---|---|---|---|
| **scorecard 기재값** | T+1 21/34 = **62%** | always-down 단독 **38%** | **+24%p** |
| scorecard창 T+1 원표본 | 12/20 = 60.0% | always-up 50.0% | +10.0%p |
| ★scorecard창 T+1 **중복접기** | **9/17 = 52.9%** | **always-up 52.9%** | **0.0%p** |
| 전체창 T+5 원표본 | 12/47 = 25.5% | always-down 66.0% | **-40.4%p** |
| 전체창 T+5 중복접기 | 9/36 = 25.0% | always-down 58.3% | -33.3%p |
| scorecard창 T+5 원표본 | 6/18 = 33.3% | always-up 55.6% | -22.2%p |
| scorecard창 T+5 중복접기 | 5/15 = 33.3% | always-up 60.0% | -26.7%p |

T+5 는 KOSPI·KOSDAQ **8개 셀 전부 열세**(-13.3 ~ -40.4%p). **반례(결론의 민감도 — 반드시 병기)**: T+1 은 **중복접기 여부가 결론을 가른다**(원표본 +10.0%p ↔ 접기 0.0%p). 따라서 "우위 0"은 접기를 적용했을 때의 값이며, 앞으로 이 축은 **두 판을 항상 나란히** 적는다.
부수 확인 — **`scored_from` 분리(A44) 시 T+1 이 크게 갈린다**: next_day **10/15 = 66.7%** vs main_call **10/36 = 27.8%**. 이 분해 없이 인용된 T+1 수치는 전부 두 모집단의 혼합값이다.

**② ★ T3(진입 전 분배합류) 정식 기각 — 강화 규약이 첫 적용에서 작동했다.**
33회차는 Δ TRUE 1행에 (a)가 발동해 기각이 막혔고, 그 자리에서 **34회차 강화 규약**((a) 성립 요건 = 누적 Δ TRUE **5행+** 또는 일내 혼합블록 **1개+ 신규 형성**)을 등재했다. 이번엔 그 규약을 데이터 확인 전 PREREG 에 고정한 뒤 실측: **Δ TRUE 0행**(Δ 4행 전부 `pre_foreign_sell_streak=0`, 비율 조건 미충족) · **Δ가 만든 신규 혼합블록 0개** → **(b) 발동 → 정식 기각**.
누적도 무신호: 전체 TRUE 78행 alpha **+1.15** vs FALSE 355행 **+2.76**(순열 p=**0.3431**) / prediction TRUE 41행 **+5.76** vs FALSE 245행 **+3.60** = **부호 반대**(p=0.3342).
★규약이 **양방향으로** 작동했다 — 33회차엔 기각을 막고, 34회차엔 기각을 집행했다. 목표이동 없이 두 방향이 모두 나온 것이 유효성의 증거다. **규칙 총량 영향 없음**(집행 중 게이트가 아닌 후보였으므로 엄밀히는 '규칙화 금지 확정').

**③ T1(픽 선별력) 반증 5회 — 종목내 대조가 결정적.**
Δ 4행 alpha **0/4 전부 음수**(-2.94 · -16.53 · -2.94 · -1.28, 평균 **-5.92**). 결정적인 것은 **종목 고정효과를 제거한 종목내 대조**다:

| Δ 종목 | Δ alpha | 같은 종목 기존 matured 평균 |
|---|---|---|
| 셀트리온 | **-2.94** | 34행 **+10.78** |
| 알테오젠 | **-16.53** | 18행 **+2.33** |
| 삼성바이오로직스 | **-1.28** | 31행 **+13.13** |

**3종목 전부 자기 자신의 과거 평균을 크게 밑돈다.** 종목 클러스터 prediction **31/53 = 58.5%, 양측 p=0.2717 — 6회차 연속 미통과**(33회차 32/53=60.4%에서 후퇴). → 승격 요건 미달, **§4(0) 가지치기 (a) 후보로 정식 지목**.
**반례**: 행 단위로는 prediction 픽 166/286 = 58.0%(p=0.0077)로 여전히 유의해 보인다 — 아래 §3.9 참조.

### §3.9 전체 vs prediction-only 병기 (의무) + §3.10 알파 분해

| 표본 | 픽 N | ret_h 평균 | **alpha 평균** | alpha>0 (외부기준 vs 50%) |
|---|---|---|---|---|
| 전체 matured | 433행(=60일, 종목 81) | **-2.09** | **+2.47** | 225/433 = 52.0% (p=0.4420) |
| **prediction-only** | 286행(=48일, 종목 53) | **-2.42** | **+3.91** | 166/286 = **58.0% (p=0.0077)** |
| archive-only | 147행(=12일, 종목 58) | -1.45 | **-0.35** | 59/147 = **40.1% (p=0.0206)** |

**§3.10 알파 분해**: 세 표본 모두 **`ret_h` < 0 인데 prediction 의 alpha 는 +3.91 로 양수** → 손실의 주범은 **시장 베타**다. 처방은 '픽 억제'가 아니라 **순노출 축소**(회고 반복 재현, [0.5]/F8-b와 일치).
★**서브샘플 분열은 유사반복 경보다**: prediction(+)과 archive(-)가 정반대인데, **같은 축을 종목 클러스터로 올리면 prediction 58.5%·p=0.2717 로 유의성이 사라진다.** BH-FDR 생존 3건에 '§3.9 픽 prediction'이 들어가지만 클러스터 판이 미통과이므로 자체검증 ④에 따라 **'판별력 없음'** 으로 기록한다. → 이 축에서 행 단위 N 으로 규칙을 만들지 마라.
※`hit`(45.3% / 46.2%)은 수익률 부호의 재라벨이므로 **같은 표본 기저율과 비교하지 않았다**(C절 08-04). 방향성 성과는 위 외부기준(코인플립 50%)으로만 검정했다.

### §3.12 maturing 조기경보 (한 줄)
미만기 **24행** 중 확정 게이트 위반(`pre_kospi_ret5d > +2%` 진입) 생존 픽 **5건** — 셀트리온 08-05(k5 +5.57) · 삼바 08-19(**+9.05**) · 삼바/POSCO홀딩스/한화에어로스페이스 08-27(+5.21). ※전원 미만기라 판정 불가.

### 다중비교 보정표 (N=15)

| 검정 | p(양측) | Bonferroni | BH-FDR |
|---|---|---|---|
| H4 T4 Spearman(순열) | <0.0001 | **생존** | **생존** |
| §3.1 IMM-REST 평균차(순열) | <0.0001 | **생존** | **생존** |
| §3.9 픽 alpha>0 prediction | 0.0077 | 기각 | **생존** |
| §3.9 픽 alpha>0 archive | 0.0206 | 기각 | 기각 |
| H1 T1 클러스터 archive | 0.0247 | 기각 | 기각 |
| §3.9 숏 alpha>0 archive | 0.0336 | 기각 | 기각 |
| H5 B204 평균차 전체(순열) | 0.0605 | 기각 | 기각 |
| H5 B204 평균차 prediction | 0.1099 | 기각 | 기각 |
| §3.9 숏 alpha>0 prediction | 0.2284 | 기각 | 기각 |
| H1 T1 클러스터 prediction | 0.2717 | 기각 | 기각 |
| H3 T3 평균차 prediction(순열) | 0.3342 | 기각 | 기각 |
| H3 T3 평균차 전체(순열) | 0.3431 | 기각 | 기각 |
| §3.9 픽 alpha>0 전체 | 0.4420 | 기각 | 기각 |
| H1 T1 클러스터 전체 | 0.6570 | 기각 | 기각 |
| §3.9 숏 alpha>0 전체 | 0.8468 | 기각 | 기각 |

### 규칙 총량
**18/30 불변 — 10회차 연속.** 신규 판단 규칙 발행 **0건**(SKILL 동결 준수), 기각 1건(T3 — 후보였으므로 총량 무관). 30 미만이므로 one-in-one-out 미발동.

---

## ② 차익실현 패턴 (§3.1 / §3.6)

| 군 | N | alpha 평균 | post_peak_drawdown | peak_day_vol_ratio |
|---|---|---|---|---|
| IMM (`days_to_peak` <= 1) | 152행(=43일) | **-5.78** | -12.25% | 1.03 |
| REST (> 1) | 388행(=59일) | **+4.57** | -7.52% | 1.37 |

평균차 **-10.35%p**, 순열 p<0.0001(seed=7·20,000회) — **Bonferroni 생존**. 즉시 고점형은 성과가 명백히 나쁘다.
★**그러나 규칙화 불가**: `days_to_peak` 는 **사후 라벨**이라 진입 아침에 계산할 수 없다(C절 08-04 '그날 아침에 계산 가능한가'). 서술로만 쓴다. 진입 시점 피처는 여전히 무판별이므로 **B9(개별 과열피처 단독 진입차단 금지) 13회 연속 유지**.
**반례**: 고점일 거래량 비율이 IMM 1.03 < REST 1.37 로 **'고점에 매물 집중' 서사와 반대 방향**이다 — 즉시 고점형은 거래량 클라이맥스 없이 조용히 무너진다. '거래량=차익실현' 서사를 IMM 에 적용하지 마라.

**§3.6 분배 공시 교차검증**: `profit_take_flag` TRUE 98행 `dist_disc_count` 평균 **6.21** vs FALSE 442행 **4.94** — 방향은 가설과 일치하나 두 컬럼 모두 **사후 라벨**이라 진입 규칙 근거로 쓸 수 없다(보유관리 신호 한정).

**§3.13 손절/익절 반사실** (정본 컬럼 `ret_if_stop8_tp12_cap_pct`)

| 표본 | N | `ret_h_pct` | `ret_if_stop8_pct` | `ret_if_stop8_tp12_cap_pct` | 손절이 건짐 / 승자 상실 |
|---|---|---|---|---|---|
| 픽 전체 | 433 | -2.09 | -2.30 | **-2.00** | 97 / **86** |
| 픽 prediction | 286 | -2.42 | -1.66 | **-1.47** | 61 / **40** |

**양면 평가**: 이득이 손실을 근소하게 앞서지만(전체 97:86), 익절 쪽 근사는 낙관 편향이 있고(C절 07-19) 15회차 발행 철회 이력이 있다 → **규칙 발행 금지, 후보 유지**.

---

## ③ 태그 / 타이밍 / 확신도 (§3.2 / §3.3)

| 태그 | N | alpha 평균 | `days_to_peak` 중앙 |
|---|---|---|---|
| [단기스윙] | 226 | +0.29 | 3.0 |
| [장투가능] | 157 | **+6.88** | 6.0 |
| 숏 | 107 | -1.61 | 3.0 |
| (무태그) | 35 | -1.78 | 2.0 |
| [장전선취매] | 15 | -1.01 | 1.0 |

| 타이밍 | N | alpha 평균 | `days_to_peak` 중앙 |
|---|---|---|---|
| 단기 | 243 | +1.00 | 3.0 |
| (없음) | 202 | -1.15 | 3.0 |
| 중기 | 77 | **+11.46** | 8.0 |
| 임박 | 18 | +0.06 | 1.0 |

★**이 표를 그대로 읽지 마라 — T1c(h 교락)의 교과서적 사례다.** [장투가능]·중기의 우위는 대부분 **h=20 창에서 소속지수가 더 깊이 빠진 것**이며, 이번 Δ 4행이 정확히 그 조합([장투가능]·h=20)인데 **alpha 는 4행 전부 음수**였다. 태그·타이밍 수치를 근거로 **비중 조정을 제안하지 않는다**(§2 규약, 6회 기각 반영).

**§3.3 확신도 — 동결 유지.** `conviction > 0.65` 만기 표본 **0건**(F5 상한 정책) → 판정 유보. `>=0.50` 이분축: <0.50 192행 alpha **+1.90** vs >=0.50 163행 **+4.63** — 방향은 정상(확신 높을수록 성과 좋음)이나 Δ 유입이 이 축을 바꾸지 않았고 승격 요건 미달 → **관찰 유지**.

---

## ④ 시장국면 의존성 (§3.5 / §3.14 / §3.16)

**A41 3조건 전부 미충족 → F1/F8 검증 대기 유지**(위 ① 표 참조, 4회차 연속 진전 0).
**A59 재확인·악화**: `pre_vkospi_label` **90/90 전부 '공포'**(84→86→90). 원값 `pre_vkospi` 는 55.31~87.79 로 분산이 살아 있으므로 **문제는 값이 아니라 라벨 임계**다. 분산 0 → **검정 불성립**(기각 아님).
**F1 준수 확인**: `pre_allow_market_up=false` 4일(07-29·07-30·08-19·08-27) 중 **KOSPI up 콜 0건**.

**§3.16 시총·거래소 분해** (버킷 분류 전용 — 수익률 크기 회귀 금지)

| 축 | N(고유종목) | alpha 평균 | alpha>0 |
|---|---|---|---|
| 메가(10조+) | 424(46) | **+2.48** | 230/424 = 54.2% |
| 대형(1조+) | 90(24) | -1.08 | 37/90 = 41.1% |
| 중형(3천억+) | 17(11) | **-5.19** | 4/17 = 23.5% |
| 소형 | 9(5) | +3.22 | 6/9 = 66.7% |
| KOSPI | 416(53) | +2.21 | 222/416 = 53.4% |
| KOSDAQ | 124(33) | -0.19 | 55/124 = 44.4% |

메가캡 편중은 유지되나 **31회차에서 이미 '규칙화 금지 확정'(H2)** 된 축이다 — 재발행하지 않는다.

**§3.4 선반영**: 미반영 105행 alpha **+3.85** · 부분 174행 **+4.28** vs **강함 8행 -2.85**(N=8, 방향만 참고). 선반영 게이트의 방향과 일치하나 표본이 얇다.

---

## ⑤ 숏 익절 경로 (§3.11 / §3.8)

**★숏 Δ = 0행 — 8회차 연속 표본 불변. §3 계상 규칙에 따라 이 축은 재현·반증·기각 계상을 하지 않았다.** 아래는 참고 수치(전 회차와 동일).

| 표본 | N | ret_h 평균 | alpha 평균 | **초과하락(alpha<0) vs 코인플립 50%** |
|---|---|---|---|---|
| 전체 | 107행(=49일, 종목 41) | -3.80 | -1.61 | 55/107 = 51.4% (p=0.8468) |
| prediction-only | 69행(=39일, 종목 29) | -4.08 | **+0.02** | 29/69 = **42.0%** (p=0.2284) |

경로: `days_to_trough` 평균 **3.1일** · `post_trough_rebound_pct` 평균 **+7.84%** — 저점 도달 후 되돌림으로 수익이 새는 구조가 유지된다.
반사실(A42, 종가 기준·-9% 체결 가정 금지): `short_cover_d4_ret_pct` 평균 **-3.47** / `short_stop9_ret_pct` 평균 **-13.81**(도달 48행). D+4 커버가 만기보유보다 유리한 행 **43/107**. → 둘 다 만기보유(-3.80)를 명확히 개선하지 못한다. **B(11) 기각 유지.**
**§3.8 국면 대칭 — 승격/기각 명시**: 강세추격(k5>+2%) 29행(=10일) 초과하락 59% / 약세 54행(=27일) 52% / 중립 24행(=12일) **42%·alpha +4.01(숏 손실)**. 방향은 '강세일수록 숏이 낫다'이나 **Δ=0 이라 이번 회차 계상 없음** → **규칙화 금지 유지**(승격도 기각도 아님, 표본 미유입).

---

## ⑥ 다음에 고칠 점

1. **★A61 — `scorecard.md` 의 `벤치마크(always-down) NN%` 단독 표기를 3방향 전부 + 최댓값 표기로 교체하라.** 현행 표기는 하락장에서만 정직하고, 상승장에서는 성적표가 아니라 응원가가 된다. 이번 회차에 T+1 "우위 +24%p"가 정직한 벤치마크에서는 **0.0%p** 였다.
2. **★A64(신규) — `market_calls` 드롭에 '이 원장은 prediction 시대 전용' 마커를 넣어라.** 마커가 없으면 회고가 매 회차 같은 수치를 두 번 적고 §3.9 를 준수했다고 기록한다(거짓 준수). 소급 적재가 가능하면 그쪽이 낫다.
3. **★A60 — SKILL 본문 0번(다)에 `Get-CimInstance` 명령 원문을 박아라(최우선, 재현 2회).** 이번에도 첫 `run_command` 가 전송 타임아웃을 냈고 `list_jobs` 는 비어 있었으나 프로세스는 살아 있었다. **지시문대로만 하면 이중 실행이 성립하는 상태가 2회차 연속**이다 — 두 번 다 회고가 원장을 기억해서 막았을 뿐이며, 이는 방어가 아니라 운이다.
4. **A47 — 신규 오염이 4회차 만에 재발했다**(초과 31→32행). 셀트리온 08-01/08-03 이 같은 정산창이다. `label_window_id` 적재 요청 유지. 이번처럼 Δ가 4행뿐인 회차에는 중복 1건이 독립 관측을 25% 깎는다.
5. **A54 — `retro_forward --push` 직전 `night_track` 1회 실행 배선(3회차 연속 미이행).** 이번 드롭도 성적표(06:21)가 라벨(18:05)보다 12시간 앞선 채 굳었다. 회고가 재채점 가능함을 A63 우회로 실증했으므로 기술적 장애는 없다.
6. **A59 — `pre_vkospi_label` 임계 재보정(4회차 연속).** 90/90 전부 '공포'라 F1 이 상시 발동한다. 원값 분산은 살아 있으므로 임계만 고치면 된다.
7. **T1 가지치기 착수 예고** — 반증 5회·클러스터 6회차 연속 미통과·종목내 대조 반전. 다음 회차 PREREG 에 **T1 기각 요건을 수치로 사전 고정**하고(강화 규약 준용: Δ TRUE 5행+ 또는 혼합블록 1개+), 미달이면 기각한다. T3 처럼 규약을 먼저 박아 두어야 목표이동이 안 생긴다.
8. **자체검증 정정 3건** (§4-(2.5) 6점검 수행) — 아래.

### 자체검증 (§4-(2.5) 6점검) — **정정 3건**

- **① 열 이름 = 실제 계산값인가** → **정정 1건.** 초안이 V1 무음 재라벨을 **"값 변경 102건"** 으로 적었다. 재검산 결과 **직전 `label_status` 별로 분리하지 않은 오류**였다 — 직전 **matured 536행의 변경은 0건**(동결 정상)이고, 102건은 전부 maturing 22행(96건)·no_label 6행(6건)의 **정상 라벨 진행**이었다. → **"무음 재라벨 0건"** 으로 정정. 분리하지 않았다면 무결성 사고로 오보할 뻔했다.
- **② 재현 카운터가 §3 계상 규칙을 지켰는가** → 통과. Δ +4 회차이므로 계상 허용. 숏(Δ 0)·확신도(Δ 0)는 **동결**했고, Δ 단일 종목 중복분은 유효 독립관측 3으로 낮춰 병기했다.
- **③ 기각 대상이 실제로 살아 있는가** → **정정 1건.** 초안이 T3 를 **"기각"** 으로 적었으나, T3 는 **집행 중 게이트가 아니라 후보**였다 → C절 08-04 구분에 따라 **'규칙화 금지 확정'** 으로 표현을 교체하고 **규칙 총량 영향 0** 을 명시했다(허수아비 기각 방지).
- **④ 부등호·부호가 표와 일치하는가** → **정정 1건.** 초안이 §3.9 픽 prediction 58.0%(p=0.0077, BH 생존)를 **'선별력 확인'** 으로 적었다. 그러나 **같은 축의 종목 클러스터 판은 58.5%·p=0.2717 로 미통과**다 — 행 단위와 클러스터 단위가 갈리므로 규약대로 **'판별력 없음'** 으로 교체했다. B204 도 전체표본 방향(반대)과 Δ 방향이 갈리므로 '검정 불성립'으로 적었다.
- **⑤ N·'전부 X' 단정을 재집계했는가** → 통과. 핵심 수치는 **행수 / 고유일 / 고유종목 3종 병기**했고 `_src_kind` 별로 분해했다. '전부 공포' 단정은 90/90 로 재집계했다.
- **⑥ 적중률 옆에 무정보 기저율을 적었는가** → 통과. `hit` 은 수익률 부호의 재라벨이므로 **같은 표본 기저율과 비교하지 않았고**(C절 08-04), 방향성은 외부기준(코인플립 50%)으로만 검정했다. market_call 은 **always-up/flat/down 3방향을 같은 창·같은 밴드로 전부 계산해 최댓값**을 상한으로 썼다(A61).

### 아침 지시서(`cowork_instructions.md`) 청소 후보 — PART A 별첨 참조
