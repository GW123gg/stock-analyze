# 회고 리포트 — 2026-09-03 (35회차)

실행: 17:36 KST 기동 · `retro_label` 17:38:13~18:27:16(**49분 3초**) · push 18:27:29 · gen_scorecard 18:28:34.
사전등록: `outbox/PREREG_2026-09-03.md`(작성 17:50, **데이터 확인 전** 고정).
계산 스크립트: `outbox/scripts/retro35_*.py` (A52 재현성 규약 — 모든 수치의 정의가 코드에 있다).

**★라벨 지연 = 0 거래일.** `partial_asof_date` 최대 **2026-09-02** = 직전 영업일. 17:30 슬롯 **5회차 연속** 실증.

---

## ⓪ 원장 대조 (해결/미해결 한 줄)

- **해결 확인**: 없음(호스트 처리 신규 반영 없음).
- **미해결 재확인 4건**: **A63**(market_calls 에 `scored_from`·채점결과 부재 — 2회차 연속 실측) · **A64**(market_calls 가 prediction 시대만 담아 §3.9 가 구조적 불성립 — 2회차 연속) · **A62**(night 콜 superseded 5/17=29%, 구본 정오 미기록) · **A41**(F1/F8 재개 3조건 전부 미충족 — '검증 대기' 유지).
- **신규 등재 3건**: **A65**(드롭의 `scorecard.md` 가 라벨보다 12시간 낡다) · **A66**(가지치기 기각 요건이 단일 축이라 종목내 대조 전패가 반영되지 않는다) · ★★**A67**(34회차 PART A 가 아침 루프에 전달되지 않아 폐루프가 2일 끊겼다).
- **운영 재발 1건**: **A50/A60 4회차 연속** — 다만 A58 처방으로 조기 검출·정리했고 **오염 0**(V1 로 실증).

> ★★**A67 — 이 회차 최대 운영 사고.** §4(2) 판정 규약 (a)(헤더의 회수 시각·회차) 직접 관측 결과,
> 덮어쓰기 직전의 `WORK\retro_feedback.md`(13,897B·79행)는 헤더가 **"33회차(2026-09-01 회고)가 2026-09-01 18:22 에 직접 저장"**
> 이고 본문 제목도 **`# PART A 추가지시 — 33회차`** 였다. 즉 **2026-09-02·09-03 아침 분석은 33회차 지시를 읽었고,
> 34회차 지시 4건은 발행됐으나 한 번도 라이브가 아니었다.** 정황: `outbox\scripts` 에 `feedback32.py`·`feedback33.py` 는
> 있는데 **`feedback34.py` 가 없다**(34회차가 SKILL 6번 직접 저장 단계를 건너뛴 것으로 보인다).
> `RETRO_DONE.flag` 도 부재라 (b) 축만 보면 '회수됨'으로 **오독**될 수 있었다 — **플래그 부재는 회수 완료가 아니라 미생성일 수 있다.**
> 이번 회차 PART A 는 34회차 유효분 2건(A61 벤치마크 · T3 기각 반영)을 **1·2번으로 재발행**했다.

---

## ① 요약

### 표본

| 구분 | 이번(35회차) | 직전(34회차) |
|---|---|---|
| matured | **542** | 540 |
| 신규 만기 Δ | **+2행 = 유효 독립관측 1** | +4행 = 유효 독립관측 3 |
| 미만기 | maturing 25 · no_label 3 | maturing 24 · no_label 3 |
| 고유종목 / 예측일 (matured) | 86 / 60 | 86 / 60 |
| _src_kind (전체 행) | prediction 385 / archive 185 | 382 / 185 |
| 소실(matured→비matured) | **0건** | 0건 |

**Δ 2행의 정체**: 둘 다 `pred_date=2026-08-04`·`h=20`·`tag=장투가능`이고 소속지수 수익이 **단일값 +4.88** 이다.
C절 08-31 규약대로 **유효 독립관측 1**로 센다(행 수로 세지 않는다).

| 종목 | ret_h | 소속지수(KOSPI) | alpha | 같은 종목 기존 평균 |
|---|---|---|---|---|
| 셀트리온 | +2.25 | +4.88 | **-2.63** | +10.39 (35행) |
| 삼성바이오로직스 | +5.48 | +4.88 | **+0.60** | +12.68 (32행) |

### 무결성

`latest.json` date=**2026-09-03**, roles **7파일 전건 일치**(sha256·bytes·rowcount 3종 재계산 — 불일치 0·결측 0).
**V1 무음 재라벨 0건**: 직전 matured **540행 × 17열** 전수 대조에서 값 변경 **0**.
(전체 공통행 기준으로는 106건이 변했지만 **전부 maturing/no_label 행**이며 라벨 진행의 정상 결과다. 이 분리를 안 하면 오판한다.)

### ★스냅샷 편향 — 부호가 뒤집혔다 (A35, 미만기 수치와 같은 문장에서만 인용)

`partial_asof_date=2026-09-02` 의 코스피는 **1d -3.99% · 5d -3.61%** = **비관 편향**이다.
34회차(asof 09-01)는 **+0.23% / +1.38% 낙관**이었으므로 **두 회차 `ret_partial` 직접 비교는 금지**한다.
원인(웹 확인): 미·이란 무력충돌 + 국제유가·금리 동반 급등에 외국인 **-1조 9,093억**·기관 **-2조 4,330억**(합 **4조 3,423억** — 기사 표기 '약 4조 3,500억')순매도, 코스피 **6,562.72(-273.08p, -3.99%)** 마감.

### 국면 게이트 커버리지·분산 (A41 — 행 수가 아니라 분산 기준)

`pre_regime_kind` 커버리지 25.6%(139/542) — 공포 75행(13일) / 눌림목 45행(8일) / 과열 16행(3일) / **중립 3행(1일)**.
`pre_allow_market_up` True 125 / **False 14(고유 3일)**. `pre_caution_score>=60` 고유 **3일**.
→ **3조건 전부 미충족**(소수 클래스 15행+ · false 5일+ · caution>=60 5일+) → **F1/F8 검증 대기 유지**.
`pre_vkospi_label` 은 92행 **전부 '공포'** 단일값 — **A59 미해결**(F1 판별력 0·상시 발동).

### 조기경보 (§3.12)

미만기 25행(픽 21·숏 4) 중 **확정 게이트 위반(k5>+2 진입)으로 살아있는 픽 = 5건** —
셀트리온 08-05(k5 5.57) · 삼바 08-19(9.05) · 삼바/POSCO홀딩스/한화에어로스페이스 08-27(5.21).
※이들의 `ret_partial` 은 위 **비관 편향 스냅샷** 값이므로 크기로 판단하지 마라.

### 사전등록·다중비교

사전등록 **H1~H9 + V1~V9**(문면 `PREREG_2026-09-03.md`). 수행 검정 총수 **N=22**.
**Bonferroni×22(α=0.00227) 생존 5 · BH-FDR(q=0.05) 생존 7.**

| 생존 검정 | p(양측) | Bonf | BH |
|---|---|---|---|
| T1b 바이오 2종목 선별 49/62 | <0.0001 | 생존 | 생존 |
| T1b 바이오×h=20 선별 35/43 | <0.0001 | 생존 | 생존 |
| T4 Spearman rho=0.6256 (N=79, 순열) | <0.0001 | 생존 | 생존 |
| IMM−REST 평균차 전체픽 −10.24%p (순열) | <0.0001 | 생존 | 생존 |
| IMM−REST 평균차 pred픽 −10.51%p (순열) | <0.0001 | 생존 | 생존 |
| T2 비과열 선별 145/243 | 0.0031 | 기각 | 생존 |
| §3.9 픽 alpha>0 prediction 167/288 | 0.0079 | 기각 | 생존 |

**규칙 총량 18/30 불변 — 11회차 연속. 신규 판단 규칙 발행 0건 · 기각 0건.**

---

## ② 핵심 발견 3가지

### 발견 1 — ★★T1 가지치기 정면 판정에서 '보류'가 나왔다. 규약을 고치지 않았다.

34회차가 T1(픽 종목 선별력)을 **§4(0) 가지치기 (a) 후보로 정식 지목**했고 반증이 누적 5회였다.
데이터를 보기 전 `PREREG_2026-09-03.md` 에 판정 규약을 수치로 고정했다:

> **(a)** Δ pred 픽 alpha 양수 비율 **>= 50% AND 누적 방향 유지** → 반증 불계상 · 관찰 유지(가지치기 보류)
> **(b)** **< 50%**(0/N 포함) → 반증 +1(누적 6회) · **가지치기 (a) 정식 발동 → '선별력 무신호 확정'으로 강등**
> **(c)** Δ pred 픽 0행 → 계상 불가 · 동결

실측 Δ 2행 = 셀트리온 **-2.63** · 삼바 **+0.60** → **1/2 = 정확히 50%**.
두 번째 조건('누적 방향 유지')도 성립한다 — prediction 픽 누적 alpha>0 **58.0% > 50%** 로 가설 방향이 유지된다.
→ **(a) 성립 → 강등 보류·관찰 유지·반증 불계상(누적 5회 유지).**

★**계산 스크립트의 라벨과 다르다는 점을 명시한다.** `retro35_analysis.py` 는 Δ **평균** alpha(**-1.01**)를 보고
`부호 음수(반증)` 이라고 출력한다. 그러나 사전등록 규약의 판정 축은 **평균이 아니라 양수 비율**이다.
평균은 -2.63 하나에 끌려가는 값이고(N=2), 규약을 사후에 평균 기준으로 바꾸는 것은 C절 09-01 위반이다.
**규약대로 비율 축을 집행했고, 스크립트 라벨은 채택하지 않았다.**

33회차가 T3 를 기각하려다 규약에 막혔던 사건의 **거울상**이다. 그때와 마찬가지로,
C절 09-01("어긋나면 규약을 바꾸지 말고 결과를 받아들여라")대로 **사후 규약 변경 없이 (a)를 집행했다**.

**누적 수치(§3.9 병기, §3.10 알파 병기)**

| 표본 | 행 단위 alpha>0 | p(양측) | 종목 클러스터 | p(양측) | 고유일 |
|---|---|---|---|---|---|
| 전체 matured 픽 | 226/435 = **52.0%** | 0.4430 | 38/81 = 46.9% | 0.6570 | 60 |
| prediction 픽 | 167/288 = **58.0%** | **0.0079** | 31/53 = **58.5%** | 0.2717 | 48 |
| archive 픽 | 59/147 = **40.1%** | 0.0206 | 20/58 = 34.5% | 0.0247 | 12 |

행 단위는 유의한데 **종목 클러스터로 올리면 5회차 연속 유의성이 사라진다**(31/53, p=0.2717 — 34회차와 동일).
§3.9 두 시대가 정반대(pred 58.0% vs archive 40.1%)인 것도 34회차와 같다 — **유사반복 경보**로만 읽고 규칙화하지 않는다.

**반례(가설에 불리한 쪽)**: 종목내 대조는 **0/2 전패**다. 셀트리온 Δ **-2.63** vs 같은 종목 기존 **35행 평균 +10.39**,
삼바 Δ **+0.60** vs 기존 **32행 평균 +12.68**. 34회차(0/3)와 합쳐 **누적 0/5** — 강건성 4종 중
**종목 고정효과를 제거한 판이 5회 연속 전패**다. 부호 축으로는 절반이 살아 있고 크기·종목내 축으로는 전패인 이 비대칭을
**신규 A66** 으로 등재하고, **36회차 판정 규약을 지금 고정**했다(아래 ⑥).

**반례(가설에 유리한 쪽)**: 삼바 Δ alpha +0.60 은 양수이고, prediction 행 단위 58.0%(p=0.0079)는
**BH-FDR 생존**이다. "무신호"라고 단정할 근거도 아직 없다.

---

### 발견 2 — ★T1 을 떠받쳐 온 '바이오 2종목 × h=20' 셀이 처음으로 후퇴했다

31회차가 특정한 대로, T1 의 유의성은 **셀트리온·삼성바이오로직스 2종목 × h=20** 단일 셀이 만든다.
그 셀의 회차별 추이(정본 정의 = 티커 068270·207940 2종목):

| 회차 | 바이오×h20 선별 | alpha |
|---|---|---|
| 31 (08-28) | 34/35 = **97.1%** | +22.55 |
| 33 (09-01) | 34/38 = **89.5%** | +19.30 |
| 34 (09-02) | 34/41 = **82.9%** | +17.71 |
| **35 (09-03)** | **35/43 = 81.4%** | **+16.84** |

**4개 관측 시점에 걸쳐 3회 연속 하락**(32회차는 Δ 비바이오·바이오 구성이 달라 계열에서 빠져 있다)이고,
이번 Δ 2행 중 셀트리온이 음수로 들어와 처음으로 분자보다 분모가 빨리 늘었다.
같은 드롭에서 표본의 58%를 차지하는 **비바이오×h=5 는 86/168 = 51.2%(p=0.8170) 코인플립**이다.

**(아래 표는 전부 `_src_kind='prediction'` 픽 288행 기준 — §3.9 서브샘플 라벨)**

| 셀 | N | 고유종목 | 선별 | p(양측) | alpha | 소속지수 |
|---|---|---|---|---|---|---|
| 바이오×h20 | 43 | **2** | 81.4% | <0.0001 | **+16.84** | -9.20 |
| 바이오×h5 | 19 | 2 | 73.7% | 0.0636 | +4.61 | -2.40 |
| 비바이오×h20 | 44 | 19 | 61.4% | 0.1742 | +5.42 | **-16.73** |
| 비바이오×h5 | 168 | 48 | **51.2%** | 0.8170 | +0.35 | -3.68 |
| 비바이오×h1 | 14 | 7 | 35.7% | 0.4240 | +0.60 | -0.45 |

**T1b(바이오 제외 판) — 카운터 동결 · 확증 실패 6회차 유지(7회차로 올리지 않는다) · 표본 완전 불변**:
비바이오 **118/226 = 52.2%(p=0.5495)** · alpha +1.35 로 **34회차와 소수점까지 동일**하다(Δ 비바이오 **0행**).
사전등록 H2 규약이 "Δ 비바이오 0행이면 표본 불변 → 불계상"이므로 **재현·반증 어느 쪽으로도 계상하지 않았고, '연속 실패' 카운터도 올리지 않았다**
(자체검증 정정 — 초안은 '7회차 연속'이라고 적어 동결 규약을 위반했다).

**단일 관측이므로 규칙화하지 않는다**(§0 절대규칙). 이 하락이 2회 더 이어지면 T1 은 부호 축으로도 무너진다 —
**36회차 사전등록 후보**로 등재했다.

**반례**: 직전 스텝의 하락 폭은 **-1.5%p** 로 작다(스텝별로는 -7.6 / -6.6 / **-1.5**%p 라 감속 중이며, 32회차는 계열에서 빠져 있다).
셀 자체는 여전히 **p<0.0001 · Bonferroni 생존**이다.
또 표본이 늘면 극단 비율이 평균으로 수렴하는 것은 자연스러운 현상이라, 이 추세를 '신호 소멸'로 읽는 것은 아직 과잉해석이다.

---

### 발견 3 — ★§3.15 신규: T+1 지수 콜의 `next_day` 경로가 처음으로 무정보 상한을 넘었다

A63(드롭에 채점 결과가 없다)이 2회차 연속 미이행이라, 이번에도 회고가 **FDR 지수 84봉을 직접 로드해 독립 재채점**했다.
★**`scored_from` 도 드롭에 없다** — 콜 원문의 `kospi.next_day` 서브객체 **존재 여부로 재구성**한 값이다(읽은 것이 아니다).
전 기간 53건 중 next_day 보유 **17건(32%)**, 그중 T+1 채점 가능한 것이 16건이다.
그 우회 덕에 A40-b 가 24회차부터 추적해 온 축에서 처음으로 **우위 방향**의 수치가 나왔다.

| 경로 | T+1 적중 | 3방향 무정보 상한(A61) | 격차 |
|---|---|---|---|
| KOSPI `next_day` | **10/16 = 62.5%** | 50.0% | **+12.5%p** |
| KOSPI `main_call` | 10/36 = 27.8% | 52.8% | **-25.0%p** |
| KOSDAQ `next_day` | 6/16 = 37.5% | 43.8% | -6.3%p |
| KOSDAQ `main_call` | 6/36 = 16.7% | 52.8% | -36.1%p |

두 경로의 격차는 KOSPI 기준 **34.7%p** 다. 혼합값(20/52 = 38.5%)은 어느 쪽도 대표하지 않는다 —
**혼용 집계 금지(A44) 준수**.

**전체 성적은 여전히 나쁘다**(맥락 병기): KOSPI T+1 20/52=38.5% vs 상한 46.2%(-7.7%p),
T+5 13/48=27.1% vs 상한 **66.7%**(-39.6%p). KOSDAQ T+1 23.1% vs 42.3%, T+5 33.3% vs 60.4%.
**`neutral` 콜은 4개 셀 전부 전패**다 — KOSPI **T+1 0/13 · T+5 0/13**, KOSDAQ **T+1 0/20 · T+5 0/20**(밴드 안에 들어온 적이 0회).

**A61 재현 — 중복접기 여부가 결론을 가른다**:

| 창 | 판 | 적중 | 3방향 상한 | 격차 |
|---|---|---|---|---|
| scorecard창 07-25~08-28 T+1 | 원표본 | 12/20 = 60.0% | always-up 50.0% | **+10.0%p** |
| scorecard창 07-25~08-28 T+1 | **중복접기** | 9/17 = 52.9% | always-up 52.9% | **+0.0%p** |

34회차와 **같은 크기의 반전이 재현**됐다. 원표본·중복접기 두 판 병기를 최소 인용 단위로 유지한다.

**신뢰도**: 관찰(견고하지 않음) — next_day 16건은 **30건 미달**이라 **규칙화·확률 조정 금지**.
**반례**: next_day 표본은 전부 2026-08-06 이후이고 그 창은 코스피가 크게 흔들린 구간이라,
경로 차이가 아니라 **국면 차이**일 가능성을 배제하지 못한다(main_call 36건은 대부분 그 이전 창이다).

---

## ③ 태그 / 타이밍 / 확신도

### ★h 혼합 경고 — 이 표는 단독 인용 금지 (T1c 확증 7회)

| 축 | 값 | N | alpha | days_to_peak 중앙 |
|---|---|---|---|---|
| tag | 단기스윙 | 226 | +0.29 | 3.0 |
| tag | 장투가능 | 159 | **+6.78** | 6.0 |
| tag | 숏 | 107 | -1.61 | 3.0 |
| tag | (무태그) | 35 | -1.78 | 2.0 |
| tag | 장전선취매 | 15 | -1.01 | 1.0 |
| timing | 단기 | 243 | +1.00 | 3.0 |
| timing | 중기 | 79 | **+11.14** | 7.0 |
| timing | 임박 | 18 | +0.06 | 1.0 |
| timing | 미기입 | 202 | -1.15 | 3.0 |

격차가 극적으로 보이지만 **h 교락**이다 — 중기·장투가능이 h=20 에 몰려 있고 h=20 구간 소속지수는 **-16.73**(비바이오)이다.
33·34회차가 **h=5 고정 시 격차 소멸**을 확인했고 이번에도 그 판정을 유지한다. **기각 유지·카운터 동결.**

### 확신도 (§3.3) — 동결 유지 · 재현 불계상 (h=5 표본 불변)

`conviction>0.65` 만기 **0건**(F5 조건부 상한 정책) → 누적 10건 도달 전 동결.
`>=0.50` 이분(h=5 고정, pred 픽 187행): conv>=0.50 **75행/33종목 alpha -0.12 · 선별 46.7%**
vs conv<0.50 **112행/42종목 +1.38 · 58.0%** = **부호가 가설과 반대**(32·33·34회차와 동일 수치 — 재계산을 재현으로 세지 않는다).

### 선반영(preprice) — 무신호 (h=5 고정, pred 픽, 표본 불변)

미반영 71행 **+1.85**·57.7% / 부분 109행 +0.32·51.4% / 강함 **7행** -2.80·42.9%(검정력 없음).

---

## ④ 시장국면 의존성

### T2 k5 과열 게이트 — 카운터 동결 (Δ 과열 부분군 0행)

Δ 2행의 `pre_kospi_ret5d` 는 **-7.38 단일값** → 과열 부분군이 늘지 않았으므로 계상 불가(C절 07-17).

| 군 | N | 고유종목 | **고유일** | ret_h | 소속지수 | alpha | 선별 | p |
|---|---|---|---|---|---|---|---|---|
| 과열 k5>+2 | 45 | 29 | **8** | -5.10 | -8.53 | **+3.43** | 22/45 = 48.9% | 1.0000 |
| 비과열 | 243 | 44 | 40 | -1.87 | -5.84 | **+3.96** | 145/243 = 59.7% | **0.0031** |

**alpha 격차 -0.53%p** — 32회차 -0.88 → 33회차 -0.74 → **35회차 -0.53**(34회차는 원장에 이 축 수치가 없어 계열에서 빠진다).
**3개 관측 시점에서 2회 연속 축소**다.
원수익 열위(-5.10 vs -1.87)는 대부분 **베타**(소속지수 -8.53 vs -5.84)이며, 관측 단위가 **8일**뿐이라
'경고·순노출 축소' 유지가 맞고 **기계적 강등 복귀 근거는 없다**.

### T3 진입 전 분배합류 — 34회차에 정식 기각 완료, 이번 회차 재판정 없음

Δ TRUE **0행**이라 재판정이 불가능하다. 자체검증 ③(허수아비 기각 금지)에 따라 **재기각하지 않았다**.
사후 확인만: prediction-only 방향이 여전히 **가설과 반대**(TRUE 41행 **+5.76** > FALSE 247행 **+3.57**, 순열 p=0.3235)로
기각 결론과 정합. 전체표본은 방향 유지(TRUE 78행 +1.15 < FALSE 357행 +2.73, p=0.3493)이나 §3.9 분열이므로 단독 인용 금지.

### F1 준수 실측 (§3.14)

`pre_allow_market_up=false` 인 날 **5일**(07-29 · 07-30 · 08-19 · 08-27 · **09-03**) 중 **KOSPI up 콜 0건** → **게이트 준수**.
★**모집단 주의**: 이 5일은 **dataset 전체 행**(570행, 미만기 포함) 기준이고, 위 A41 판정의 **3일**은 **matured 행만**(542행) 기준이다.
두 수치는 모순이 아니라 모집단이 다르다 — A41 재개 조건은 matured 기준이므로 **미충족 판정이 정본**이다.

### 시총·거래소 (§3.16) — ★h 혼합 표, 규칙화 금지 확정 유지

메가 426행/46종목 +2.46(54.2%) / 대형 90행/24종목 -1.08(41.1%) / 중형 17행/11종목 -5.19(23.5%) / 소형 9행/5종목 +3.22
/ KOSPI 418행 +2.19(53.3%) / KOSDAQ 124행 -0.19(44.4%).
31회차가 **h=5 고정으로 Bonferroni·BH 생존 0** 을 확인하고 탐색을 종결한 축이다 — 이 h 혼합 수치로 결론을 되살리지 마라.

---

## ⑤ 숏 · 익절 경로 · 즉시고점

### 숏 — Δ 0행, 8회차 연속 불계상

| 표본 | N | 고유종목 | ret_h | 소속지수 | alpha | **초과하락(alpha<0)** | p |
|---|---|---|---|---|---|---|---|
| 전체 숏 | 107 | 41 | -3.80 | -2.19 | -1.61 | 55/107 = 51.4% | 0.8468 |
| prediction 숏 | 69 | 29 | -4.08 | -4.10 | **+0.02** | **29/69 = 42.0%** | 0.2284 |
| archive 숏 | 38 | 21 | -3.29 | +1.27 | **-4.56** | 26/38 = 68.4% | 0.0336 |

무정보 기저는 **초과하락 코인플립 50%** 이고 prediction 은 **미달**이다.
방향적중 원값(pred 46/69=66.7%)은 **단독 인용 금지**(수익률 부호의 재라벨 = 동어반복, C절 08-04).
§3.9 분열이 픽과 **반대 방향**(픽은 pred 우수, 숏은 pred 열등)이므로 archive 혼입 수치 인용 금지.

### 손절/익절 반사실 (§3.13) — 후보 등재 유지 · 발행 금지

정본 컬럼은 **`ret_if_stop8_tp12_cap_pct`**(A15 캡판)다.

| 표본 | N | ret_h | stop8 | **stop8_tp12_cap** | 손절이 건진 행 | 승자를 잃은 행 |
|---|---|---|---|---|---|---|
| 픽 전체 | 435 | -2.07 | -2.28 | **-1.93** | 97 | 86 |
| 픽 prediction | 288 | -2.38 | -1.63 | **-1.38** | 61 | 40 |

양면 평가상 **건진 행이 잃은 행보다 많지만**(97 vs 86 / 61 vs 40) 평균 개선폭은 +0.14 ~ +1.00%p 수준이고
익절 근사가 낙관 편향(A15)이라 **발행 금지 유지**.

### 즉시고점 IMM (§3.1) — 서술 유지, 진입규칙 금지

정본 정의(`days_to_peak <= 2` 단독 · 픽 한정):

| 표본 | 군 | N | 고유종목 | 고유일 | alpha | 선별 | 평균차 | 순열 p |
|---|---|---|---|---|---|---|---|---|
| 전체 픽 | IMM | 182 | 62 | 51 | **-3.51** | 30.8% | **-10.24%p** | <0.0001 |
| 전체 픽 | REST | 253 | 55 | 56 | +6.74 | 67.2% | | |
| pred 픽 | IMM | 125 | 42 | 39 | **-2.07** | 36.8% | **-10.51%p** | <0.0001 |
| pred 픽 | REST | 163 | 37 | 44 | +8.44 | 74.2% | | |

**Bonferroni×22 생존**이지만 `days_to_peak` 는 **사후 라벨**이라 진입 규칙 조건으로 쓸 수 없다(C절 08-04
"이 조건을 그날 아침에 계산할 수 있는가"). **B9 개별종목 과열 단독 진입차단 금지 — 14회 연속 유지.**
Δ IMM **0행**(Δ 2행의 `days_to_peak` 가 둘 다 >2) → 부분군 동결.

### 전야(23시) 콜 (§3.15-b) — 비교 전용 · 합산 금지 · 30건 전 규칙화 금지

`night_scorecard` 갱신 2026-09-03 06:24 · 원장 **17건**(운용 12 · **superseded 5 = 29%**) · 채점 11 / 대기 1.

| 지수 | 방향 적중 | Brier | 무정보 Brier |
|---|---|---|---|
| KOSPI | **6/11 = 55%**(CI 28.0~78.7%) | **0.6374** | 0.6667 |
| KOSDAQ | **3/11 = 27%**(CI 9.7~56.6%) | **0.7060** | 0.6667(**열위 — 4회차 연속**) |

고유 for_date 12건 < 30건 → 규칙화 금지. superseded 구본의 정오가 기록되지 않아 생존편향 판정 불가(**A62 미해결**).

---

## ⑥ 다음에 고칠 점

1. **★A66 신규 — 가지치기 기각 요건이 단일 축이라 종목내 대조 전패가 반영되지 않는다.**
   승격 요건은 강건성 4종을 **판정 축**으로 요구하는데(§4(0) v9.7), 기각 요건에서 강건성 4종은 **병기 의무**였을 뿐 **판정 축이 아니었다** — 실제로 (a)/(b)를 가른 것은 이항 부호 비율 하나다.
   그 결과 이번 회차에 Δ 종목내 대조가 **0/2**(누적 **0/5**)인데도 (a)가 성립해 강등이 보류됐다.
   ★**36회차 T1 판정 규약을 지금 고정한다(PREREG 에 그대로 복사할 것)**:
   **(i)** Δ alpha 양수 비율 **<50%** → 즉시 (b) 강등 /
   **(ii)** 양수 비율 >=50% 이더라도 **종목내 대조가 3회차 연속 전패(누적 0/N, N>=6)** 면 → (b) 강등 /
   **(iii)** 둘 다 통과할 때만 관찰 유지.
2. **★A65 신규 — 드롭의 `scorecard.md` 가 라벨보다 12시간 낡다.**
   실측: 드롭 `scorecard_2026-09-03.md` = **5,316B · mtime 06:24:47**(= `WORK\scorecard.md` 그대로).
   17:30 회고가 지시 순서대로 돌린 `gen_scorecard.py` 산출은 **별도 파일**(`예측채점_리포트.md` · 69,474B · 18:28:34)이라
   드롭에 반영되지 않는다. push(18:27:29)가 gen_scorecard 보다 **먼저**인 순서 탓에 구조적으로 항상 그날 아침 판이다.
   **요청**: push 직전에 `WORK\scorecard.md` 생성기를 1회 돌리거나, SKILL 순서를 `retro_label → 채점 → push` 로 바꿔라.
   ※이번 회차는 scorecard 인용을 피하고 **독립 재채점**으로 우회했다.
3. **A63 2회차 연속 미이행** — 드롭 `market_calls` 에 `realized_*`·`hit_*`·`scored_from`·`band` 가 전부 없다.
   회고가 매 회차 지수 봉을 다시 로드해야 §3.15 를 계산할 수 있다. 이번 회차 발견 3 은 그 우회의 산물이다.
4. **A64 2회차 연속 실측** — market_calls 53건 **전부 prediction**, archive 12일 콜 **0/12**.
   §3.15 의 "전체 vs prediction-only"가 **항상 동일한 수치**로 찍힌다. 리포트에 그 사실을 명시해 '거짓 준수'를 차단했다.
5. **A59 미해결** — `pre_vkospi_label` 92행 전부 '공포' 단일값. F1 은 라벨이 아니라 `pre_vkospi_pct_rank` 수치를 직접 써야 한다.
6. **운영: A50/A60 4회차 연속 재발.** 첫 `run_command` 전송 타임아웃(약 60초, `wait_ms` 무관) →
   `list_jobs` "No active or recent jobs"(MCP 서버 재기동) → 재기동으로 **2 인스턴스**(PID 13336 @17:36:57 · 10412 @17:38:13).
   A58 처방(`Get-CimInstance Win32_Process -Filter "Name LIKE 'python%'"`)으로 17:47 검출·정리 → **오염 0**(V1 0건).
   ★순서 조정만으로는 4회차 연속 재발을 못 막았다 — **기동 자체를 멱등하게**(락파일 또는 `retro_label` 진입부 자기중복 검사) 만들 것.
   부수 실측: 터미널 MCP 잡에 **1800초 하드 캡**이 있어 장시간 감시 루프가 강제 종료된다(감시 루프만 종료·본 프로세스는 생존).
7. **★★A67 신규 — 폐루프가 2일 끊겼다. 회고 자신의 절차에 '전달 확인' 단계가 없다.**
   34회차 PART A 4건이 `retro_feedback.md` 로 전달되지 않았다(위 ⓪ 참조).
   **요청 ①(회고 자신)**: 회고 절차 마지막에 **`retro_feedback.md` 헤더의 회차 번호가 이번 회차인지 재확인**하는 단계를 넣어라
   — §4(3)이 플래그 생성 조건으로 '원장 헤더 회차 일치'를 요구하는 것과 같은 형태다.
   **요청 ②**: `RETRO_DONE.flag` **부재를 '회수됨'으로 읽지 마라.** 헤더 회차를 유일 전거로 삼고 (b) 축은 보조로 강등하라.
   **요청 ③(호스트)**: 아침 [0.5]가 `retro_feedback.md` 를 읽을 때 **헤더 회차와 오늘 날짜의 간격이 2일 이상이면 경고**를 띄워라(무음 단절 방지).
   ※파급 실측: T3 기각이 반영되지 않아 최근 10거래일 픽의 **23.1%(9/39 · 7/10일)** 가 여전히 기각된 규칙의 판단을 받고 있다
   (34회차 측정 시점 17.9% → **증가**).
   ★**사각지대의 기술적 원인도 특정했다**: 이번 회차에 직접 저장(18:46) 후 SKILL 6번대로 `--scan-back` 을 돌렸더니,
   **scan-back 이 회수 헤더를 `호스트가 2026-09-03 18:46 에 회수` 라는 1줄로 덮어써 회차 번호를 지웠다.**
   즉 §4(2) 판정 축 (a)는 **scan-back 을 돌리는 순간 소실**된다. 34회차가 scan-back 만 돌렸다면 회차 판정 자체가 불가능했을 것이고,
   남아 있던 헤더가 33회차 **직접 저장분**이었던 덕에 이번 발견이 가능했다.
   **요청 ④**: scan-back 회수 헤더에 **회차 번호와 원본 mtime** 을 포함시켜라. 이번엔 `retro35_hdrfix.py` 로 회차 주석을 복원했다.
   **종료 시점 상태: (a) 헤더 회차 = 35회차 ✓.**
(b) 축은 이번 회차에 한해 유효하다 — 플래그를 **18:47 에 생성한 사실을 이 회고가 직접 관측**했고 그 뒤 `--scan-back`(18:46:41 로그)이 회수·삭제했기 때문이다.
**생성 사실을 관측하지 못한 회차에는 (b) 축을 쓰지 마라**(부재 = 미생성일 수 있다 — 34회차가 그 경우다).
8. **자체검증 정정 8건**(1차 6점검 4건 + 2차 적대검증 4건) — 아래 ⑦.

---

## ⑥-b 사전등록 항목 잔여 보고 (H5 · H6 · V2 · V3 · V8 — 초안 누락분)

2차 자체검증에서 **사전등록했으나 리포트 본문에 없던 5개 항목**이 적발돼 여기에 채운다.

**H5 T4(시간축 전망) — 재현 +1 · 확증 보류 유지.**
Spearman(동순위 평균 랭크) **rho = 0.6256**, 유효쌍 **N = 79**, 순열 p<0.0001(seed=7 · 20,000회).
Δ 유효쌍 **2**·부호 양수 유지 → 재현 +1. 다만 **N 79 < 배증 기준 102** 이므로 **확증 보류 유지**.
Pearson 은 재현 근거로 쓰지 않는다(C절 08-27). Bonferroni×22 · BH-FDR **둘 다 생존**.

**H6 B204(profit_take_flag) — 검정 불성립 2회차 연속.**
Δ 2행이 **전부 TRUE** 라 Δ 내 대조군이 **0행**이다(34회차와 동일 사유) → 기각도 재현도 아니다.
누적은 방향 반대 유지(= 손절 근거로 쓰면 안 된다):

| 표본 | TRUE | FALSE | 순열 p |
|---|---|---|---|
| 전체 matured | 100행 alpha **+3.86** | 442행 **+1.15** | 0.0690 |
| prediction | 55행 **+5.53** | 302행 **+2.70** | 0.1360 |

**보유관리 신호로만 사용 · 진입회피·손절 트리거 금지** 유지.

**V2 사후 라벨 커버리지 단조성(A46 회귀 감시) — 전 항목 OK.**

| 컬럼 | 직전(09-02) | 이번(09-03) | 판정 |
|---|---|---|---|
| flow_foreign_eok / flow_inst_eok / flow_indiv_eok | 558/567 | **564/570** | OK |
| dist_disc_count | 531/567 | **532/570** | OK |
| peak_day_vol_ratio | 558/567 | **564/570** | OK |
| pre_foreign_5d_eok | 567/567 | **570/570** | OK |
| pre_short_change_10d | 553/567 | **556/570** | OK |

**감소(회귀) 0건** — 26회차 A46 형태의 비단조 회귀 재발 없음.

**V3 34회차 원장 수치 재현 — 5종 전건 일치(단 사전등록은 10종 이상을 요구했다).**

| 항목 | 재계산 | 원장 | |
|---|---|---|---|
| 34회차 matured | 540 | 540 | OK |
| §3.9 전체 픽 alpha>0 | 225/433 | 225/433 | OK |
| §3.9 prediction 픽 | 166/286 | 166/286 | OK |
| §3.9 archive 픽 | 59/147 | 59/147 | OK |
| T1c 부호 괴리 | 138/540 | 138/540 | OK |

★**미달 실토**: PREREG V3 는 **"10종 이상"** 을 요구했는데 스크립트가 **5종**만 돌렸다. 불일치는 0이지만
**요구 수량 미달**이므로 '전건 재현'이라는 표현을 '**5종 전건 일치(요구 10종 미달)**'로 정정한다. 36회차에 10종으로 확장한다.

**V8 A47 라벨창 중복 — 30그룹 · 초과 32행(5.9% of matured) · Δ 신규 중복 0.**
dedup 판 matured **N = 510**. 34회차(30그룹·초과 32행)와 **동일** — Δ 2행이 새 중복을 만들지 않았다.
`(ticker, kind, horizon, entry_ref, settle_close)` 기준, 초과행은 그룹 첫 행 제외로 센다(C절 08-24).

**H3(T1c 부호 괴리) 비교치 병기 의무 이행**: 전체 matured 부호 괴리 **139/542 = 25.6%**
vs 34회차 **138/540 = 25.6%** — 비율 불변.
※주의: ① 절의 `pre_regime_kind` 커버리지도 우연히 **139/542** 이지만 **전혀 다른 수치**다(혼동 금지).

**T3 관련 — 스크립트 출력과 리포트 판정이 다른 이유(명시).**
`retro35_analysis.py` 는 34회차 규약을 그대로 재실행해 `판정: (b) 요건 미달 -> 정식 기각` 을 출력한다.
그러나 T3 는 **34회차에 이미 기각됐고**, 이번 회차 Δ TRUE = 0행이라 재판정 자체가 불가능하다.
자체검증 ③(허수아비 기각 금지)에 따라 **스크립트의 이 출력은 채택하지 않았다** — 같은 기각을 두 번 세면 카운터가 부풀려진다.


---

## ⑦ 자체검증 (§4-(2.5) 6점검 + 2차 적대검증) — **정정 8건**

| # | 점검 | 결과 |
|---|---|---|
| ① | 열 이름 = 실제 계산값 | 통과 — alpha 는 `alpha_vs_own_idx_pct`(소속지수 차감), 소속지수는 `ret_h − alpha` 역산(전용 컬럼 없음), 클러스터는 종목별 alpha 평균>0 방식, 손절 정본은 `ret_if_stop8_tp12_cap_pct`. `alpha_vs_own_idx_pct != alpha_h_pct` 인 행 **124/542**(KOSDAQ) |
| ② | 재현 카운터 계상 규칙 | 통과 — 부분군별 Δ 유입 실측: T1 2행(계상) · T1b 비바이오 **0행**(동결) · T2 과열 **0행**(동결) · B204 FALSE **0행**(불성립) · 숏 **0행**(동결) · IMM **0행**(동결) · T4 유효쌍 2(계상) |
| ③ | 기각 대상이 살아 있는가 | 통과 — T3 는 34회차에 이미 기각 → **재기각 금지**(허수아비 방지). T1 은 집행 중 게이트가 아니므로 강등해도 총량 불변. B204 는 Δ 대조군 0행 → '검정 불성립' |
| ④ | 부등호·대소 방향 | **정정 1건** — `retro35_analysis.py` §3.12 가 위반 건수(5)를 출력한 뒤 **maturing 25행 전부를 나열**해 5건처럼 보이지 않았다. 재집계 결과 위반 픽은 **정확히 5건**(셀트리온 08-05 · 삼바 08-19 · 삼바/POSCO/한화에어로 08-27). 리포트에는 5건만 적었다 |
| ⑤ | N·'전부 X' 재집계 | **정정 1건** — `retro35_extra.py` 의 `isbio()` 가 이름 매칭으로 **5종목**을 바이오로 잡아 원장 회차(고유종목 **2**)와 비교 불가한 수치(비바이오 106/204)를 냈다. 정본 정의(티커 **068270·207940** 2종목)로 재계산해 **118/226 = 52.2%(p=0.5495)** 로 교체 — 이 값은 34회차와 소수점까지 일치한다. 3종 병기(행/고유종목/고유일) 전건 재집계 완료 |
| ⑥ | 무정보 기저율 병기 | **정정 1건** — `retro35_supp.py` 의 다중비교표에 **34회차 p 값이 하드코딩**돼 있었다(B204 pred 0.1099 vs 실측 0.1360, T3 pred 0.3342 vs 0.3235 등). 35회차 실측 p 로 **N=22 표를 전면 재작성**했다. 무정보 기저는 픽·숏 코인플립 50%, 시장콜 3방향 최댓값(A61), 전야 콜 Brier 0.6667 로 전건 병기 |

**추가 점검(규정 외, §4(2) 판정 규약)** — **정정 1건**: 초안 PART A 는 "34회차 지시 1~4 는 전부 유효하며 계속 집행한다"고
적었으나, `retro_feedback.md` 헤더를 직접 관측하니 **33회차 본문**이었다(A67). 34회차 지시는 **한 번도 라이브가 아니었으므로**
"계속 집행"이 아니라 **재발행(이월)** 이 맞다 — PART A 1·2번을 34회차 유효분의 재발행으로 **전면 교체**했다.
추론('발행했으니 라이브겠지')으로 판정할 뻔한 것을 직접 관측이 막았다(C절 v11.6 standby 오보 사고와 같은 형태).

**1차 정정 4건**(④ 조기경보 카운트 표기 · ⑤ T1b 바이오 정의 · ⑥ 다중비교표 p 값 · 추가 A67 라이브 여부 오판).

### 2차 적대검증(초안 전체를 원시 출력과 대조) — **추가 정정 4건**

| # | 정정 | 초안 | 정정 후 |
|---|---|---|---|
| 5 | 34회차 matured 고유종목 | **89** (드롭 메타 `n_unique_tickers`=570행 기준값을 잘못 인용) | **86** (09-02 드롭 matured 재계산). matured 는 소실 0이므로 86 초과가 애초에 불가능했다 |
| 6 | T1b 카운터 | "확증 실패 **7회차 연속**" | Δ 비바이오 **0행 = 표본 불변** → 사전등록 H2 규약대로 **6회차에서 동결**. 같은 문단에서 '계상하지 않았다'고 써 놓고 카운터를 올린 자기모순이었다 |
| 7 | `neutral` 콜 표기 | "T+1·T+5 모두 **0/13 · 0/20**" | KOSPI **0/13 · 0/13**, KOSDAQ **0/20 · 0/20** (지수별로 갈라 적어야 한다) |
| 8 | 사전등록 (a) 인용 | "양수 비율 >= 50%" 만 인용 | 원문은 "**>= 50% AND 누적 방향 유지**". 두 번째 조건도 성립함을 명시적으로 평가해 적었다(pred 누적 58.0% > 50%) |

부수 정정(수치 아님·서술 정밀화 4건): ① 순매도 합계 4조 3,500억 → **4조 3,423억**(기사 표기 병기) ② "4회차 연속 단조 하락"·"3회차 연속 축소" → **관측 시점 수와 하락 횟수를 구분**해 재서술(빠진 회차 명시) ③ `pre_allow_market_up=false` **3일 vs 5일**은 모집단(matured vs 전체 행) 차이임을 명시 ④ `scored_from` 은 드롭에서 읽은 값이 아니라 `next_day` 서브객체 **존재 여부로 재구성**한 값임을 명시.

★2차 검증에서 **사전등록했으나 리포트에 없던 5개 항목**(H5 T4 · H6 B204 · V2 · V3 · V8)도 적발돼 **⑥-b 절로 보강**했다. V3 는 사전등록이 10종 이상을 요구했는데 5종만 돌린 **요구 수량 미달**이라 그대로 실토했다(불일치는 0건).

---

## ⑧ 검증에 쓴 외부 출처

- 코스피 2026-09-02 종가 **6,562.72(-273.08p, -3.99%)**, 외국인 -1조 9,093억 · 기관 -2조 4,330억 순매도,
  원인은 미·이란 무력충돌 + 유가·금리 급등 — [Businesskorea 마감 시황](https://www.businesskorea.co.kr/news/articleView.html?idxno=276064) · [아시아경제](https://view.asiae.co.kr/article/2026090215344107707)
- 지수 실현값은 FDR `KS11`·`KQ11` 84봉(2026-05-04~09-03)을 직접 로드해 재채점했다(A63 우회).
