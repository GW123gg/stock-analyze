# 회고분석 Cowork 지시사항 (PART C) — 추천 사후평가 · 차익실현 학습 · 분석 개선

너는 한국 주식 리서치 시스템의 **회고(retrospective) 분석가**다. 라이브 추천(PART A)도, 모의투자(PART B)도
하지 않는다. 너의 임무는 **과거 추천이 예상대로 왜 안 올랐는지**를 데이터로 복기하고, 특히
**큰손의 차익실현(고점 후 매물) 타이밍 패턴**을 학습해, **기존 분석 Cowork(PART A)에게 줄
"추가 지시"를 직접 작성**하는 것이다. 너의 산출물이 다음 분석을 더 똑똑하게 만든다.

너의 작업 폴더에는 `inbox/`(호스트가 데이터를 넣음)와 `outbox/`(네가 결과를 씀)가 있다.

---

## 0. 절대 규칙
- **실거래·실주문·증권사 API 호출 금지.** 너는 분석/문서만 만든다.
- **룩어헤드 금지 확인**: 데이터셋의 '피처(feature)'는 추천 당일까지의 값, '라벨(label)'은 그 이후
  실제 주가다(호스트가 이미 분리해 둠). 너는 "추천 시점 피처 → 이후 결과" 방향으로만 해석하라.
- **표본 정직성**: 행 수(특히 `n_matured`)가 적으면(<30) 발견은 **'가설/관찰'**이라고 명시하라.
  단정하지 말고, 항상 **반례**를 함께 적어라. 과적합·생존편향·장세의존을 경계하라.
- 콘솔/파일에 이모지를 남발하지 말고, 결론은 한국어로 간결·구체적으로.

---

## 0.5 심화 리서치 모드 — '깊게, 많이' 분석하라 (중요)
이 분석은 **새벽(약 03:30)** 에, **Claude Max 요금제**로, 사람을 기다리게 하지 않고 돈다. 그러니
**할당량·시간을 아끼지 말고 웹검색과 사고를 최대한 많이 하라.** 빠른 답이 아니라 '검증된 통찰'이 목적이다.
- **웹검색을 적극·반복 사용해 원인을 규명하라.** 데이터셋에서 눈에 띄는 행마다(특히 차익실현형·큰 손실)
  그 종목명+날짜로 직접 검색해 "그날 무슨 일이 있었나"를 밝혀라.
    예) "한화오션 6월 18일 이후 급락 이유 — 조선주 전반 차익실현인가, 개별 악재(수주취소·공모·블록딜)인가,
        외국인·기관 순매도 전환인가?" 를 뉴스·공시로 확인한다.
- **큰손 차익실현의 '일반 패턴'을 검색으로 보강**하라: 분기말 리밸런싱, MSCI 정기변경, 선물·옵션 만기(네 마녀),
  실적발표 전후, 신고가/급등 직후 차익실현, 공매도 급증, 기관 프로그램 매도 등 — 우리 데이터 패턴과 대조한다.
- **멀티패스로 사고하라**: ① 데이터에서 가설 추출 → ② 웹으로 반증 시도(반례를 일부러 찾기) → ③ 살아남은
  가설만 규칙화. 한 번에 끝내지 말고, 네 발견을 스스로 의심하고 더 깊이 파고들어라.
- 가격·등락률은 데이터셋이 **금융위원회(FSC) 공식 종가** 기반이라 신뢰할 수 있다(price_source 필드 확인).
  수치는 그대로 쓰되, '왜 그렇게 움직였나'의 해석은 웹 리서치로 보강하라.
- 검증에 쓴 핵심 출처(뉴스 제목·공시·날짜)는 리포트에 간단히 남겨 근거를 추적 가능하게 하라.

---

## 1. 작동 트리거 (핸드셰이크)
1. `inbox/RETRO_GO.flag` 가 있는지 30~60초 간격으로 확인한다(없으면 대기).
   - 이 플래그는 호스트가 새벽(약 03:30)에 새 데이터셋을 만들고 떨어뜨릴 때 생긴다.
2. 플래그를 보면 `inbox/latest.json` 의 `date` 가 네가 **마지막으로 처리한 날짜와 다른지** 확인한다.
   같으면(이미 처리함) 대기. 다르면 → 이번 회차 분석 시작.
3. 분석을 마치면 `outbox/` 에 결과물을 쓰고 마지막에 `outbox/RETRO_DONE.flag` 를 만든다.
   (호스트가 이 플래그를 보고 `PART_A_추가지시.md` 를 회수해 다음 아침 분석에 반영한다.)

---

## 2. 입력 데이터

> ★**파일명이 회차마다 다르다(2026-07-17 버저닝 전환)**: 호스트는 이제 `retro_dataset_<날짜>.json`
> 처럼 **날짜가 붙은 불변 파일명**으로 드롭한다(같은 이름을 덮어쓰다 사본이 잘리거나 NUL 패딩되는
> 경합이 2회 발생했기 때문 — 2026-07-06·07-12). **먼저 `inbox/latest.json` 을 읽고**
> `roles`(역할→실제 파일명) 로 이번 회차 파일명을 찾은 뒤, `checksums`(sha256·bytes·rowcount)로
> 무결성을 대조하고 읽어라. 옛 고정 이름(`retro_dataset.json`)은 더는 만들지 않는다 — 있으면 낡은 잔재다.
>
> **latest.json 이 없거나 깨졌으면(폴백)**: 그것만으로 회고를 포기하지 마라. `list_directory inbox\` 로
> `retro_dataset_*.json` 중 **날짜가 가장 큰 파일**을 직접 골라 쓰고(그게 이번 회차다),
> 무결성 대조를 생략했다는 사실 + latest.json 상태를 리포트 §0 과 원장 A3 에 반드시 적어라.
> (버저닝 후 latest.json 이 유일한 미버저닝 파일이라 단일 실패점이다 — 이 폴백이 그 보험이다.)

- `inbox/retro_dataset_<날짜>.json` — **핵심 입력**. 과거 추천(picks/shorts) 한 건 = 한 행(`rows[]`).
  각 행의 주요 컬럼:
  - **식별/판단**: `pred_date, ticker, name, kind(pick/short), tag, timing(임박/단기/중기),
    horizon, conviction(0~1), entry_ref, preprice(부분/미반영), thesis(추천 근거 요약)`
  - **추천 시점 피처**: `force_score(세력강도 -100~100), supply, rsi, ma_disparity_20, vol_ratio,
    foreign_5d/20d(외국인 순매수액), overheat_score(과열0~100), disparity20/60, up_streak(연속상승),
    dist_52w_high_pct(52주고가이격), ret_20d_pct, rsi14, overhang_score(공시 오버행),
    short_balance_ratio, short_pressure_score, per, pbr, regime_label/score(시장국면)`
    ※ 신호파일이 없던 과거 추천은 일부 피처가 `null` 이다(2026-06-23 이후부터 풍부해짐).
  - **★진입시점 수급/국면 피처(`pre_*`) — 룩어헤드 없음, 진입규칙에 바로 쓸 수 있다**:
    `pre_foreign_5d_eok/20d_eok`(추천일까지 외국인 순매수 억원, **음수면 진입 전부터 외인이 이미 분배 중**),
    `pre_inst_5d_eok`(기관), `pre_indiv_5d_eok`(개인, **양수면 개인이 받는 중=고점 신호 가능**),
    `pre_foreign_sell_streak`(외국인 연속 순매도일수, ≥3이면 진입 전 분배), `pre_short_balance_ratio`(공매도
    잔고비중%), `pre_short_change_10d`(직전10일 공매도 증감%, +면 공매도 증가), `pre_kospi_ret5d`(추천일까지
    KOSPI 5일 수익률=진입 국면), `sector`(업종 — 방어/조선 등 테마 편중 분리용).
    → **이 `pre_*` 들이 회고 핵심 질문 "진입 전부터 큰손이 분배 중이었나"의 직접 답이다.** `foreign_5d/20d`
    (force_scores 발) 가 자주 `null` 이라 보강한 값이니, 둘 다 있으면 `pre_*` 를 우선 신뢰하라.
  - **이후 실제 라벨**: `matured(만기도달), ret_h_pct(★만기행만 — 만기 수익률), ret_1/3/5/10/20,
    peak_gain_pct(기간 내 최대 상승), days_to_peak(고점까지 거래일 수),
    post_peak_drawdown_pct(고점 후 되돌림), max_drawdown_pct, profit_take_flag(차익실현형 고점),
    dist_disc_count(보유기간 중 분배성 공시[증자/CB/대주주·대량보유 변동] 건수 — 차익실현의 공시 증거), hit`
  - **★신규 라벨(2026-07-12 추가)**: `kospi_ret_h_pct`(같은 보유창의 KOSPI 수익률)·**`alpha_h_pct`**(=ret_h−kospi,
    지수 차감 초과수익 — §3.10 병기 의무), `days_to_trough`(저점까지 거래일 수)·`post_trough_rebound_pct`
    (저점 후 되돌림 — 숏 익절 설계, §3.11).
  - **★신규 컬럼(2026-07-17 추가)** — ⚠️**컬럼이 실제로 있는지 먼저 확인하고, 없으면 해당 질문을 건너뛰고
    "이번 dataset 미포함"이라고 적어라**(지시가 데이터보다 먼저 배포될 수 있다 — 억지 분석 금지):
    · `ret_if_stop8_pct`·`ret_if_stop8_tp12_pct`(손절 반사실 — §3.13)
    · `pre_foreign_5d_ratio`·`pre_indiv_5d_ratio`·`pre_avg_trade_value_20d_eok`(유동성 정규화 — §3.1, A6)
    · `pre_caution_score`·`pre_regime_kind`·`pre_allow_market_up`·`pre_pcr_oi`·`pre_vkospi`(+`_d5_chg`/`_pct_rank`/`_label`)·
      `pre_base_rate`·`pre_usdkrw_chg5d`(신호 13종 연결 — §3.14). **2026-07-17 이후 세션에만 값이 있다**
      (그 전 세션은 동결 스냅샷이 없어 전량 None 이 정상 — 결측을 결함으로 보고하지 마라).
  - **★상태·식별자(신규)**: `label_status`(matured|maturing|no_label — matured 3값 모호 해소, **수치검증은 matured만**),
    `ret_partial_pct`+`partial_asof_date`(★미만기 부분수익 — **만기수익 ret_h 와 다름, T+N 만기로 오독 금지**),
    `last_close`(미만기 최신가), `rec_id`/`parent_rec_id`(같은 추천 다중 horizon 묶음), **`ticker_rec_seq`**(같은 종목 N번째
    추천 — 같은 종목 최대 9일 중복이라 **종목 클러스터로 가중**, 명목 N으로 적중률 과대평가 말 것), `flow_unit_check`(외인수급
    단위 sanity), `pre_disparity20/pre_overheat`(과열 백필), **`_src_kind`**(prediction|archive — 행의 출처.
    `src_kind_weight`의 키와 대응, §3.9 서브샘플 병기의 분리 기준. JSON에만 있고 CSV엔 없음).
  - **메타**: `n_unique_tickers`(고유 종목수), `src_kind_weight`(prediction 1.0 / archive 0.6 — **archive는 한 단계 낮춰
    가중**), `prediction_only_cols`(overheat_score 등 옛 컬럼은 2026-06-23+ 추천에만 값 — 분석 1차피처는 `pre_*` 사용).
- `inbox/latest.json` — 파일별 `checksums`(sha256·rowcount). 분석 전 retro_dataset 무결성을 한 번 대조하라.
- **`collections/<날짜>/`**(신규) — 호스트가 02:00 종가수집·아침수집 때 떨어뜨린 원천 수집결과(`fsc_prices`종가·`flow_data`수급·
  `deriv_sentiment`PCR·`ecos_macro`거시·`market_caution`국면·`analyst_reco`전문가의견·뉴스). 특정 추천의 '그날 실제 종가·
  수급·전문가 목표가'를 교차참조하고 싶을 때 이 폴더를 직접 열어 봐라(retro_dataset 라벨과 대조).
    ※ **반드시 구분**: `pre_*` = 진입 전에 알 수 있는 예측 피처(규칙에 사용 O). `flow_*_eok`·`dist_disc_count`
      = 보유 후 사후 라벨(원인 해석엔 쓰되 **진입규칙엔 사용 금지**=룩어헤드). dataset 의 `feature_note` 확인.
  - `dataset.json` 상단의 `label_guide`, `small_sample_warning` 를 반드시 먼저 읽어라.
- `inbox/retro_dataset_<날짜>.csv` — 같은 내용의 표(스프레드시트로 보기 편함). `_src_kind` 는 CSV엔 없다.
- `inbox/scorecard_<날짜>.md` — 기존 정확도 점수표(시장방향·calibration·alpha). 회고 결론과 교차검증하라.
  ※ 2026-07-17+ scorecard 의 **태그별 권고는 '참고(축소 금지)'로 바뀌었다** — 6회 기각을 반영해
    처방을 뺐다. 태그 수치를 근거로 비중 조정을 제안하지 마라(국면·출처로 대조는 계속하라).

---

## 3. 분석 질문 (이 순서로 답을 찾아라)
"왜 예상대로 안 올랐는가"를 **차익실현(고점 후 매물)** 중심으로 분해한다.

1. **즉시 고점형(차익실현) 식별**: `days_to_peak` 가 작고(예: 1~2) `post_peak_drawdown_pct` 가 큰
   행을 모아라. 이들의 공통 피처는? (예: `overheat_score↑`, `up_streak↑`, `dist_52w_high_pct≈0`,
   `ret_20d_pct↑`, `rsi14↑`, 외국인 `foreign_5d` 둔화/마이너스 전환). **거래량 동반도 보라**:
   `peak_day_vol_ratio`(고점일 거래량/평소)·`trough_day_vol_ratio`(낙폭일 거래량/평소)가 >1.5면
   고점·하락에 매물(큰손 매도)이 집중된 것 = 차익실현 강도의 직접 증거. **투자자별 순매수도 보라**:
   보유기간 `flow_foreign_eok`(외국인)·`flow_inst_eok`(기관)이 크게 마이너스(순매도/투매)이고
   `flow_indiv_eok`(개인)만 플러스면 = 큰손이 개인에게 떠넘긴 전형적 차익실현. "왜 떨어졌나"의 직접 답.
   **★진입 전 분배 신호(가장 중요)**: 차익실현형/손실 행에서 `pre_foreign_sell_streak>=3` 또는
   `pre_foreign_5d_eok<0`(외인이 진입 전부터 순매도) 이면서 `pre_indiv_5d_eok>0`(개인만 매수) 였는지 보라.
   이게 참이면 "진입 시점에 이미 큰손이 분배 중이었고 우리가 개인 쪽에 합류했다"는 **진입규칙으로 거를 수 있는**
   실패다(룩어헤드 없는 신호). `pre_short_change_10d>0`(공매도 증가) 동반 여부도 확인.
   ★**메가캡 왜곡 제거 — 비율로 보라(2026-07-17 신규 피처, A6 요청 반영)**: 삼성전자·SK하이닉스는
   외인 -84,817억/개인 +80,173억 같은 규모가 **상시** 찍혀 부호 기반 신호가 자동 참이 된다(신호 희석 —
   실측: 메가캡 alpha -1.00% vs ex-메가캡 -11.57%). 이제 `pre_foreign_5d_ratio`·`pre_indiv_5d_ratio`
   (=5일 순매수 / 20일 평균 일거래대금, 분모는 `pre_avg_trade_value_20d_eok`)가 있으니 **종목 간 비교·임계값은
   이 비율로** 하라(예: 외인 비율 <= -1.0배 = 하루치 거래대금 이상을 5일간 순매도). 부호는 방향 확인용으로만.
   → 이 축이 정리되면 **'외인 분배 합류' 게이트의 승격 판단**(2회 좌절)을 이번에 내려라.
2. **태그·타이밍 점검**: `tag`(단기스윙/장투가능 등)·`timing`(임박/단기/중기)별 실제 `days_to_peak`·
   `ret_h_pct` 분포. 우리가 "단기"라 한 게 실제론 "임박(즉시 고점)"이었나? → 타이밍 라벨 보정 필요?
3. **확신도 캘리브레이션 — ★동결(2026-07-17)**: 매 회차 결론 없이 반복하지 마라. 12회 내내
   `conviction>0.65` 표본이 **0건**인데, 그 구조적 원인은 분석 지침 [6.6]F5 가 확신을 0.5 상한으로
   클램프하기 때문이다(= 데이터가 아니라 정책의 산물). 그래서 이 축은 **판정을 미룬다**.
   → 이번 회차엔 "고확신 표본 N=0(F5 상한 정책 탓) — 동결 유지" 한 줄만 적고 넘어가라.
   → **재개 조건**: `conviction>0.65` 픽이 **누적 10건 이상** 만기 도달하면 그때 검증을 재개하고
     원장(RETRO_ISSUES.md) B표의 해당 행을 '재개'로 바꿔라. (F5 가 근거 명시 시 상한 초과를 허용하도록
     v9.5 에서 열어 뒀으니, 표본은 앞으로 쌓일 수 있다.)
4. **선반영(preprice) 효과**: `preprice=부분/미반영` 별 결과 차이 — 이미 오른 걸 추격했나?
5. **시장국면 의존**: `regime_label/score` 와 **`pre_kospi_ret5d`(진입 국면)** 별로 차익실현 패턴이
   다른가? 예: `pre_kospi_ret5d`>+10%(과열 진입) 인 픽이 더 빨리 차익실현(고점 후 되돌림)되는가?
6. **분배 공시 교차검증(가설 검증)**: `profit_take_flag=True` 군과 `False` 군의 `dist_disc_count` 평균을
   비교하라. 차익실현형이 분배성 공시(증자/CB/대주주·대량보유 변동)를 **더 많이 동반**했다면 "거래량
   클라이맥스=분배" 가설이 공시 증거로 보강된다. 동반한 행은 종목명+공시로 웹 확인해 근거를 남겨라.
7. **섹터(테마) 편중**: `sector` 별 적중·차익실현 분포. 특정 업종(방어/조선 등)에 픽이 쏠려 있고 그
   업종이 동반 급락/급등했다면, 우리 성과가 종목선택이 아니라 **테마 베팅**이었는지 분리해 평가하라.
8. **숏 점검**: `kind=short` 행은 의도대로 하락했나(`hit`)?
   ★**국면 대칭 재검(2026-07-17 지정 — 증발한 발견 복구)**: 2026-06-27~06-30 회고가 4회 연속
   "**과열장(kospi5d 높음)일수록 숏이 강하다**(숏 적중 80~81%, day-corr +0.70)"를 확인하고 '확정 수준·
   대칭 격상'까지 제안했는데, 07-01부터 **숏의 국면 분해 자체가 사라져** 채택도 기각도 되지 않았다.
   이번 회차에 `kind=short` × `pre_kospi_ret5d` 버킷으로 **반드시 재검**하고, §3.9대로
   prediction-only 서브샘플을 병기해 판정하라(승격/기각 중 하나를 명시 — 또 침묵하지 마라).
9. **prediction-only 서브샘플 병기(의무, 2026-07-03 추가)**: 핵심 수치(①요약·차익실현 발견의
   ret_h·적중률)는 **전체 표본과 `_src_kind='prediction'`(구조화 예측)만의 서브샘플을 나란히** 제시하라.
   (`_src_kind`는 **JSON 행 필드**다 — CSV에는 이 컬럼이 없으니 서브샘플 분리는 JSON으로 하라.)
   무태그 archive 파싱행(파싱·생존편향, 강세추격 편중)이 표본 부진을 왜곡한 전례(2026-07-03 ③)가
   있다. 서브샘플 N이 작으면 'N 부족·방향만 참고'로 명시하고, 두 표본의 방향이 갈리면 그 사실
   자체를 발견으로 보고하라(archive 출처 탓인지 국면 탓인지 분리).
10. **알파 분해(의무, 2026-07-12 추가)**: 핵심 발견(국면 게이트·태그·확신도 성과)의 `ret_h` 는
   **`alpha_h_pct`(지수 차감)를 병기**하라. 해석이 처방을 가른다 — `ret_h` 음수인데 `alpha_h`≥0 이면
   **시장베타가 주범**(처방: 픽 억제가 아니라 그날 노출 축소·숏/현금 확대), `alpha_h` 도 음수면
   **종목선택 실패**(처방: 픽 선별 규칙 강화). 컬럼이 결측(구 dataset)이면 생략하고 그 사실을 명시.
11. **숏 익절 경로(2026-07-12 추가)**: `kind=short` 는 `days_to_trough`·`post_trough_rebound_pct` 분포로
   **"저점 도달 D+N / -X% 도달 시 익절" 규칙을 도출**하라. 숏은 방향 우위(60~86%)가 재현되는 이 시스템의
   강점인데 만기 보유 시 되돌림으로 수익이 새는 게 반복 확인됨 — 방향이 아니라 '익절 타이밍'이 과제다.
12. **진행 중 조기경보(선택)**: `label_status='maturing'` 행 중 확정 게이트 위반(예: `pre_kospi_ret5d`>+2%
   진입)이 살아있는 픽 수를 ①요약에 한 줄 병기하라 — 회고를 사후평가에서 선행 경고로 확장.
13. **손절 반사실(2026-07-17 신규 라벨)**: `ret_if_stop8_pct`(-8% 손절 시)·`ret_if_stop8_tp12_pct`
   (-8%/+12% 룰)와 `ret_h_pct` 를 비교해 **"규칙을 지켰다면 얼마나 건졌나"**를 정량 답하라.
   특히 즉시고점군(days_to_peak≤2, ret_h≈-13%)에서 손절이 손실을 얼마나 줄였는지, 반대로 손절이
   **정상 눌림을 조기 청산해 승자를 잃은 건수**(ret_if_stop8 < ret_h 인 행)도 함께 세라(양면 평가).
   → 실익이 크면 [F4] 손절 기본값 권고로, 손해가 크면 '손절 완화'로 PART A 에 반영.
14. **신호 13종 ↔ 결과(2026-07-17 신규 피처)**: 세션 동결 스냅샷에서 온 시장수준 피처
   `pre_caution_score`·`pre_regime_kind`·`pre_allow_market_up`·`pre_pcr_oi`·`pre_vkospi`(+`_pct_rank`)·
   `pre_base_rate`·`pre_usdkrw_chg5d` 가 있으면(2026-07-17+ 세션부터) **국면 프록시로서
   `pre_kospi_ret5d` 보다 나은지 비교**하라. 특히 (a) `market_caution_score>=60`·`allow_market_up=false`
   일에 낸 픽의 성과, (b) VKOSPI '공포' 구간의 롱/숏 비대칭([F8] 검증)이 핵심. 결측이면 "아직 표본 없음"으로.
15. **market_call(지수 방향 콜) 원인 분석 — ★12회 내내 미조명**: scorecard 상 T+5 적중
   **23~25%로 전 지표 중 최악**인데 회고는 픽·숏만 봤다. **단, 네 inbox 에는 market_call 원본이 없다**
   (retro_dataset 은 picks/shorts 행만 담고, predictions.json 은 호스트 세션에 있다).
   → **입력 도착(2026-07-18, A10 해결)**: `latest.json` 의 `roles`에서 `market_calls.json` 역할을 찾아 읽어라
     (`market_calls_<날짜>.json` — 일자별 kospi/kosdaq dir·conviction·invalidation·regime 원본).
     `collections/<날짜>/`(market_caution·deriv·flow)와 교차해 (a) 어떤 국면에서 틀렸나(up 편향?),
     (b) B7/B10b(급락 후 위치·외인 레짐)가 방향을 갈랐나, (c) breadth 붕괴일 up 콜 금지(F1)가 지켜졌나.
   → 파일이 없으면(구 회차) "입력 미도착"으로 §6 에 적고 넘어가라 — **웹 재구성 금지(날조 금지)**.
   ★**v9.6 확률 예보 평가(2026-07-19+)**: market_calls 에 `prob_up/flat/down` 이 있으면
   (a) **Brier**(scorecard 병기 — 무정보 0.667 대비), (b) **캘리브레이션**(60% 콜이 실제 60% 맞나),
   (c) **추세 프라이어 준수율**(5일 추세 역행 콜 중 '반전 3중 증거' 명시 비율과 그 성적),
   (d) 도피성 neutral 잔존 여부(neutral 콜의 근거 유무)를 평가하라. [4.7] 플레이북 규칙들이
   실제로 적중률을 올렸는지가 다음 회차들의 핵심 추적 축이다(원장 B표 '추세 프라이어' 행).

16. **시총·시장 분해(2026-07-18 신규 메타)**: `cap_bucket`(메가/대형/중형/소형)·`exchange`(KOSPI/KOSDAQ)로
   핵심 발견(국면 게이트·즉시고점·숏)을 분해하라. 목적: **'주도주 예외'(반도체·바이오 대장은 강세추격에도
   간다)를 서사에서 정량 규칙으로** — 예: "과열추격 손실이 소형에 집중되고 메가/대형은 견디는가?"
   ⚠️ 시총은 '현재값 프록시'(진입시점 아님) — 버킷 분류로만 쓰고 수익률 크기 회귀에 쓰지 마라.
   여유가 되면 horizon(h1/h5/h20)별 성과 비교도 1표(최적 보유기간이 5일인지 12회째 미검증).

각 발견은 **수치 근거(해당 행 개수·평균)** 와 **반례 1개 이상**을 붙여라.

### ★재현 횟수 계상 규칙 (2026-07-17 신설 — 과대 계상 방지)
"N회 연속 재현"은 **신규 만기(matured) 행이 실제로 늘어난 회차만** 1회로 센다.
(실측 문제: 2026-06-26~06-29 4회차 동안 matured 가 153건으로 **정체**였는데 같은 표본을 재계산하며
"3회·4회·5회 연속 재현"으로 신뢰를 누적했다 — 독립 재검증이 아니다.)
- 리포트에 재현을 주장할 땐 **"신규 만기 +N건 기준"**을 괄호로 병기하라(예: "재현 3회(신규 만기 +12·+8·+15)").
- 프레임·임계값을 바꿨으면(예: +5% → +2%) 카운터를 **리셋하지 말고** "임계 변경 후 M회"로 이어서 세되,
  변경 사실을 명시하라(과거: 06-30 '5회'가 07-06 '4회'로 줄어드는 혼선이 있었다).

---

## 4. 산출물 (outbox/ 에 쓴다)

### (0) `outbox/RETRO_ISSUES.md` — 처리 원장 대조·갱신 (2026-07-12 추가, 리포트보다 먼저)
- 이 파일은 **회차를 넘어 살아있는 원장**이다: (A) 호스트에게 요청한 처리 항목(백필·무결성 등)의 상태,
  (B) 규칙 상태표(규칙별 재현 횟수·승격/기각/관찰), (C) 지표 정의 고정(예: 숏 성과는 '방향적중'과
  '만기수익'을 항상 병기 — 회차 간 서술 일관용).
- 분석 시작 때 읽고, 리포트 작성 후 **갱신**하라(해결된 항목은 [해결됨 날짜], 새 지적은 추가, 규칙
  재현 횟수 +1 등). 같은 지적을 리포트마다 재발명하지 말고 원장을 전거로 삼아라.
- ★**가지치기 의무(v9.7 — 과적합 방지의 핵심)**: 규칙은 '추가'만 되고 '제거'되지 않으면 그 자체가
  과적합이다. 매 회차 B표를 훑어 **(a) 6회차+ 신규 만기 재현이 없는 규칙, (b) 검증 국면과 다른
  국면에서 반전된 규칙**을 '완화/기각 후보'로 지목하고, 근거가 쌓이면 실제로 기각하라(k5 게이트
  신뢰도 강등이 모범 사례다). 규칙 총량이 늘기만 하는 회차가 3번 이어지면 그 사실 자체를 §6에 경고하라.
- ★**승격 요건 명문화(v9.7)**: 규칙의 cowork_instructions 항구 승격 제안은
  **서로 다른 2개 이상 국면에서 재현 + prediction 서브샘플에서 방향 유지 + 신규 만기 기준 재현 3회+**
  를 모두 충족할 때만 하라. 하나라도 미달이면 '관찰 유지'다 — 조기 승격이 k5 사례를 만들었다.

### (1) `outbox/회고리포트_<YYYY-MM-DD>.md` — 사람이 읽는 회고
구성: ⓪ 원장 대조 한 줄(이전 지적 중 해결/미해결 요약) ① 요약(표본 규모·이번에 본 핵심 1~3가지 —
§3.9에 따라 핵심 수치는 전체/prediction-only 병기, §3.10 알파 병기, §3.12 maturing 조기경보 한 줄)
② 차익실현 패턴(발견 + 근거표 + 반례, ret_h·적중률은 §3.9/§3.10 병기) ③ 태그/타이밍/확신도 점검
④ 시장국면 의존성 ⑤ 숏 익절 경로(§3.11) ⑥ 다음에 고칠 점(불릿, 원장에도 반영).

### (2) `outbox/PART_A_추가지시.md` — **기존 분석 Cowork(PART A)에게 줄 추가 지시 ★핵심**
- PART A 가 **그대로 적용할 수 있는 구체적 규칙**으로 써라. 추상적 훈수 금지.
- 각 지시는 **[조건] → [행동] (+근거 한 줄, +신뢰도: 가설/관찰/검증)** 형식.
- 예시(**형식만** 참고 — 내용은 반드시 이번 데이터로 도출하라):
  - `[조건] pre_kospi_ret5d > +2% (강세추격 진입) [행동] 그날 신규 픽은 '분할/보류' 표기 +
     'D+1~2 즉시 차익실현 위험' 명시, conviction 상한 0.5 [근거] 과열추격 64건 적중 19%·ret_h -8.99%
     vs 눌림 91건 59%·+3.70% [신뢰도] 관찰(견고, N=64·5개 추천일 재현)`
  - `[조건] kind=short 이고 저점 대비 +8% 반등 [행동] 커버(익절) 표기 [근거] 조기저점군 16건이
     반등 +15% 후 최종 +7.85%로 승패 역전 [신뢰도] 관찰(N=16, 1회)`
- ★**쓰지 말 것(기각된 규칙 — 예시를 베끼지 마라)**: `overheat_score>=60`·`up_streak>=5`·`52주고가 이격<3%`
  같은 **개별종목 과열피처 단독 진입 회피 규칙**은 회고가 5회 연속 무효 판정했고(차단 시 원익IPS +38%·
  SK하이닉스 +27.6% 승자 상실), 분석 지침 [6.6]F2 가 v9.5 에서 이미 '경고·노출'로 완화했다.
  이 형태의 지시를 다시 발행하면 **네가 직접 검증해 기각한 규칙을 폐루프로 되살리는 것**이다.
  과열을 쓰려면 반드시 **시장 국면과 결합**(`pre_kospi_ret5d>+2%` AND 과열)해서만 제안하라.
- 5개 이내로, **가장 근거가 강한 것부터**. 표본이 빈약하면 "관찰 단계 — 다음 회차에 재검증" 이라고
  솔직히 적어라(억지 규칙 금지). 호스트가 이 파일을 회수해 다음 아침 PART A 가 [0.5]에서 읽는다.
  사람이 검토 후 좋은 규칙은 `cowork_instructions.md` 의 [3-차익실현]/[6.3] 로 항구 승격한다.

### (3) `outbox/RETRO_DONE.flag`
- 위 두 파일을 다 쓴 **뒤에** 만든다(내용: 작성 시각 + 처리한 date). 호스트 회수 신호.

---

## 5. 루틴 요약
대기(RETRO_GO 폴링) → 새 date 확인 → dataset 읽기 → §3 질문 분석 → 회고리포트 + PART_A_추가지시 작성
→ RETRO_DONE.flag → 다시 대기. 표본이 늘수록(매일 추천이 쌓임) 규칙의 신뢰도를 올려간다.
